# Test Plan — Live-Odds Display & Fetching
Generated by /plan-eng-review on 2026-05-31
Branch: master
Repo: cs2-major-sim (swiss-mc)

## CRITICAL regression (iron rule — write first)
- `tests/test_epistemic.py::test_point_probs_valid_under_epistemic` — with `market_blend={"1-9": (0.5, 0.05)}`
  (var>0 → K=12), assert `max(run_mc(...).p_advance().values()) <= 1.0` AND
  `abs(sum(p_advance().values()) - 8.0) < 0.05`. Proves the `n = len(sample)` fix. Without the fix this
  asserts 96.0 != 8.0 and fails (RED-first).

## Engine (montecarlo.py)
- `n = len(sample)` correct under K>1; byte-identical for K=1 (re-run `test_rating_only_noop_unchanged`).
- D2: `band_*_sampling ⊆ band_*` under K>1; `band_*_sampling == band_*` when K=1 (single-tone collapse).
- Regression-verify optimizer + live still pass (they read `result.sample`, should be unaffected).

## UI helpers (ui/render.py — pure, unit)
- `fmt_age`: just-now / 4m / 6h / 2d boundaries; bad/empty ISO → handled.
- `is_stale`: crosses the 2h (7200s) boundary correctly; bad/missing timestamp → True.
- `provider_labels`: oddspapi→Pinnacle, polymarket→Polymarket, kalshi→Kalshi; unknown dropped; order preserved.
- `priced_ids`: union of ids from "lo-hi" keys; empty/malformed → empty set.
- `source_spread`: max(p)-min(p); single source → 0.
- `ci_bar_two_tone_html`: outer==inner output equals `ci_bar_html` (rating-only single-tone); XSS guards intact.

## Cache schema (scripts/fetch_odds.py + ui/odds_loader.py)
- fetch writes v2 `blended[*].sources = [{book, p}, ...]` from the per-provider `group`.
- loader accepts v1 (no `sources` → counts-only) AND v2; malformed `sources` dropped fail-soft.
- `_SUPPORTED_VERSION` regression: a v1 fixture still loads (back-compat).

## App (AppTest — tests/test_app.py)
Affected: the controls column (Fetch button) and the header strip + per-team probs table.
- Fetch click → outcome persisted across `st.rerun()` (Bug A): success/info/error visible after the rerun.
- Header panel copy: OFF (no cache) / LIVE (recorded v2 fixture) / STALE (old `fetched_at`).
- Two-tone bars + `[mkt]`/legend + "Why the books disagree" drill-down present ONLY under a non-empty cache;
  drill-down sorted by spread (widest first).
- Rating-only run (no cache): renders identically to today — single-tone bars, no panel content, no drill-down.

## Fixtures
- `tests/fixtures/odds_cache_live_v2.json` — non-empty `blended`, `var>0`, `sources` populated, recent `fetched_at`.

## Critical paths (must work end-to-end)
- Fetch → cache written → header flips to "live" → probs table shows valid (<=100%) two-tone bars.
- Stale cache → "may be stale" surfaces → re-fetch refreshes age.
- Missing/empty cache → fully fail-soft rating-only, zero behavior change vs today.
