# v1.0 Milestone Audit - swiss-mc

**Audit date:** 2026-05-30
**Auditor:** Claude (integration audit pass)
**Scope:** Phases 1-5 (v1.0). Phase 6 (V3-01..04) is DEFERRED to a future v3 milestone - listed as out-of-scope, not gaps.
**Branch:** phase-1-backtest-closeout (clean working tree, all Phase 1-5 work merged)

---

## 1. Test Suite

    uv run pytest -q  => 113 passed in 186.77s
    uv run pytest -q tests/test_backtest_budapest_2025.py  => 3 passed in 0.27s [GATE-01 GREEN]

No failures, no unexpected skips. GATE-01 is confirmed GREEN: the engine reproduces Budapest 2025 Stage 1 actual round-by-round pairings exactly (R1-R5), including the 6-team R4/R5 groups that exercise Valve verbatim 15-row priority table.

---

## 2. Requirements Coverage

### Phase 1 - Engine / Backtest Gate (ENG-01..09, MC-01..06, PROB-01..05, GATE-01..05)

| REQ-ID | Status | Evidence |
|--------|--------|----------|
| ENG-01 | SATISFIED | build_round1_pairs uses range(1,9); no second hardcoded copy; verified by backtest |
| ENG-02 | SATISFIED | swiss.py loop has no round-number branch; single code path rounds 2+ |
| ENG-03 | SATISFIED | engine/probs.py:71 sum(o.wins-o.losses); test_difficulty_formula asserts result is 2 not 3 |
| ENG-04 | SATISFIED | rank_group key (-difficulty(t), t.seed); test_fold_uses_difficulty_rank proves load-bearing |
| ENG-05 | SATISFIED | _least_disruptive_rematch fallback + warning; test_no_valid_matching_fallback PASS |
| ENG-06 | SATISFIED | _is_bo3 checks wins==2 or losses==2; test_bo3_placement_and_termination PASS |
| ENG-07 | SATISFIED | opps uniqueness; test_locked_results_honored PASS |
| ENG-08 | SATISFIED | _play checks locked key before sampling; records history in opps; test PASS across different rng seeds |
| ENG-09 | SATISFIED | DEFAULT_N_CHUNKS=20; test_reproducibility_survives_n_chunks_default PASS |
| GATE-01 | SATISFIED | test_backtest_budapest_2025 GREEN (3 passed); seeds RESOLVED from Valve VRS snapshot 2025_11_03; TODO moved to completed/ |
| GATE-02 | SATISFIED | test_difficulty_formula PASS in test_probs.py and test_swiss.py |
| GATE-03 | SATISFIED | test_forced_rematch_pairing and test_no_valid_matching_fallback PASS |
| GATE-04 | SATISFIED | Budapest R4/R5 are 6-team groups; backtest reproduces them exactly (seeding_validation in fixture confirms GATE-04 exercised) |
| GATE-05 | SATISFIED | BACKTEST_PASSED=True in ui/state.py; badge caveated pending Cologne seed confirm (second gate) |
| MC-01 | SATISFIED | run_mc_progressive hot loop; test_invariants PASS at N=50k |
| MC-02 | SATISFIED | wilson() closed-form; test_wilson_closed_form PASS to 1e-12 |
| MC-03 | SATISFIED | Sums structurally exact 2/8/2; test_invariants PASS |
| MC-04 | SATISFIED | Result.sample accumulates; test_record_sample_retained PASS (len==N) |
| MC-05 | SATISFIED | run_mc_progressive yields Partials; run_mc drains; test_progress_partials_yielded PASS |
| MC-06 | SATISFIED | series() in probs.py; single rng.random() < p_a draw in _play; test_bo3 PASS |
| PROB-01 | SATISFIED | engine/probs.py:29 logistic with S=40; test_p_map PASS |
| PROB-02 | SATISFIED | series_prob() passthrough; test_odds_override_passthrough PASS; replicated in _play via market_series_prob |
| PROB-03 | SATISFIED | epistemic_draws wraps chunk loop in run_mc_progressive; test_band_wider_than_wilson confirms outer spread |
| PROB-04 | SATISFIED | beta_moment_fit clamps strictly; test_epistemic_stub_and_beta_clamp PASS |
| PROB-05 | SATISFIED | _union_band; test_epistemic_band_collapses_to_wilson PASS to 1e-12; test_band_stable_in_N PASS |

**Phase 1 coverage: 25/25 SATISFIED.** GATE-01/04/05 are now GREEN. Phase 1 VERIFICATION report was written before the backtest resolved (states BACKTEST_PASSED=False) - expected; shipped code and tests confirm all three gates pass.

---

### Phase 2 - Streamlit UI Shell (UI-01..07, DX-01..05)

| REQ-ID | Status | Evidence |
|--------|--------|----------|
| UI-01 | SATISFIED | st.segmented_control wired to Mode; test_mode_toggle_reorders_main_column PASS |
| UI-02 | SATISFIED | freeze_locked real positional arg (not underscore-excluded); 5-tuple session cache key; test_locked_in_cache_key PASS |
| UI-03 | SATISFIED | _drive_progress iterates run_mc_progressive at default n_chunks=20; test_progress_preserves_reproducibility PASS |
| UI-04 | SATISFIED | ci_bar_html in every cell of _render_probs_table; test_ci_bars_on_every_cell_after_run asserts >=48 bars |
| UI-05 | SATISFIED | Four states; validate_ratings gate before engine; test_bad_rating_blocks_run PASS |
| UI-06 | SATISFIED | STATUS palette tested; test_status_palette_never_red_green PASS |
| UI-07 | SATISFIED | BACKTEST_PASSED=True; caveated string exact; badge caveated while seeds unconfirmed; test_trust_badge_wording asserts BACKTEST_PASSED is True |
| DX-01 | SATISFIED | 16 teams, seeds_confirmed=false; test_zero_config_first_run PASS; httpx absent (ModuleNotFoundError) |
| DX-02 | SATISFIED | _render_header_strip; test_seed_banner and test_seed_banner_dismissable PASS |
| DX-03 | SATISFIED | pyproject.toml requires-python >=3.12; headless launch confirmed |
| DX-04 | SATISFIED | test_readme_quickstart_and_cold_start_drill PASS |
| DX-05 | SATISFIED | All three provider keys in .env.example; test_env_example_present_and_gitignored PASS |

**Phase 2 coverage: 12/12 SATISFIED.** Note: Phase 2 VERIFICATION report states BACKTEST_PASSED=False (correct at time of that report). Current ui/state.py correctly reads BACKTEST_PASSED=True.

---

### Phase 3 - Pick'Em Optimizer (OPT-01..05)

| REQ-ID | Status | Evidence |
|--------|--------|----------|
| OPT-01 | SATISFIED | ballot_a in engine/optimizer.py; docstring updated to greedy per-bucket marginal baseline per Phase 3 V&V Finding 1 |
| OPT-02 | SATISFIED | ballot_b + _neighbors includes _rebucket moves (V&V Finding 2 fix); test_ballot_b_is_full_neighborhood_local_optimum PASS |
| OPT-03 | SATISFIED | Ballot.__post_init__ validates 2/6/2; ballot_diff computes diff; test_ballot_columns_lists_names_and_marks_diff PASS |
| OPT-04 | SATISFIED | p_ge5 over Result.sample; test_pge5_known_answer (hand-computed 0.5) PASS; hero = recommended_pge5 from OptimizerOutput |
| OPT-05 | SATISFIED | correlated_03_in_r1 reuses build_round1_pairs (no seed re-derivation); id-lookup fixed in V&V Finding 3; test_correlated_03_warning_fires_and_b_avoids_it PASS |

**Phase 3 coverage: 5/5 SATISFIED.** Minor: ui/render.py:195 still labels Ballot A as 'A - Max E[correct]' while engine/optimizer.py docstring was updated to 'greedy per-bucket marginal baseline'. Render and test are mutually consistent; no integration break (WARNING-01 below).

---

### Phase 4 - Conditional Re-Sim / Live Mode (RESIM-01..04)

| REQ-ID | Status | Evidence |
|--------|--------|----------|
| RESIM-01 | SATISFIED | locked_dict(KEY_LOCKED) -> freeze_locked -> 5-tuple cache key; test_live_lock_changes_cache_key and test_live_lock_moves_p_advance PASS |
| RESIM-02 | SATISFIED | classify_pick, pge5_delta, _render_live_hero, _render_status_chips; test_live_status_chips_render and test_live_delta_anchor_uses_pre_key PASS |
| RESIM-03 | SATISFIED | validate_lock 5 rejection modes; _commit_lock gates on reason; test_impossible_lock_shows_reason PASS |
| RESIM-04 | SATISFIED | bracket_columns_html opacity:1/0.5 solid/faint; test_bracket_columns_html_is_columns_not_tree PASS |

**Phase 4 coverage: 4/4 SATISFIED.**

---

### Phase 5 - Odds Ensemble (ODDS-01..08)

| REQ-ID | Status | Evidence |
|--------|--------|----------|
| ODDS-01 | SATISFIED | odds/base.py protocol; all three adapters; 10 odds tests PASS |
| ODDS-02 | SATISFIED | devig_fixed_two_way vs normalize_market_price routed by vig_type; test_market_price_not_devigged PASS |
| ODDS-03 | SATISFIED | pool() originate x log1p(liquidity) weights; soft-book pre-pooled to ONE Pinnacle-anchored quote; test_softbook_bundle_one_opinion PASS |
| ODDS-04 | SATISFIED | engine/backsolve.py; invert_series bisection (Bo3 exact inverse); fit_ratings Gauss-Newton numpy-only; test_rating_roundtrip PASS; test_cache_present_feeds_sim confirms P(advance) moves |
| ODDS-05 | SATISFIED (deferred) | discover_fixtures returns {} gracefully (markets not posted); schema, fail-soft, cache paths wired and tested; live-slug confirm = one-function fill once bracket posts |
| ODDS-06 | SATISFIED | Fetch odds now button in app.py; scripts.fetch_odds LAZY-imported inside click branch only; test_app_makes_no_network_on_rerun PASS |
| ODDS-07 | SATISFIED | scripts/fetch_odds.main writes frozen schema; ui/odds_loader json+pathlib only; test_app_makes_no_network_on_rerun PASS (httpx booby-trapped) |
| ODDS-08 | SATISFIED | test_no_odds_failsoft, test_zero_config_first_run_still_works, test_cache_loader_failsoft (5 None-return cases) PASS |

**Phase 5 coverage: 8/8 SATISFIED.** ODDS-05 live-slug confirm explicitly deferred pending Cologne market posting - not a gap.

---

### Deferred / Out-of-Scope (not gaps)

| REQ-ID | Status |
|--------|--------|
| V3-01 | OUT OF v1.0 SCOPE - Phase 6, deferred to v3 milestone |
| V3-02 | OUT OF v1.0 SCOPE - Phase 6, deferred to v3 milestone |
| V3-03 | OUT OF v1.0 SCOPE - Phase 6, deferred to v3 milestone |
| V3-04 | OUT OF v1.0 SCOPE - Phase 6, deferred to v3 milestone |
| OPS-01 | DEFERRED to v2 - zero app change if ODDS-07 seam honored |
| OPS-02 | DEFERRED to v2 - only if pure-Python latency annoys in live use |

REQUIREMENTS.md header note: header reads '48 total requirements'; enumerated REQ-IDs total 58. All 58 are mapped. Informational discrepancy; not a missing requirement.

---

## 3. Cross-Phase Integration Wiring

### Wiring Summary

| | Count |
|-|-------|
| Cross-phase seams verified WIRED end-to-end | 8 |
| Orphaned exports (created but unused) | 0 |
| Missing expected connections | 0 |

### Seam-by-Seam Verification

**Seam 1: Phase 1 engine -> Phase 2 UI (MC generator, locked key)**

engine.montecarlo.run_mc_progressive imported in app.py:35; called in _drive_progress with no n_chunks override (preserves Phase 1x2 reproducibility invariant). freeze_locked in ui/cache.py:26-35 converts locked dict to a deterministic hashable tuple; used as real positional arg in run_mc_cached and as component 4 of the session mc_cache 5-tuple key. _cache_key_for(ratings, locked, fetched_at) returns the 5-tuple (freeze_ratings(ratings), S, int(N), freeze_locked(locked), fetched_at) - Phase 5 fetched_at extension is backward-compatible (defaults None). WIRED: test_locked_in_cache_key and test_progress_preserves_reproducibility PASS.

**Seam 2: Phase 1 engine -> Phase 3 optimizer (Result.sample retention)**

Result.sample populated in run_mc_progressive inner loop. optimize(_result, teams) in engine/optimizer.py:299 calls build_outcome_matrices(result.sample, ids) - reads only .sample, never re-sims. test_optimizer_consumes_sample_not_mc monkeypatches simulate_stage and run_mc to raise; optimize() still returns. WIRED.

**Seam 3: Phase 2 cache key -> Phase 3 optimizer memoization**

optimize_cached(_result, ratings_key, S, N, locked_key, fetched_at=None) shares the MC 5-tuple key. _render_ballot_panel(result, cache_key) calls optimize_cached(result, *cache_key) - the 5-tuple unpacks into the 5 positional params after _result. Signature alignment: index 0=ratings_key, 1=S, 2=N, 3=locked_key, 4=fetched_at. test_hero_is_pge5_not_placeholder PASS. WIRED.

**Seam 4: Phase 1 engine -> Phase 4 live-mode (locked dict, re-sim)**

engine/live.py:locked_dict_from_results projects list[(round_idx, winner_id, loser_id)] -> dict[frozenset: winner_id]. ui/state.py:locked_dict delegates to it (lazy import, streamlit-free). app.py:503 derives locked = locked_dict(st.session_state.get(KEY_LOCKED, [])) flowing through _compute_or_serve -> _drive_progress -> run_mc_progressive -> simulate_stage. simulate_stage._play checks if key in locked before sampling and records locked history in opps. test_live_lock_changes_cache_key and test_live_lock_moves_p_advance PASS. WIRED.

**Seam 5: Phase 4 live-mode -> Phase 3 optimizer (anchor ballot, pge5_delta)**

_live_anchor in app.py:508 computes Ballot B once from empty-locked pre_key result via optimize_cached(pre_lock_result, *pre_key).recommended, stores in KEY_LIVE_ANCHOR, never re-optimizes. pge5_delta(anchor, pre_lock_result, post_lock_result, ids) in engine/live.py:181-203 calls p_ge5 twice on the SAME anchor ballot. engine.live imports build_outcome_matrices, p_ge5 from engine.optimizer - Phase 3 reused directly (no duplication). test_live_delta_anchor_uses_pre_key PASS: before == p_ge5(anchor, matrices(pre_result.sample, ids)) to 1e-12. WIRED.

**Seam 6: Phase 5 odds -> Phase 1 engine (market_blend, epistemic outer loop)**

_odds_from_cache calls invert_series(p, bo3) per entry (series->map inversion before fitting, Pitfall 9), then fit_ratings from engine/backsolve.py. market_blend passed to _drive_progress -> run_mc_progressive(market_blend=market_blend). _epistemic_inputs builds blend/var/keys; epistemic_draws is the OUTER loop (var all-zero -> one no-op draw, byte-identical rating-only path). Each draw p-vector -> overrides -> simulate_stage(market_overrides=overrides) -> _play id-bucket lookup with p_a = p if a.id < b.id else (1-p) (T-05-ORIENT) -> series_prob(market_series_prob=p_a) (Bo3 NOT re-applied, PROB-02). test_cache_present_feeds_sim PASS; test_app_makes_no_network_on_rerun PASS. WIRED.

**Seam 7: Phase 5 odds -> Phase 3 optimizer (fetched_at cache invalidation)**

_cache_key_for returns 5-tuple with fetched_at at index 4. optimize_cached accepts fetched_at=None as 6th param; *cache_key unpacks the 5-tuple cleanly (index 4 = fetched_at). test_fresh_fetch_invalidates_cache PASS: new fetched_at -> new mc_cache key -> optimizer recomputes. WIRED.

**Seam 8: Phase 4 bracket -> Phase 1 engine (derive_bracket replay)**

engine/live.derive_bracket calls simulate_stage(_fresh_teams(teams), None, S, rng, dict(locked), pairings_out=pairings_out) - reuses shipped pairing path, never re-derives pairings by hand. legal_pairings_for_round raises LivePrefixIncomplete on incomplete prefix. test_legal_pairings_requires_full_prefix PASS: RNG-invariant pairings returned on full prefix; raises on incomplete. WIRED.

### Import Direction (Architecture Invariant)

grep import-ui in engine/ -> no matches (engine never imports ui). ui/ imports from engine/ at defined call sites only. odds/base.py imports no engine/app/ui code. ui/odds_loader.py imports no httpx/dotenv/engine (json+pathlib only).

---

## 4. E2E Flow Verification

**Flow 1: Zero-Config First Run (DX-01) - COMPLETE**
data/stage1.json ships 16 teams -> validate_ratings -> run_mc_progressive -> Result. test_zero_config_first_run PASS.

**Flow 2: Pre-Stage Optimizer Ballots and P(>=5) Hero - COMPLETE**
MC runs -> Result.sample retained -> optimize_cached -> ballot_a + ballot_b -> hero P(>=5). PASS.

**Flow 3: Live Mode Lock -> Re-Sim -> Delta + Status Chips + Bracket - COMPLETE**
User locks -> _commit_lock -> validate_lock -> add_lock -> re-sim -> _live_anchor -> pge5_delta -> _render_status_chips -> derive_bracket. All live AppTests PASS. Illegal lock shows st.error, KEY_LOCKED unchanged.

**Flow 4: Odds Fail-Soft No Key -> Rating-Only + Banner - COMPLETE**
No ODDSPAPI_KEY -> info banner -> _odds_from_cache returns (base_ratings, None, None) -> one no-op draw -> byte-identical rating-only path. test_no_odds_failsoft PASS.

**Flow 5: Odds Cache Present -> Feeds Sim + Epistemic Band - COMPLETE**
data/odds_cache.json -> _odds_from_cache -> invert_series -> fit_ratings -> run_mc_progressive(market_blend=...) -> K Beta OUTER draws -> overrides -> simulate_stage -> _union_band -> band > Wilson x 1.2. PASS.

---

## 5. Honesty/Correctness Invariant Spot-Checks

| Invariant | Result |
|-----------|--------|
| Buchholz = sum(opp.wins-opp.losses), NOT sum(opp.wins) | CORRECT - engine/probs.py:71; test_difficulty_formula PASS |
| Closed-form Bo3 p^2(3-2p) as single Bernoulli draw | CORRECT - engine/probs.py:39; single rng.random() < p_a draw in _play |
| Epistemic as OUTER loop (not folded into per-sim noise) | CORRECT - epistemic_draws wraps chunk loop; test_band_wider_than_wilson confirms outer spread |
| var < p(1-p) clamp before Beta fit | CORRECT - beta_moment_fit clamps strictly; called from epistemic_draws; never pre-clamped in pool() |
| Series override NOT re-Bo3 applied | CORRECT - series_prob(market_series_prob=p_a) passthrough path in _play |
| CI bands always visible, never hover-hidden | CORRECT - ci_bar_html in every cell; test_ci_bars_on_every_cell_after_run asserts >=48 bars |
| [INFERRED]-seed banner persists until confirmed | CORRECT - _render_header_strip persistent st.warning; data/stage1.json seeds_confirmed=false |
| Trust badge caveated pending Cologne seed confirmation | CORRECT - trust_badge_state requires BOTH BACKTEST_PASSED AND seeds_confirmed |
| BACKTEST_PASSED=True (GATE-01 green) | CORRECT - ui/state.py:78; test_trust_badge_wording asserts BACKTEST_PASSED is True |
| Market override oriented to lower-id | CORRECT - _play:250 p_a = p if a.id < b.id else (1.0 - p); test_override_no_bo3_reapply PASS |
| No network on rerun | CORRECT - test_app_makes_no_network_on_rerun (httpx booby-trapped) PASS |
| Engine purity: no ui/httpx imports in engine/ | CORRECT - grep returns no matches; httpx absent (ModuleNotFoundError) |

---

## 6. Outstanding-by-Design Items (not gaps)

1. Phase 5 live-slug /browse checkpoint (ODDS-05 dynamic discovery): discover_fixtures() returns {} by design - Cologne fixture slugs/tickers cannot be confirmed until markets post (not posted 2026-05-30). Schema contract, fail-soft empty-cache path, and all non-slug fetch infrastructure are fully wired and tested. One-function fill once the bracket posts.

2. [INFERRED] Cologne seeds pending official confirmation: data/stage1.json ships seeds_confirmed=false. Banner persists and trust badge stays caveated until user confirms. Engine is validated against Budapest 2025 (GATE-01 green).

3. STATE.md Blockers/Concerns stale entry: GATE-01/04 DEFERRED entry was accurate at Phase 1 but not cleaned up when the backtest passed. GATE-01 TODO moved to completed/; ui/state.py correctly updated. No code impact.

4. Phase 1 VERIFICATION report states BACKTEST_PASSED=False: written before the backtest resolved. Current ui/state.py correctly reads BACKTEST_PASSED=True. Historical snapshot; no code correction needed.

---

## 7. Findings Classification

### BLOCKERs

None. All cross-phase seams are wired end-to-end. 113 tests pass. All five E2E flows complete without breaks.

### WARNINGs

**WARNING-01 - Ballot A UI label over-claims**

File: C:/Users/sweek/Documents/Claude/Projects/cs2 major sim/ui/render.py, line 195.
Current label: A - Max E[correct].
Phase 3 V&V established via brute-force counterexample that this greedy is not the global E[correct] maximizer. engine/optimizer.py:ballot_a docstring was updated to greedy per-bucket marginal baseline; the UI render label and tests/test_app.py:249 were not updated. Both are mutually consistent (no integration break); user-facing label is technically inaccurate. Impact: cosmetic; does not affect any probability computation or cross-phase seam.

**WARNING-02 - STATE.md stale GATE-01 deferral entry**

File: C:/Users/sweek/Documents/Claude/Projects/cs2 major sim/.planning/STATE.md, lines 116-118.
Entry GATE-01/04 DEFERRED was not cleaned up when the backtest passed. No code impact; shipped code and tests are correct.

---

## 8. Requirements Integration Map

| Requirement | Integration Path | Status | Issue |
|-------------|-----------------|--------|-------|
| ENG-01..09 | Phase 1 engine/swiss.py + engine/probs.py + engine/teams.py; called by engine/montecarlo.py per-sim | WIRED | - |
| GATE-01..05 | Phase 1 engine -> tests/test_backtest_budapest_2025.py against tests/fixtures/budapest_2025_stage1.json | WIRED | GATE-01 GREEN; BACKTEST_PASSED=True in ui/state.py; badge caveated pending Cologne seed confirm |
| MC-01..06 | Phase 1 engine/montecarlo.py -> Phase 2 app.py:_drive_progress -> Phase 3 engine/optimizer.py -> Phase 4 engine/live.py | WIRED | - |
| PROB-01..05 | Phase 1 engine/probs.py (p_map, series, epistemic_draws stub) -> Phase 5 epistemic_draws fill + engine/backsolve.py invert | WIRED | - |
| UI-01..07 | Phase 2 app.py + ui/state.py + ui/render.py + ui/cache.py | WIRED | UI-07: BACKTEST_PASSED=True; badge caveated pending Cologne seed confirm |
| DX-01..05 | Phase 2 data/stage1.json, README.md, .env.example, pyproject.toml | WIRED | - |
| OPT-01..05 | Phase 3 engine/optimizer.py -> ui/cache.py:optimize_cached -> app.py:_render_ballot_panel | WIRED | OPT-01 UI label in ui/render.py:195 still reads Max E[correct] (WARNING-01) |
| RESIM-01..04 | Phase 4 engine/live.py + ui/state.py lock helpers + app.py LIVE mode wiring | WIRED | - |
| ODDS-01..08 | Phase 5 odds/ + engine/backsolve.py + scripts/fetch_odds.py + ui/odds_loader.py -> app.py:_odds_from_cache -> run_mc_progressive(market_blend=...) | WIRED | ODDS-05 live-slug discovery deferred pending Cologne market posting (expected, not a gap) |

Requirements with no cross-phase wiring: None. Every requirement has touchpoints in the cross-phase connection graph.

---

## 9. Audit Verdict

    status: passed

All 54 v1.0 REQ-IDs (ENG-01..09, GATE-01..05, MC-01..06, PROB-01..05, OPT-01..05, RESIM-01..04, ODDS-01..08, UI-01..07, DX-01..05) are satisfied by shipped, verified code. The full test suite passes (113 tests). GATE-01 is confirmed GREEN. All eight cross-phase seams are wired end-to-end. All five E2E user flows complete without breaks.

The two WARNINGs (Ballot A UI label cosmetic inaccuracy, STATE.md stale entry) do not represent broken seams or false probabilities.

V3-01..04 (Phase 6 full-major scope) are correctly classified as out-of-v1.0-scope and are not counted as gaps.

---

*Audit performed: 2026-05-30*
*Auditor: Claude (integration audit)*