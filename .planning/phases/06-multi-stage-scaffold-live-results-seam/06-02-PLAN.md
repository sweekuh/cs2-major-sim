---
phase: 06-multi-stage-scaffold-live-results-seam
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - app.py
  - ui/state.py
  - data/stage3.json
  - data/playoffs.json
  - tests/test_app.py
autonomous: true
requirements: [STG-05]
must_haves:
  truths:
    - "Each stage shows its OWN [INFERRED]-seed banner state: stage1 (seeds_confirmed:true) shows no banner; stage2/stage3 (seeds_confirmed:false) show the loud warning"
    - "The seeds-confirmed toggle is per-stage — confirming one stage does not dismiss another stage's banner"
    - "The per-stage seeds_confirmed flag is read from that stage's fixture read-only (fail-safe to False), never mutating the JSON or the engine"
  artifacts:
    - path: "app.py"
      provides: "per-stage banner keyed on f'seeds_confirmed_{stage_id}', seeded from read_seeds_confirmed(stage_fixture_path)"
      contains: "seeds_confirmed_"
    - path: "data/stage3.json"
      provides: "16-team Stage-3 fixture, seeds_confirmed:false, carrying an all_bo3 flag Phase 8 consumes (unused this phase)"
      contains: "\"seeds_confirmed\": false"
    - path: "data/playoffs.json"
      provides: "8-team playoff fixture (size:8), seeds_confirmed:false (not simulated this phase)"
      contains: "\"size\": 8"
  key_links:
    - from: "app.py per-stage banner"
      to: "ui.state.read_seeds_confirmed"
      via: "read_seeds_confirmed(_path_for_stage(stage_id)) — the path arg already exists"
      pattern: "read_seeds_confirmed\\("
---

<objective>
Slice 2 — per-stage `[INFERRED]`-seed banner (STG-05). Parameterize the EXISTING `read_seeds_confirmed`/`KEY_SEEDS_CONFIRMED` banner mechanism by the active stage so each stage carries its own confirm state: Stage 1 (already confirmed) shows no banner, Stage 2/3 (unconfirmed) show the loud warning with the reconcile expander and a per-stage confirm toggle. Also ship the remaining committed fixtures (`stage3.json`, `playoffs.json`) so all four stages are loadable.

This rides on Slice 1's selector + `stage_id`; it is a small additive change to one existing UI block.

Purpose: STG-05 (a per-stage `[INFERRED]`-seed banner that persists with editable seeds until each stage is positively confirmed).
Output: a per-stage banner + confirm toggle in `app.py` keyed on the active stage; a per-stage session key + the path-passing wiring; `data/stage3.json` (with an `all_bo3` stage flag for Phase 8) and `data/playoffs.json` (size 8); the per-stage-banner test.

After this slice: switching to Stage 2 shows its own "seeds INFERRED" warning; confirming Stage 2 does not change Stage 1's badge, and vice versa.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-multi-stage-scaffold-live-results-seam/06-PATTERNS.md
@CLAUDE.md

# This plan depends on the stage selector + stage_id + _path_for_stage from 06-01:
@.planning/phases/06-multi-stage-scaffold-live-results-seam/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: data/stage3.json + data/playoffs.json committed fixtures</name>
  <read_first>
    - data/stage1.json — the committed fixture schema to mirror (top-level `_comment`, `stage:{size,advance_at_wins,eliminate_at_losses}`, `seeds_confirmed`, `teams:[{seed,name,rating}]`).
    - .planning/phases/06-multi-stage-scaffold-live-results-seam/06-PATTERNS.md — the `data/stage2.json` section (388-409): all v3 Swiss fixtures are size 16; `playoffs.json` is size 8; ship `seeds_confirmed:false` with `[INFERRED]` placeholders.
    - .planning/REQUIREMENTS.md — the verified-format block (lines 11-15): Stage 3 is ALL Bo3; Playoffs are 8-team single-elim. (Stage 3's all-Bo3 behavior is wired in Phase 8 via the `all_bo3` flag — this fixture just carries the flag now.)
    - data/stage2.json — the minimal second fixture created in 06-01 (mirror its shape).
  </read_first>
  <action>
    Create two committed fixtures mirroring `data/stage1.json`'s schema:
    1. `data/stage3.json` — `stage:{size:16, advance_at_wins:3, eliminate_at_losses:3, all_bo3:true}` (the `all_bo3` flag is carried for Phase 8 to consume; it is NOT read this phase), `seeds_confirmed:false`, a 16-team `teams` array with seeds 1..16, distinct `[INFERRED]` placeholder names (e.g. `"Stage3 Seed N"`), in-range ratings. `_comment` notes real Stage-3 qualifier seeds derive from the Stage-2 final via the Phase-7 seeding chain.
    2. `data/playoffs.json` — `stage:{size:8}` (8-team bracket; no Swiss advance/eliminate fields needed, or include them as null/omitted), `seeds_confirmed:false`, an 8-team `teams` array with seeds 1..8, distinct placeholder names, in-range ratings. `_comment` notes the 8 teams seed from the final Stage-3 Buchholz in Phase 9; not simulated this phase.
    Both files must parse cleanly through `engine.teams.load_stage` (size-correct seed set 1..size). Do NOT add them to `.gitignore` — per-stage fixtures stay committed (only `results_cache.json` is gitignored).
  </action>
  <verify>
    <automated>cd "C:\Users\sweek\Documents\Claude\Projects\cs2 major sim" && uv run python -c "from engine.teams import load_stage; t3,c3=load_stage('data/stage3.json'); t8,c8=load_stage('data/playoffs.json'); assert len(t3)==16 and c3['size']==16 and c3['seeds_confirmed'] is False; assert len(t8)==8 and c8['size']==8 and c8['seeds_confirmed'] is False; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `data/stage3.json` loads via `load_stage` to 16 teams, `cfg["size"] == 16`, `cfg["seeds_confirmed"] is False`, and `cfg` carries `all_bo3` (truthy).
    - `data/playoffs.json` loads via `load_stage` to 8 teams, `cfg["size"] == 8`, `cfg["seeds_confirmed"] is False`.
    - Neither file appears in `.gitignore` (they are committed source like `stage1.json`).
    - `uv run pytest tests/test_teams_load_stage.py -x` exits 0 (the loader still passes against all fixtures).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: per-stage [INFERRED]-seed banner + per-stage confirm toggle (STG-05)</name>
  <read_first>
    - app.py — read lines 150-195: the `_render_header_strip` banner block. Line 158-159 seeds `KEY_SEEDS_CONFIRMED` from `read_seeds_confirmed()` (no path → stage1). Lines 174-189 are the `st.warning` + reconcile expander; 192-195 the `st.toggle` bound to `KEY_SEEDS_CONFIRMED`. Lines 161, 164 read `seeds_confirmed` for the badge.
    - ui/state.py — read `read_seeds_confirmed(path=None)` (126-139 — it ALREADY accepts a path arg, fail-safe to False), `KEY_SEEDS_CONFIRMED` (103), `_DATA_PATH` (152-154), `trust_badge_state` (110-123).
    - .planning/phases/06-multi-stage-scaffold-live-results-seam/06-PATTERNS.md — the "Per-stage [INFERRED]-seed banner — STG-05" section (451-476): make the session key `f"seeds_confirmed_{stage_id}"` and call `read_seeds_confirmed(stage_fixture_path)`; the warning/expander/toggle block is reused verbatim, keyed per stage.
    - 06-01-SUMMARY.md — the `KEY_STAGE` constant + `_path_for_stage` location from Slice 1.
  </read_first>
  <action>
    Make the banner per-stage in `app.py`'s `_render_header_strip` (the block at 150-195), using the active `stage_id` from `KEY_STAGE` (Slice 1):
    1. Compute the per-stage session key once: `seeds_key = f"seeds_confirmed_{stage_id}"`. On first load of a given stage, seed it from the stage's own fixture: `if seeds_key not in st.session_state: st.session_state[seeds_key] = read_seeds_confirmed(_path_for_stage(stage_id))`. (Import `_path_for_stage` from `ui.cache`, or move it to `ui.state`/a shared spot if a cross-import is cleaner — keep one definition, no second copy.)
    2. Replace the three uses of `KEY_SEEDS_CONFIRMED` in this block with `seeds_key`: the read for `seeds_confirmed` (161), the toggle's `key=` (194), and the badge gate input (164 via `trust_badge_state(seeds_confirmed)`). The `st.warning` + reconcile expander (174-189) and `st.toggle` (192-195) stay structurally identical — only the bound key changes per stage.
    3. The reconcile expander already iterates `teams` (the per-stage globals rebound in Slice 1), so it shows the active stage's seed→team rows automatically.
    Do NOT mutate any fixture JSON; `read_seeds_confirmed` stays read-only. Stage 1 (`seeds_confirmed:true`) must continue to show NO banner; Stage 2/3 (`false`) show it.
  </action>
  <verify>
    <automated>cd "C:\Users\sweek\Documents\Claude\Projects\cs2 major sim" && uv run pytest tests/test_app.py::test_per_stage_seed_banner -q && uv run pytest tests/test_backtest_budapest_2025.py -q</automated>
  </verify>
  <acceptance_criteria>
    - New `tests/test_app.py::test_per_stage_seed_banner` (AppTest): on Stage 1 the app shows NO "Seeds are INFERRED" warning (Stage 1 fixture `seeds_confirmed:true`); after selecting Stage 2 the app SHOWS the "Seeds are INFERRED" warning; the toggle key for Stage 2 is `seeds_confirmed_stage2` (distinct from Stage 1's), proving per-stage state. The test passes.
    - `app.py` reads the per-stage flag via `read_seeds_confirmed(_path_for_stage(stage_id))` (grep `read_seeds_confirmed(` in app.py shows the path-passing call).
    - The Stage-1-only behavior is preserved: an existing test asserting Stage-1's badge/banner state (e.g. around `tests/test_app.py:334-348`) still passes.
    - `uv run pytest tests/test_app.py -q` exits 0 and `uv run pytest tests/test_backtest_budapest_2025.py -x` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| per-stage fixture `seeds_confirmed` flag → UI banner state | the flag is read read-only from the stage's committed JSON; a missing/malformed flag must fail safe to False (banner shows), never raise. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | `data/stage3.json` / `data/playoffs.json` parsed by `load_stage` | mitigate | Both validate through `_validate_fixture_for_size` (size 16 / size 8) — a wrong seed count fails loud. `all_bo3` is an inert carried flag this phase (Phase 8 consumes it); no behavior depends on it now. |
| T-06-05 | Information disclosure / integrity | `read_seeds_confirmed(stage_path)` | accept | Read-only JSON flag, fail-safe to False (banner shows) on any error; never mutates the fixture or the engine. A false-negative (banner shows when seeds are fine) is the safe failure — it never hides an unconfirmed stage. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Phase 6 installs ZERO packages (RESEARCH § Package Legitimacy Audit: N/A). No `[ASSUMED]`/`[SUS]` packages. |
</threat_model>

<verification>
- After each task: `uv run pytest tests/test_backtest_budapest_2025.py <task test> -q`.
- End of plan: `uv run pytest -q` (full baseline + the per-stage-banner test), GATE-01 green.
- DEFERRED manual eyeball: `uv run streamlit run app.py`, switch to Stage 2, confirm the loud INFERRED-seed banner + reconcile expander render; confirm Stage 1 shows no banner.
</verification>

<success_criteria>
- Each stage shows its own `[INFERRED]`-seed banner state, seeded from that stage's fixture flag; the confirm toggle is per-stage (STG-05).
- `data/stage3.json` (size 16, `all_bo3` flag for Phase 8) and `data/playoffs.json` (size 8) load cleanly via `load_stage` and stay committed.
- Stage 1 shows no banner; Stage 2/3 show the warning; confirming one stage does not affect another.
- GATE-01 green; full suite green.
</success_criteria>

<output>
Create `.planning/phases/06-multi-stage-scaffold-live-results-seam/06-02-SUMMARY.md` when done.
</output>
