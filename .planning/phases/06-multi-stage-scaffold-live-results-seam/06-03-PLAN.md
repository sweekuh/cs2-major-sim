---
phase: 06-multi-stage-scaffold-live-results-seam
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - ui/results_loader.py
  - scripts/fetch_results.py
  - data/team_aliases.json
  - .gitignore
  - .env.example
  - tests/test_results_loader.py
  - tests/test_results_fetch.py
  - tests/fixtures/bo3gg_finished_sample.json
  - tests/fixtures/pandascore_finished_sample.json
autonomous: true
requirements: [RES-01, RES-04, RES-05]
must_haves:
  truths:
    - "load_results_cache returns None on missing / malformed / non-dict / wrong-version cache and a valid v1 cache round-trips — never raises into the UI"
    - "A recorded FINISHED-series fixture parses to (match,winner,round_idx) rows; an unparseable row is DROPPED, never raises; a non-FINISHED row is excluded"
    - "The provider team→engine-id join is slug-first via data/team_aliases.json; an unresolved team is DROPPED + surfaced, never mis-joined to a wrong team"
    - "httpx is lazy-imported inside the fetcher's fetch path ONLY; ui/results_loader.py imports only json+pathlib (no httpx/dotenv/provider/engine)"
  artifacts:
    - path: "ui/results_loader.py"
      provides: "JSON+pathlib read-only loader for data/results_cache.json (four-branch fail-soft None), mirrors ui/odds_loader.py"
      contains: "def load_results_cache"
    - path: "scripts/fetch_results.py"
      provides: "the ONLY results fetcher — out-of-app, lazy httpx, FINISHED-only, slug-first join, atomic versioned write; mirrors scripts/fetch_odds.py"
      contains: "def main"
    - path: "data/team_aliases.json"
      provides: "{provider_slug -> internal_name/engine_id} join map (placeholder slugs; event-time editable)"
      contains: "_comment"
    - path: "tests/fixtures/bo3gg_finished_sample.json"
      provides: "recorded FINISHED bo3.gg series fixture for the no-network parse test"
  key_links:
    - from: "scripts/fetch_results.py join"
      to: "odds/_match.resolve_id + data/team_aliases.json"
      via: "slug-first exact hit, then resolve_id substring fallback, then DROP on no match"
      pattern: "resolve_id"
    - from: "ui/results_loader.py"
      to: "data/results_cache.json"
      via: "json.loads + version gate, None on any error"
      pattern: "_SUPPORTED_VERSION"
---

<objective>
Slice 3 — the read-only results seam (RES-01/04/05), a STRUCTURAL TWIN of the shipped odds seam. Loader-first: `ui/results_loader.py` (JSON+pathlib, four-branch fail-soft `None`, copied from `ui/odds_loader.py`) reads a versioned read-only `data/results_cache.json`. Then `scripts/fetch_results.py` (out-of-app, lazy httpx, FINISHED-only, slug-first team join) writes that cache — mirroring `scripts/fetch_odds.py` byte-for-byte (lazy dotenv, httpx-logger→WARNING, per-provider fail-soft, atomic versioned write). Both tested against RECORDED fixtures with no network.

Loader before fetcher because the loader is pure + testable without network (exactly how the odds seam shipped). This slice does NOT wire results into the app — that is Slice 4 (06-04). httpx must never enter the app import path.

Purpose: RES-01 (fetch FINISHED series only into a read-only cache, httpx out of the app import path), RES-04 (fail-soft — missing key/network/source → manual entry + banner, never crash/gate; first run needs no source), RES-05 (slug-first team join via `team_aliases.json`; unresolved → drop + loud surface, never mis-locked).
Output: `ui/results_loader.py`; `scripts/fetch_results.py` (+ optional `results/` provider parsers); `data/team_aliases.json` (committed, placeholder slugs); `.gitignore` + `.env.example` updates; recorded provider fixtures + the loader/fetch tests.

After this slice: `scripts/fetch_results.py` can write a `results_cache.json` from a recorded FINISHED fixture, and the app's read-only loader returns it (or `None` fail-soft) without ever importing httpx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-multi-stage-scaffold-live-results-seam/06-RESEARCH.md
@.planning/phases/06-multi-stage-scaffold-live-results-seam/06-PATTERNS.md
@CLAUDE.md

# The seams this slice mirrors byte-for-byte:
@ui/odds_loader.py
@odds/_match.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ui/results_loader.py read-only fail-soft loader (RES-04)</name>
  <read_first>
    - ui/odds_loader.py — read the WHOLE file (53 lines). The four-branch fail-soft read (`load_odds_cache`, lines 35-52): None on OSError/ValueError, None on non-dict, None on `_meta.version != _SUPPORTED_VERSION`; `_CACHE_PATH` (28); `_SUPPORTED_VERSION = 1` (32); the module docstring's explicit "no httpx/dotenv/provider/engine import" contract.
    - .planning/phases/06-multi-stage-scaffold-live-results-seam/06-PATTERNS.md — the `ui/results_loader.py` section (53-96): copy the structure exactly, swap only path + version + docstring.
    - .planning/phases/06-multi-stage-scaffold-live-results-seam/06-RESEARCH.md — the "Frozen results-cache schema" (lines 347-359): `{"_meta":{fetched_at,version:1,source,stage}, "results":[{match:[lo,hi],winner,round_idx,bo,status,provider_slugs}]}`. Freeze the STACK.md `match:[lo,hi]`/`winner` shape.
    - tests/test_app.py — read `test_cache_loader_failsoft` (lines 750-785): the four fail-soft cases + the valid-v1 round-trip. Copy 1:1 against `load_results_cache`.
  </read_first>
  <behavior>
    - Test (test_results_cache_failsoft): `load_results_cache(tmp/"nope.json")` is None (missing); a file containing `"{not json"` → None (malformed); a file containing `"[1,2,3]"` → None (non-dict); a file with `{"_meta":{"version":2},"results":[]}` → None (wrong version); a valid `{"_meta":{"fetched_at":"...","version":1,"source":"bo3gg","stage":1},"results":[...]}` round-trips (returns the dict). No call raises.
  </behavior>
  <action>
    Create `ui/results_loader.py` by copying `ui/odds_loader.py` and swapping: `_CACHE_PATH = Path(__file__).resolve().parent.parent / "data" / "results_cache.json"`; keep `_SUPPORTED_VERSION = 1`; rename `load_odds_cache` → `load_results_cache(path: str | Path | None = None) -> dict | None`; keep the four fail-soft branches IDENTICAL (try `json.loads(src.read_text(encoding="utf-8"))` → `except (OSError, ValueError): return None`; `if not isinstance(raw, dict): return None`; `meta = raw.get("_meta")`; `if not isinstance(meta, dict) or meta.get("version") != _SUPPORTED_VERSION: return None`; `return raw`). Update the module docstring to describe the results cache + the frozen schema (RESEARCH lines 347-359) and keep the explicit "imports ONLY json + pathlib — no httpx/dotenv/provider/engine" contract. Write `tests/test_results_loader.py::test_results_cache_failsoft` mirroring `test_cache_loader_failsoft`, with the valid payload using the `results` schema. RED first, then GREEN.
  </action>
  <verify>
    <automated>cd "C:\Users\sweek\Documents\Claude\Projects\cs2 major sim" && uv run pytest tests/test_results_loader.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_results_loader.py::test_results_cache_failsoft -x` exits 0.
    - `ui/results_loader.py` imports ONLY `json` and `pathlib` (grep the file: `import httpx`, `from dotenv`, `import scripts`, `from scripts`, `from engine` each have count 0).
    - `load_results_cache` returns `None` (not a raise) for missing / malformed / non-dict / wrong-version inputs and the dict for a valid v1 cache.
    - `python -c "import ui.results_loader"` does not import httpx (no network dep pulled).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: recorded provider fixtures + scripts/fetch_results.py (FINISHED-only, slug-first join) (RES-01/05)</name>
  <read_first>
    - scripts/fetch_odds.py — read the WHOLE file (213 lines). Copy: `load_dotenv_safe()` (195-207, dotenv imported INSIDE the function, fail-soft); the httpx/httpcore logger → WARNING clamp (135-136); per-provider try/except fail-soft (70-78, 103-115 — a missing key skips its provider, a dead endpoint logs+continues, no market → empty valid cache); the ISO-UTC `_meta.fetched_at` + versioned cache + atomic write (174-192, `mkdir(parents=True, exist_ok=True)` + `write_text`); the `__main__` guard (210-212); `load_teams()` at 137 (the results fetcher uses `load_stage(path)` instead — it must work for ANY stage).
    - odds/_match.py — read the WHOLE file. `resolve_id(name, name_to_id)` (25-38, exact-norm hit then substring either direction then None), `build_name_to_id(teams)` (15-17), `_norm` (20-22). The slug-first join layers an exact `team_aliases` slug hit BEFORE this `resolve_id` fallback, then DROP on None.
    - .planning/phases/06-multi-stage-scaffold-live-results-seam/06-PATTERNS.md — the `scripts/fetch_results.py` section (100-173, the five load-bearing patterns) + the `data/team_aliases.json` section (209-246, slug-first then resolve_id then DROP+log).
    - .planning/phases/06-multi-stage-scaffold-live-results-seam/06-RESEARCH.md — the RES-01 provider API shapes (308-345, bo3.gg + PandaScore finished-series JSON shapes; `[VERIFY AT EVENT TIME]` for the live Cologne slug) + the frozen results-cache schema (347-359). Environment Availability (439-453): live APIs are event-time/optional; build + test against RECORDED fixtures (the exact pattern `odds/kalshi.py` `get_quotes` uses — parse a fixture, no httpx).
    - tests/test_odds.py — read lines 1-60: the recorded-fixture parse pattern (no network, httpx out of the import path).
    - tests/fixtures/kalshi_sample.json — the recorded-provider-fixture convention to mirror for the new bo3.gg/PandaScore fixtures.
  </read_first>
  <behavior>
    - Test (test_finished_fixture_parses): parsing `tests/fixtures/bo3gg_finished_sample.json` (a recorded bo3.gg FINISHED response) yields a list of `{match:[lo,hi], winner, round_idx, status:"finished", provider_slugs}` rows; an entry whose `status` is not `finished`/`settled` is EXCLUDED; a row with a missing/garbled field is DROPPED (never raises). No httpx is imported by the parse path.
    - Test (test_unresolved_team_fails_soft): a fixture row whose two teams resolve to engine ids via slug-first join produces a row with the SORTED `match` tuple and the `winner` engine id; a row with a team slug absent from `data/team_aliases.json` AND unresolvable via `resolve_id` is DROPPED (not present in the output) — never joined to a wrong engine id.
  </behavior>
  <action>
    Create the recorded fixtures + the fetcher, mirroring the odds seam:
    1. `tests/fixtures/bo3gg_finished_sample.json` and `tests/fixtures/pandascore_finished_sample.json` — small recorded FINISHED-series responses shaped per RESEARCH 308-345 (bo3.gg: an array of match objects each with `status:"finished"`, a winner/`winner_team_id`, two `teams` each with id/name/slug/acronym, a `tournament` include; PandaScore: `status:"finished"`, `winner_id`, `opponents[]→{opponent:{id,name,slug}}`, `results[]`). Use Stage-1 team slugs that map to `data/team_aliases.json` (Task creates that map below), plus at least one unresolvable team and one non-finished row to exercise drop-on-unresolved and the FINISHED-only filter.
    2. `data/team_aliases.json` — a committed `{provider_slug -> internal_name}` map (or `-> engine_id`) with placeholder slugs for the Stage-1 teams (e.g. `"gamerlegion":"GamerLegion"`, `"natus-vincere":"NaVi"`, etc.) and a leading `_comment` stating these are placeholders seeded at the event-time `/browse` verify and are editable. Do NOT add to `.gitignore` (committed).
    3. `scripts/fetch_results.py` — the ONLY results fetcher, mirroring `scripts/fetch_odds.py`:
       - module docstring describing the read-only seam + the FINISHED-only contract + "NO streamlit import" + the lazy-httpx discipline.
       - `load_dotenv_safe()` copied from the odds fetcher (dotenv inside the function, fail-soft).
       - the httpx/httpcore logger → WARNING clamp at the top of `main` (the optional `PANDASCORE_KEY` `?token=`/Authorization must never log).
       - a pure parse layer per provider (a `_parse_bo3gg(raw, *, teams, aliases)` / `_parse_pandascore(...)` that takes an already-loaded JSON dict and returns the row list) — these import NO httpx and are what the tests exercise via recorded fixtures. The FINISHED-only filter (`status in {"finished","settled"}`) and the slug-first join live here: for each row, resolve each team SLUG via an exact `aliases` hit first, else `odds/_match.resolve_id(name, build_name_to_id(teams))`, else DROP the whole row + `log.warning("couldn't map team %s — dropping row", slug)`. Emit the SORTED `match:[lo,hi]` tuple + the `winner` engine id + `round_idx` + `status` + `provider_slugs` per the frozen schema.
       - a `main(out_path="data/results_cache.json", *, stage_id="stage1", fixtures=None, tournament="cologne")` that: calls `load_stage(_path_for_stage(stage_id))` for the active stage's teams (NOT `load_teams()`); loads `data/team_aliases.json`; if `fixtures` is provided (recorded/test path) parses each provider's fixture with the pure parsers; else LAZY-imports httpx INSIDE a live-fetch helper and queries bo3.gg → PandaScore → Kalshi-derived per-provider fail-soft (a missing `PANDASCORE_KEY` logs `"no PANDASCORE_KEY — skipping PandaScore fallback"` and falls through; a dead endpoint logs+continues; the live Cologne tournament filter is by slug/substring, NEVER a hardcoded match id — mark that block `# [VERIFY AT EVENT TIME]`). Write the frozen cache atomically: `{"_meta":{"fetched_at":<iso-utc>,"version":1,"source":<provider>,"stage":<n>},"results":[...]}` via `mkdir(parents=True, exist_ok=True)` + `write_text(json.dumps(cache, indent=2))`. Return the cache dict. Add the `if __name__ == "__main__": logging.basicConfig(level=logging.INFO); main()` guard.
    4. `.env.example` — add a commented `PANDASCORE_KEY=` line (with a one-line note: optional FALLBACK A; bo3.gg primary is keyless; first run needs no key). Keep the existing `ODDSPAPI_KEY`/`POLYMARKET_API_KEY`/`KALSHI_API_KEY` lines (do NOT break `test_env_example_present_and_gitignored` at tests/test_app.py:456-473).
    5. `.gitignore` — add a `data/results_cache.json` block mirroring the existing `data/odds_cache.json` comment+entry (7-10). The fetched cache is a gitignored runtime artifact; its absence is the normal first-run state.
    Write `tests/test_results_fetch.py` with the two `<behavior>` tests (pure, fixture-driven, no network — assert `import httpx` is not triggered by the parse path). RED first, then GREEN.
  </action>
  <verify>
    <automated>cd "C:\Users\sweek\Documents\Claude\Projects\cs2 major sim" && uv run pytest tests/test_results_fetch.py tests/test_app.py::test_env_example_present_and_gitignored -q && uv run pytest tests/test_backtest_budapest_2025.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_results_fetch.py::test_finished_fixture_parses -x` exits 0 (parses recorded fixture to rows; non-finished excluded; bad row dropped, never raises).
    - `uv run pytest tests/test_results_fetch.py::test_unresolved_team_fails_soft -x` exits 0 (slug-first join resolves a known slug; an unresolved team's row is DROPPED, never mis-joined).
    - `scripts/fetch_results.py`'s parse layer imports no httpx at module top — httpx is referenced ONLY inside the live-fetch helper (grep: top-of-file has no `import httpx`; the live helper does the lazy import).
    - `main(fixtures={...}, stage_id="stage1")` writes a valid `data/results_cache.json` with `_meta.version == 1` and a `results` list (run against the recorded fixture in the test).
    - `.env.example` contains `PANDASCORE_KEY` AND still contains `ODDSPAPI_KEY`/`POLYMARKET`/`KALSHI`; `.gitignore` contains a `data/results_cache.json` line; `data/team_aliases.json` is NOT gitignored. `uv run pytest tests/test_app.py::test_env_example_present_and_gitignored -x` exits 0.
    - `uv run pytest tests/test_backtest_budapest_2025.py -x` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider JSON (bo3.gg / PandaScore / Kalshi) → fetcher parse | untrusted external JSON; schema-validate + drop unparseable rows; FINISHED-only filter. |
| provider team strings/slugs → engine ids | a name collision must never mis-attribute a result; slug-first join + drop-on-unresolved. |
| optional `PANDASCORE_KEY` (`.env`) → request URL / logs | the key must never be logged via httpx's request-URL INFO logging. |
| `data/results_cache.json` (fetched artifact) → app | read-only; the app (Slice 4) only reads it via `ui/results_loader.py`; httpx never enters the app import path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Information disclosure | `scripts/fetch_results.py` (optional `PANDASCORE_KEY`) | mitigate | `httpx`/`httpcore` loggers forced to WARNING at the top of `main` (mirror `scripts/fetch_odds.py:135-136`) so the `?token=`/Authorization is never printed; the key VALUE is never returned or logged (presence-only). `.env` gitignored; `.env.example` gains a commented `PANDASCORE_KEY=`. |
| T-06-07 | Tampering | provider JSON parse (`_parse_bo3gg`/`_parse_pandascore`) | mitigate | Schema-validate before emitting a row; drop unparseable rows (never raise); FINISHED-only filter (`status in {finished,settled}`) excludes live/in-progress rows. Test `test_finished_fixture_parses` proves drop-on-bad-row + non-finished exclusion. |
| T-06-08 | Tampering | provider team → engine-id join | mitigate | Slug-first exact `team_aliases.json` hit, then `odds/_match.resolve_id` substring fallback, then DROP + `log.warning` on no match — never a `name==name` guess. Test `test_unresolved_team_fails_soft`. (The fabricated-lock corruption this prevents feeds the Phase-7 seeding chain.) |
| T-06-09 | Denial of service / surprise calls | httpx in the app render path | mitigate | httpx lazy-imported INSIDE the live-fetch helper only; `ui/results_loader.py` imports only json+pathlib. This slice ships no app wiring; the app-import guard is extended in Slice 4 (`test_no_network_on_rerun_with_results`). Acceptance criterion asserts `import ui.results_loader` pulls no httpx. |
| T-06-12 | Tampering | `data/results_cache.json` as a read-only artifact | mitigate | Versioned (`_meta.version:1`) — a foreign/stale schema fails closed to `None` in the loader; written atomically (`mkdir(parents=True)` + `write_text`); gitignored like `odds_cache.json`. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Phase 6 installs ZERO packages — httpx is already pinned and shipped (RESEARCH § Package Legitimacy Audit: N/A). The only `.env.example` addition is a commented `PANDASCORE_KEY=` placeholder (a user account action, not a package install). No `[ASSUMED]`/`[SUS]` packages; no slopcheck pass required. |
</threat_model>

<verification>
- After each task: `uv run pytest tests/test_backtest_budapest_2025.py <task test> -q`.
- End of plan: `uv run pytest -q` (full baseline + the loader + fetch tests), GATE-01 green.
- `[VERIFY AT EVENT TIME]` (DEFERRED, not a build blocker): once Cologne markets post, gstack `/browse` bo3.gg + PandaScore to confirm the tournament slug/id and seed `data/team_aliases.json` with the real 16 Stage-1 slugs. The fetcher filters by tournament dynamically; the recorded-fixture tests prove the seam now.
</verification>

<success_criteria>
- `ui/results_loader.py` reads `data/results_cache.json` read-only with the four-branch fail-soft `None` and imports only json+pathlib (RES-04).
- `scripts/fetch_results.py` parses FINISHED-only series from recorded fixtures, joins teams slug-first via `team_aliases.json` (drop-on-unresolved), and writes a versioned read-only cache atomically; httpx is lazy-imported inside the fetch path only (RES-01/05).
- The optional `PANDASCORE_KEY` is never logged; `.env.example`/`.gitignore` updated; `data/team_aliases.json` committed.
- GATE-01 green; full suite green.
</success_criteria>

<output>
Create `.planning/phases/06-multi-stage-scaffold-live-results-seam/06-03-SUMMARY.md` when done.
</output>
