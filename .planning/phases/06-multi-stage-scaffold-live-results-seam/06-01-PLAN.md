---
phase: 06-multi-stage-scaffold-live-results-seam
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/teams.py
  - ui/cache.py
  - app.py
  - ui/state.py
  - data/stage2.json
  - tests/test_teams_load_stage.py
  - tests/test_ui_helpers.py
  - tests/test_app.py
autonomous: true
requirements: [STG-01, STG-04]
must_haves:
  truths:
    - "User can select Stage 1 / Stage 2 in the app; the selected stage loads its own teams from data/stageN.json"
    - "The same (ratings,S,N,locked) under two different stage_ids returns DIFFERENT cached Results; same stage_id is a cache HIT"
    - "Switching stages in the UI never shows the prior stage's per-team probs (no cross-stage stale-serve)"
    - "GATE-01 (Budapest backtest) stays green AND load_teams() returns the byte-identical 16-team Stage-1 list after the loader generalization"
  artifacts:
    - path: "engine/teams.py"
      provides: "NEW load_stage(path) + _validate_fixture_for_size(rows, size) siblings; load_teams/_DEFAULT_FIXTURE/_validate_fixture BYTE-FROZEN"
      contains: "def load_stage"
    - path: "ui/cache.py"
      provides: "stage_id a REAL leading positional in run_mc_cached AND optimize_cached; both load load_stage(_path_for_stage(stage_id))"
      contains: "_path_for_stage"
    - path: "data/stage2.json"
      provides: "minimal 16-team second fixture so the cross-stage isolation test has a real second stage"
      contains: "\"size\": 16"
    - path: "tests/test_teams_load_stage.py"
      provides: "STG-01 load_stage validate/return + the test_load_teams_unchanged freeze-regression companion"
      contains: "def test_load_teams_unchanged"
  key_links:
    - from: "ui/cache.py run_mc_cached"
      to: "engine.teams.load_stage"
      via: "load_stage(_path_for_stage(stage_id)) — NOT load_teams()"
      pattern: "load_stage\\(_path_for_stage"
    - from: "app.py _cache_key_for"
      to: "ui/cache.py optimize_cached"
      via: "tuple order (stage_id leading) matches optimize_cached params after _result, splatted at app.py:820"
      pattern: "stage_id"
---

<objective>
Slice 1 — THE PIVOT. Make the app multi-stage-aware end-to-end for the EXISTING Stage 1 (plus one minimal second fixture) without breaking the freeze: a stage selector in `app.py`, a NEW `engine.teams.load_stage(path)` sibling wrapper, and `stage_id` threaded as a REAL leading positional cache-key arg into BOTH `run_mc_cached` and `optimize_cached` so switching stages can never serve another stage's cached numbers.

This is the STG-04 correctness gate. Every later slice rides on the `stage_id`-keyed cache and the `load_stage` wrapper landing here first.

Purpose: STG-01 (per-stage fixture load via a wrapper that leaves the GATE-01 path frozen) + STG-04 (stage_id in both cache keys — the correctness gate against cross-stage stale-serve).
Output: `load_stage`/`_validate_fixture_for_size` siblings in `engine/teams.py`; `stage_id`-keyed `run_mc_cached`/`optimize_cached` + a `_path_for_stage` helper in `ui/cache.py`; a stage selector + `stage_id` threading in `app.py`; `data/stage2.json`; the STG-01/STG-04 tests + the freeze-regression companion + the `k[3]`→`k[4]` updates to the existing LIVE tests.

After this slice: a user can switch Stage 1 ↔ Stage 2 in the UI, each loads its own teams, and the per-team table for one stage never flashes the other stage's numbers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-multi-stage-scaffold-live-results-seam/06-RESEARCH.md
@.planning/phases/06-multi-stage-scaffold-live-results-seam/06-PATTERNS.md
@CLAUDE.md

# Frozen path (WRAP, never edit) + the cache-boundary correctness module + the existing breaking tests:
@engine/teams.py
@ui/cache.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: load_stage(path) + _validate_fixture_for_size siblings (STG-01) + freeze-regression test</name>
  <read_first>
    - engine/teams.py — the FROZEN GATE-01 path. Read `_validate_fixture` (lines 74-100), `_teams_from_rows` (102-107), `load_teams` (110-140 — its dup-seed `if seed in rows` guard at 137-139), `build_round1_pairs` (143-150), the `Team` dataclass (50-71), `STAGE_SIZE`/`DATA_PATH` (21-26). You will ADD siblings, NEVER edit these bodies.
    - .planning/phases/06-multi-stage-scaffold-live-results-seam/06-PATTERNS.md — the FROZEN-PATH GUARDRAIL (top) + the `load_stage`/`_validate_fixture_for_size` section (lines 250-318) with the exact sibling bodies to mirror.
    - tests/test_backtest_budapest_2025.py — read lines 26-27 and 56-60: GATE-01 builds its OWN Team objects and imports `simulate_stage`/`build_round1_pairs`/`Team` — it does NOT call `load_teams`. This is why a NEW sibling cannot perturb it.
    - data/stage1.json — the committed fixture schema (top-level `stage:{size,advance_at_wins,eliminate_at_losses}`, `seeds_confirmed`, `teams:[{seed,name,rating}]`) `load_stage` must read.
  </read_first>
  <behavior>
    - Test (test_load_stage_returns_teams_and_cfg): `load_stage("data/stage1.json")` returns `(teams, cfg)` where `teams` is a 16-element seed-ordered `list[Team]` identical in id/name/seed/rating to `load_teams()`, and `cfg["size"] == 16` and `cfg["seeds_confirmed"] is True`.
    - Test (test_load_stage_validates_against_declared_size): a fixture whose `stage.size` is 8 with 8 valid seeds 1..8 loads OK; a 16-`size` fixture with only 15 teams raises ValueError mentioning the size; a duplicate seed raises ValueError mentioning "duplicate seed".
    - Test (test_load_stage_bad_rating_raises): a fixture with a non-numeric or out-of-range rating raises ValueError (mirrors `_validate_fixture`'s loud fail).
    - Test (test_load_teams_unchanged — FREEZE REGRESSION): after importing the new siblings, `load_teams()` still returns the byte-identical 16-team Stage-1 list (assert each `(id,name,seed,rating)` tuple against the known `_DEFAULT_FIXTURE`/stage1.json values), proving the frozen path was not perturbed.
  </behavior>
  <action>
    Add TWO new sibling functions to `engine/teams.py` BELOW the frozen `_validate_fixture`/`load_teams` (do not touch lines 74-100, 110-140, or `_DEFAULT_FIXTURE` at 30-47):
    1. `_validate_fixture_for_size(rows: dict[int, tuple[str, float]], size: int) -> None` — copy the `_validate_fixture` body verbatim but replace every `STAGE_SIZE` literal with the `size` parameter (the seed-count check, the `set(range(1, size+1))` check, the duplicate-name check, and the numeric/range rating check all parameterized on `size`).
    2. `load_stage(path: str | Path) -> tuple[list[Team], dict]` per 06-PATTERNS.md lines 298-313: `json.loads(Path(path).read_text(encoding="utf-8"))`; read `stage = raw.get("stage") or {}` and `size = int(stage.get("size", 16))`; build `rows` from `raw["teams"]` reusing `load_teams`'s dup-seed guard (`if seed in rows: raise ValueError(f"duplicate seed {seed} in {path}")`); call `_validate_fixture_for_size(rows, size)`; build `teams = [Team(id=s, name=n, seed=s, rating=float(r)) for s, (n, r) in sorted(rows.items())]` (same ordering as `_teams_from_rows`); return `(teams, {"size": size, "seeds_confirmed": bool(raw.get("seeds_confirmed", False)), **stage})`.
    Do NOT import streamlit/httpx (functional-core invariant). Write the four tests above into `tests/test_teams_load_stage.py`. RED first (the tests fail because `load_stage` does not exist), then implement to GREEN.
  </action>
  <verify>
    <automated>cd "C:\Users\sweek\Documents\Claude\Projects\cs2 major sim" && uv run pytest tests/test_teams_load_stage.py tests/test_backtest_budapest_2025.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_teams_load_stage.py -x` exits 0 (all four tests pass, including `test_load_teams_unchanged`).
    - `uv run pytest tests/test_backtest_budapest_2025.py -x` exits 0 (GATE-01 stays green — 3 passed).
    - `git diff engine/teams.py` shows ZERO changes to lines inside `_validate_fixture` (74-100), `_DEFAULT_FIXTURE` (30-47), or `load_teams` (110-140) — only ADDED sibling functions.
    - `load_stage` imports no `streamlit`/`httpx`/`requests` (grep the new code: `import streamlit` count is 0 in engine/teams.py).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: data/stage2.json minimal second fixture + stage_id in BOTH cache keys (STG-04 gate)</name>
  <read_first>
    - ui/cache.py — read the WHOLE file (89 lines). The module docstring (1-13) documents the leading-underscore trap to AVOID. `run_mc_cached` (47-65) — line 64 `teams = load_teams()` is the v3 BUG (always Stage 1). `optimize_cached` (68-88) — line 88 `optimize(_result, load_teams())` is the same bug; `_result` correctly carries the underscore. `freeze_locked`/`freeze_ratings` (26-44).
    - .planning/phases/06-multi-stage-scaffold-live-results-seam/06-PATTERNS.md — the `ui/cache.py` section (322-385): the v3 TARGET signatures with `stage_id` LEADING and `_path_for_stage(stage_id)`; the `data/stage2.json` section (388-409).
    - data/stage1.json — copy this exact schema for stage2.json.
    - tests/test_ui_helpers.py — read `test_run_mc_cached_uses_frozen_seed_and_default_chunks` (lines 381-401): it calls `run_mc_cached(rk, 40.0, 2000, lk)` (4 args) and must gain the leading `stage_id`; READ the exact form of its `direct = run_mc(load_teams(), ...)` line and its `direct == cached` (or `assert_array_equal(direct..., cached...)`) equality assertion — you must preserve that assertion verbatim except for adding the leading `stage_id` to the `run_mc_cached(...)` call.
    - engine/teams.py — the `load_stage` signature you added in Task 1 (returns `(teams, cfg)`).
  </read_first>
  <action>
    Three changes:
    1. Create `data/stage2.json` mirroring `data/stage1.json`'s schema exactly: top-level `_comment` (note it is a minimal Phase-6 second fixture for cache-isolation testing; real Stage-2 qualifier seeds derive in Phase 7), `stage:{size:16, advance_at_wins:3, eliminate_at_losses:3}`, `seeds_confirmed: false`, and a 16-team `teams` array with seeds 1..16, placeholder `[INFERRED]` names (e.g. `"Stage2 Seed 1"` … or any 16 distinct names), and any in-range ratings. It MUST differ from stage1.json's team set so a stage switch is observably different.
    2. In `ui/cache.py`: add a module-level `_path_for_stage(stage_id: str) -> str` helper mapping `"stage1"→"data/stage1.json"`, `"stage2"→"data/stage2.json"`, `"stage3"→"data/stage3.json"`, `"playoffs"→"data/playoffs.json"` (resolve relative to the repo root like `engine.teams.DATA_PATH` does; raise ValueError on an unknown stage_id). Import `load_stage` from `engine.teams` (alongside or replacing the `load_teams` import). Change `run_mc_cached` to `run_mc_cached(stage_id: str, ratings_key: tuple, S: float, N: int, locked_key: tuple)` with `stage_id` as the FIRST positional, and replace line 64 `teams = load_teams()` with `teams, _cfg = load_stage(_path_for_stage(stage_id))`. Change `optimize_cached` to `optimize_cached(_result: Result, stage_id: str, ratings_key: tuple, S: float, N: int, locked_key: tuple, fetched_at=None)` (`stage_id` first AFTER `_result`), and replace line 88 with `teams, _cfg = load_stage(_path_for_stage(stage_id)); return optimize(_result, teams)`. Keep `_result` underscore-escaped (correct here). Update both docstrings to note `stage_id` is a REAL key arg scoping the whole key (never the underscore escape).
    3. Update `tests/test_ui_helpers.py::test_run_mc_cached_uses_frozen_seed_and_default_chunks` to pass `"stage1"` as the new leading arg: `run_mc_cached("stage1", rk, 40.0, 2000, lk)`. The `direct = run_mc(load_teams(), ...)` line and the `direct == cached` (Stage-1-equivalence) assertion MUST be PRESERVED — only the leading `stage_id` arg is added to the `run_mc_cached(...)` call. Do NOT weaken, comment out, loosen, or delete that equality assertion; it is the proof that `run_mc_cached("stage1", …) == run_mc(load_teams(), …)` (the Stage-1 freeze). The equality still holds because `load_stage("data/stage1.json")` returns the same teams as `load_teams()`.
  </action>
  <verify>
    <automated>cd "C:\Users\sweek\Documents\Claude\Projects\cs2 major sim" && uv run pytest tests/test_ui_helpers.py -q && uv run pytest tests/test_backtest_budapest_2025.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `data/stage2.json` exists, has `stage.size == 16`, 16 distinct seeds 1..16, `seeds_confirmed: false`, and a team set that differs from `data/stage1.json`.
    - `ui/cache.py` `run_mc_cached`'s first parameter is `stage_id` and its body calls `load_stage(_path_for_stage(stage_id))` (grep `load_teams()` in ui/cache.py: count is 0).
    - `ui/cache.py` `optimize_cached`'s first parameter after `_result` is `stage_id` and its body calls `load_stage(_path_for_stage(stage_id))`.
    - `uv run pytest tests/test_ui_helpers.py -x` exits 0 (the updated positional call passes).
    - FREEZE-PROOF PRESERVED: the `direct == cached` Stage-1-equivalence assertion in `test_run_mc_cached_uses_frozen_seed_and_default_chunks` SURVIVES the edit — it is NOT weakened, commented out, or removed. Concretely: `grep -nE "direct *== *cached|cached *== *direct|assert_array_equal\\(.*direct" tests/test_ui_helpers.py` returns ≥1 (the equality proof line referencing both `direct` and `cached` is intact, with only the leading `stage_id` added to the `run_mc_cached(...)` call inside the test). If the pre-edit assertion uses a different spelling, that exact line must remain present and still reference both `direct` and `cached`.
    - `uv run pytest tests/test_backtest_budapest_2025.py -x` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3a: MECHANICAL — k[3]→k[4] index updates + positional-call updates to the existing LIVE tests</name>
  <read_first>
    - .planning/phases/06-multi-stage-scaffold-live-results-seam/06-PATTERNS.md — the App Wiring section (413-449): the EXACT list of breaking tests with line numbers and the `k[3]`→`k[4]` rationale (leading `stage_id` shifts locked_key from index 3 to 4).
    - tests/test_app.py — read `test_locked_in_cache_key` (114-127, calls `run_mc_cached(rk, 40.0, 2000, lk_*)`), `test_live_lock_changes_cache_key` (521-541, asserts `k[3] != ()` at 541), `test_live_lock_moves_p_advance` (544-567, `k[3] == ()` at 552 and `k[3] != ()` at 561), `test_live_delta_anchor_uses_pre_key` (668-704, `k[3]` at 684 and 698).
  </read_first>
  <action>
    PURELY MECHANICAL test edits — no app/source code changes in this task (the behavior lands in Task 3b, which depends on this). This task ships ONLY the index/positional updates so they are independently verifiable and committed first:
    1. `tests/test_app.py::test_locked_in_cache_key` (114-127): add `"stage1"` as the new first arg to BOTH `run_mc_cached(...)` calls (e.g. `run_mc_cached("stage1", rk, 40.0, 2000, lk_empty)` and the `lk_locked` call).
    2. `tests/test_app.py::test_live_lock_changes_cache_key:541` — change `k[3] != ()` → `k[4] != ()`.
    3. `tests/test_app.py::test_live_lock_moves_p_advance:552` — change `k[3] == ()` → `k[4] == ()`; `:561` — change `k[3] != ()` → `k[4] != ()`.
    4. `tests/test_app.py::test_live_delta_anchor_uses_pre_key:684` — change `k[3] == ()` → `k[4] == ()`; `:698` — change `k[3] != ()` → `k[4] != ()`.
    Touch ONLY these test assertions/calls. Do NOT edit `app.py`, `ui/cache.py`, or `ui/state.py` here. After this task, these LIVE tests are written for the post-`stage_id` key shape; they GO GREEN only once Task 3b lands the actual `stage_id` threading. (This task therefore commits the test deltas first; run its verify after Task 3b is implemented, OR run it standalone expecting these specific tests to fail until 3b — see verify note.)
  </action>
  <verify>
    <automated>cd "C:\Users\sweek\Documents\Claude\Projects\cs2 major sim" && git diff --stat tests/test_app.py && grep -nE "k\[4\]" tests/test_app.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "k\\[3\\]" tests/test_app.py` no longer matches the four updated LIVE-test lines (541, 552, 561, 684, 698) — they now read `k[4]` (count of `k[4]` occurrences is ≥5).
    - `test_locked_in_cache_key` calls `run_mc_cached("stage1", ...)` for BOTH its empty-locked and locked-key calls (grep `run_mc_cached(rk` returns 0 in test_locked_in_cache_key's body — every call carries the leading `"stage1"`).
    - `git diff` shows changes ONLY to `tests/test_app.py` in this task (ZERO changes to `app.py`/`ui/cache.py`/`ui/state.py` — that is Task 3b).
    - These edits are mechanical index/positional shifts only; no assertion's intent is altered (a locked_key check is still a locked_key check, just at index 4).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3b: stage selector + _cache_key_for surgery + stage_id threading in app.py + STG-04 isolation tests</name>
  <read_first>
    - app.py — read lines 104-105 (`teams = load_teams()` / `by_seed = {t.seed: t for t in teams}` — the MODULE-LEVEL globals every render path reads; the selector must rebind these per-stage), 230-256 (the mode segmented_control + the `controls, main` columns where the selector belongs), 550-557 (`_cache_key_for`), 560-580 (`_compute_or_serve` — calls `_cache_key_for` at 572), 605-612 (the Run flow calling `_compute_or_serve` at 611), 615-641 (`_live_anchor` — the empty-locked `pre_key` compute at 634-636 and `optimize_cached(pre_lock_result, *pre_key)` at 639), 820 (`optimize_cached(result, *cache_key)` splat).
    - ui/state.py — read lines 45-50 (the `KEY_*` session constants + the `KEY_MC_CACHE` comment at 47 `dict: (ratings_key, S, N, locked_key) -> Result`). You will add `KEY_STAGE` and update the comment.
    - .planning/phases/06-multi-stage-scaffold-live-results-seam/06-PATTERNS.md — the App Wiring section (413-449): the `_cache_key_for` v3 target, the optimizer splat.
    - tests/test_app.py — the LIVE tests Task 3a already re-indexed to `k[4]` (this task makes them go green); read `test_app_makes_no_network_on_rerun` (952-971) for the no-network guard pattern.
  </read_first>
  <behavior>
    - Test (tests/test_ui_helpers.py::test_stage_id_in_cache_key — STG-04 unit): with the SAME `(ratings_key, S, N, locked_key)`, `run_mc_cached("stage1", rk, S, N, lk)` and `run_mc_cached("stage2", rk, S, N, lk)` return DIFFERENT Results (different team set → different `counts_advance`); calling `run_mc_cached("stage1", rk, S, N, lk)` twice returns the SAME cached object (cache HIT). This is the smallest proof of cross-stage isolation — assert it is NOT achieved via the underscore escape (stage_id is a real positional).
    - Test (tests/test_app.py::test_stage_switch_isolates_cache — AppTest): running the app, selecting Stage 2 via the selector, then Running, creates an `mc_cache` key whose leading element is `"stage2"`; the prior Stage-1 key (leading `"stage1"`) is still distinct — no key is shared across stages.
  </behavior>
  <action>
    Wire `stage_id` through `app.py` end-to-end (depends on Task 3a, which already shipped the breaking-test index updates):
    1. Add `KEY_STAGE = "active_stage"` to `ui/state.py` and update the `KEY_MC_CACHE` comment (line 47) to `dict: (stage_id, ratings_key, S, N, locked_key) -> Result`.
    2. In `app.py`, add a stage selector near the mode control (use `st.selectbox` or `st.radio` with options `["stage1", "stage2"]` — or display labels "Stage 1"/"Stage 2" mapped to those ids — writing `KEY_STAGE`; default `"stage1"`). Read the active `stage_id = st.session_state.get(KEY_STAGE, "stage1")`. Rebind the module globals so every downstream render uses the selected stage: replace the unconditional `teams = load_teams()` / `by_seed = ...` (lines 104-105) so that AFTER the selector runs, `teams, _stage_cfg = load_stage(_path_for_stage(stage_id))` and `by_seed = {t.seed: t for t in teams}` (import `load_stage` + `_path_for_stage`; keep a Stage-1 default for the initial top-of-module bind before the widget exists, then re-bind once the selector value is known). Stage 1 MUST remain the zero-config first-run default (`load_stage("data/stage1.json")` returns the same teams as today).
    3. Update `_cache_key_for(ratings, locked, stage_id, fetched_at=None)` to return `(stage_id, freeze_ratings(ratings), S, int(N), freeze_locked(locked), fetched_at)` (stage_id LEADING — same order as `optimize_cached`'s params after `_result`). Add a one-line docstring note that the element order MUST match `optimize_cached` because `app.py:820` splats it.
    4. Thread `stage_id` into every `_cache_key_for`/`_compute_or_serve` call: `_compute_or_serve` gains a `stage_id` param and passes it to `_cache_key_for` (line 572 region); update its call site in the Run flow (611) and the `_live_anchor` empty-locked `pre_key` compute (634-636) so the LIVE empty-locked anchor key also carries `stage_id` (so the pre_key still matches a real cache entry). `optimize_cached(result, *cache_key)` (820) and `optimize_cached(pre_lock_result, *pre_key)` (639) need no change — the splat picks up the new leading element automatically.
    5. Add the two new tests from `<behavior>` (`test_stage_id_in_cache_key` in tests/test_ui_helpers.py, `test_stage_switch_isolates_cache` in tests/test_app.py). RED first, then implement to GREEN. The Task-3a re-indexed LIVE tests (`k[4]`) MUST also be green once this lands.
  </action>
  <verify>
    <automated>cd "C:\Users\sweek\Documents\Claude\Projects\cs2 major sim" && uv run pytest tests/test_ui_helpers.py::test_stage_id_in_cache_key tests/test_app.py -q && uv run pytest tests/test_backtest_budapest_2025.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_ui_helpers.py::test_stage_id_in_cache_key -x` exits 0 (two stages → different Results; same stage → cache hit).
    - `uv run pytest tests/test_app.py::test_stage_switch_isolates_cache -x` exits 0.
    - `uv run pytest tests/test_app.py -q` exits 0 (ALL existing app tests pass, INCLUDING the Task-3a re-indexed `k[4]` LIVE tests and `test_locked_in_cache_key`).
    - `uv run pytest tests/test_app.py::test_app_makes_no_network_on_rerun -x` exits 0 (the selector added no network import).
    - `uv run pytest tests/test_backtest_budapest_2025.py -x` exits 0.
    - Manually switching Stage 1 ↔ Stage 2 in `uv run streamlit run app.py` shows a different team set and never the prior stage's numbers (deferred manual eyeball — see <verification>).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| per-stage JSON fixture → engine | `data/stageN.json` is committed source; `load_stage` parses it. A malformed fixture must fail loudly (V5), never silently corrupt pairings. |
| stage selector (UI state) → cache key | the selected `stage_id` scopes the entire `@st.cache_data` key; omitting it re-creates the stale-serve class. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | `engine.teams.load_stage` parsing `data/stageN.json` | mitigate | `_validate_fixture_for_size(rows, size)` enforces exactly `size` unique seeds 1..size, unique names, finite in-range ratings (mirrors `_validate_fixture`); dup-seed guard raises `duplicate seed {seed}`. A bad fixture fails loud, never silent (V5 input validation). |
| T-06-02 | Tampering / integrity | `ui.cache.run_mc_cached` / `optimize_cached` cache key | mitigate | `stage_id` is a REAL leading positional in BOTH keys (never the `_arg` underscore escape) and `run_mc_cached` loads `load_stage(_path_for_stage(stage_id))` not `load_teams()` — so two stages can never collide on a shared key and one stage can never serve another's cached Result. Gate test `test_stage_id_in_cache_key`. |
| T-06-03 | Tampering | the GATE-01 frozen path (`load_teams`/`_DEFAULT_FIXTURE`/`_validate_fixture`) | mitigate | New siblings only; `test_load_teams_unchanged` asserts the byte-identical Stage-1 list + GATE-01 stays green; `test_run_mc_cached_uses_frozen_seed_and_default_chunks`'s `direct == cached` Stage-1-equivalence assertion is preserved (proves `run_mc_cached("stage1", …) == run_mc(load_teams(), …)`). Any diff to lines 74-100 / 30-47 / 110-140 of `engine/teams.py` is a freeze violation (flag in review). |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Phase 6 installs ZERO packages (RESEARCH § Package Legitimacy Audit: N/A — httpx/numpy/dotenv/streamlit/stdlib already shipped). No `[ASSUMED]`/`[SUS]` packages; no slopcheck pass required. |
</threat_model>

<verification>
- After each task: `uv run pytest tests/test_backtest_budapest_2025.py <the task's test file> -q` (GATE-01 + the slice under test).
- End of plan (full): `uv run pytest -q` — the 128-test baseline + the new STG-01/STG-04 tests, ALL green, GATE-01 green.
- DEFERRED manual eyeball (not a build blocker): `uv run streamlit run app.py`, switch Stage 1 ↔ Stage 2, confirm the team table changes and never flashes the prior stage's probs.
</verification>

<success_criteria>
- `load_stage(path)` validates against the JSON's `stage.size` and returns `(teams, cfg)`; the frozen `load_teams`/`_DEFAULT_FIXTURE`/`_validate_fixture` path is byte-identical (STG-01).
- `stage_id` is a real leading positional in BOTH `run_mc_cached` and `optimize_cached`; same `(ratings,S,N,locked)` under different stages → different cached Results; same stage → cache hit (STG-04 — correctness gate).
- The app exposes a Stage 1 / Stage 2 selector that loads each stage's teams and never cross-stage stale-serves.
- GATE-01 green AND `load_teams()` returns the byte-identical 16-team Stage-1 list (freeze regression); the `direct == cached` Stage-1-equivalence assertion is preserved.
- Full suite green.
</success_criteria>

<output>
Create `.planning/phases/06-multi-stage-scaffold-live-results-seam/06-01-SUMMARY.md` when done.
</output>
