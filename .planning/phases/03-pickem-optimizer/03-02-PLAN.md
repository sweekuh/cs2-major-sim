---
phase: 03-pickem-optimizer
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - ui/cache.py
  - ui/render.py
  - app.py
  - tests/test_app.py
  - tests/test_ui_helpers.py
autonomous: true
requirements: [OPT-03, OPT-04, OPT-05]
tags: [streamlit, ui, ballot, hero, optimizer, cache]
user_setup: []

must_haves:
  truths:
    - "After a Run, Ballot A (Max E[correct]) and Ballot B (Max P(>=5)) render side by side, each listing its 3-0 / advance / 0-3 picks by team name, valid 2/6/2 (OPT-03)"
    - "Picks that differ between Ballot A and Ballot B are visually highlighted so the difference is the insight (OPT-03 / UI-SPEC)"
    - "The pre-stage hero number is the recommended ballot's (Ballot B) P(>=5), filling the Phase-2 _hero_slot placeholder — no longer the top-team P(advance) stand-in (OPT-04)"
    - "A correlated-pick warning banner appears when Ballot A's two 0-3 picks meet in Round 1, noting that Ballot B avoids the trap; it is colorblind-safe (amber + glyph + text, never red/green) (OPT-05 / UI-06)"
    - "The optimizer runs on the cached Result and is memoized on the SAME (ratings_key,S,N,locked_key) key — it does not recompute on every Streamlit rerun and never re-runs the MC (RESEARCH D6)"
    - "The ballot panel renders all its interaction states: empty-pre-run ('Run to see the recommended ballot') and success (A vs B + diff); loading is the existing Run progress bar (UI-05 / UI-SPEC)"
  artifacts:
    - path: "ui/cache.py"
      provides: "optimize_cached — memoized optimizer over a Result keyed on the MC cache tuple (RESEARCH D6)"
      exports: ["optimize_cached"]
    - path: "ui/render.py"
      provides: "ballot_panel_html / ballot rendering helper + correlated_pick_warning rendering; differing-pick highlight; reuses hero_number_html for the P(>=5) hero"
      exports: ["ballot_columns"]
    - path: "app.py"
      provides: "_hero_slot filled with Ballot B P(>=5); ballot A-vs-B panel + diff in the pre-stage main column; correlated-pick warning"
    - path: "tests/test_app.py"
      provides: "AppTest: ballot panel renders A and B after Run; hero shows a P(>=5) percentage; warning path"
    - path: "tests/test_ui_helpers.py"
      provides: "pure tests for the ballot rendering helper + diff highlight (numeric/text-only, no XSS surface)"
  key_links:
    - from: "app.py"
      to: "ui.cache.optimize_cached"
      via: "memoized optimizer over the cached Result on the MC cache key"
      pattern: "optimize_cached"
    - from: "ui.cache.optimize_cached"
      to: "engine.optimizer.optimize"
      via: "pure call on (Result, teams); no MC re-run"
      pattern: "optimize\\("
    - from: "app.py _hero_slot"
      to: "engine.optimizer.optimize"
      via: "hero = recommended ballot (B) P(>=5)"
      pattern: "hero_number_html"
---

<objective>
Wire the Plan-01 optimizer into the Phase-2 shell: fill the reserved `_hero_slot` with the
recommended ballot's P(≥5) (OPT-04), render Ballot A vs Ballot B side by side with the
differing picks highlighted (OPT-03), and surface the correlated-0-3-in-R1 warning (OPT-05),
all memoized on the existing MC cache key so it computes once per run and never re-runs the
MC. This turns the pre-stage placeholder ("Placeholder hero — Phase 3/4 fills the
optimal-ballot P(>=5)") into the real decision layer.

Purpose: a user can now do something new — get the two recommended ballots, the true coin
odds (P(≥5)), and the correlated-pick warning — from a browser, against the proven engine.
Live-mode "P(≥5)-from-here" delta stays Phase 4; this plan owns the pre-stage ballot.

Output: `ui/cache.py` (+`optimize_cached`), `ui/render.py` (ballot rendering), `app.py`
(hero + ballot panel + warning), and AppTest/helper test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/03-pickem-optimizer/03-RESEARCH.md
@.planning/phases/03-pickem-optimizer/03-01-PLAN.md
@.planning/phases/02-streamlit-ui-shell/02-UI-SPEC.md
@engine/optimizer.py
@engine/montecarlo.py
@engine/teams.py
@app.py
@ui/cache.py
@ui/render.py
@ui/state.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: optimize_cached memo + ballot/warning render helpers + RED tests</name>
  <read_first>
    @ui/cache.py
    @ui/render.py
    @engine/optimizer.py
    @tests/test_ui_helpers.py
    @.planning/phases/03-pickem-optimizer/03-RESEARCH.md
  </read_first>
  <files>ui/cache.py, ui/render.py, tests/test_ui_helpers.py</files>
  <behavior>
    - optimize_cached(ratings_key, S, N, locked_key) reconstructs inputs, fetches/derives the Result the SAME way run_mc_cached does, and returns engine.optimizer.optimize(result, teams); decorated @st.cache_data so repeat calls on the same key are instant and the MC is never re-run (RESEARCH D6).
    - ballot_columns(ballot_a, ballot_b, diff, teams) returns the side-by-side render payload/HTML: each ballot's 3-0 / advance / 0-3 picks by team NAME, with differing picks marked; built from team names + numbers only (no user free-text → no XSS, mirroring ci_bar_html discipline).
    - the correlated-pick warning helper produces amber + glyph + text (never red/green) per UI-06.
    - hero stays hero_number_html (reused) — the value is Ballot B P(>=5).
  </behavior>
  <action>
    Add `optimize_cached` to `ui/cache.py` next to `run_mc_cached`, keyed on the same
    `(ratings_key, S, N, locked_key)` tuple (RESEARCH D6); inside, reconstruct ratings/locked
    and obtain the Result via the existing frozen path, then return `engine.optimizer.optimize`.
    Keep `engine.optimizer` import lazy/local if needed so `ui/render` stays streamlit-light.
    Add to `ui/render.py` a `ballot_columns(...)` helper (and a small warning-banner helper or
    reuse status styling) following the existing token rules: monospace numbers via `fmt_pct`,
    colorblind-safe amber for the warning with an ASCII glyph + label, differing picks given a
    distinct marker. Names come only from `load_teams()`, never user free-text (T-02-XSS).
    Add RED pure tests to `tests/test_ui_helpers.py`: ballot_columns lists 2/6/2 names per
    ballot and marks the diff; the warning helper output contains the glyph + text and no
    red/green. (optimize_cached itself is exercised via AppTest in Task 2.)
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_ui_helpers.py 2>&1 | tail -5</automated>
  </verify>
  <done>optimize_cached exists and is @st.cache_data-keyed on the MC tuple; ballot_columns + the warning helper exist and pass new pure tests (diff marked, colorblind-safe, names-only); no engine mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: app.py — fill hero with Ballot B P(>=5), render A-vs-B panel + diff + warning (GREEN)</name>
  <read_first>
    @app.py
    @ui/cache.py
    @ui/render.py
    @engine/optimizer.py
    @.planning/phases/02-streamlit-ui-shell/02-UI-SPEC.md
    @tests/test_app.py
  </read_first>
  <files>app.py, tests/test_app.py</files>
  <behavior>
    - test_ballot_panel_renders: after a zero-config Run, the pre-stage main column shows both "Max E[correct]" (Ballot A) and "Max P(>=5)" (Ballot B) with team names; no exception headless.
    - test_hero_is_pge5: the hero slot shows a P(>=5) percentage (not the old top-team P(advance) stand-in); the caption no longer says "Placeholder".
    - test_ballot_empty_state: before Run, the ballot section shows "Run to see the recommended ballot." (existing string) and no ballot rows.
    - test_correlated_warning_render (optional/where feasible under AppTest): with ratings forcing the two top-P(0-3) teams to be an R1 pair, the warning banner text appears; otherwise covered by the engine test + a render-helper unit test.
    - The optimizer is read from optimize_cached / session_state on the SAME key as the Result — not recomputed each rerun, MC never re-run.
  </behavior>
  <action>
    In `app.py`, replace the `_hero_slot` placeholder body (currently the top-team
    P(advance) stand-in + "Placeholder hero — Phase 3/4 fills..." caption) so the pre-stage
    hero renders `hero_number_html(optimizer.recommended_pge5)` with a real label
    ("P(≥5) — recommended ballot"). After computing/fetching the Result on a Run, call
    `optimize_cached(...)` (or read it from session_state keyed by the same tuple, Phase-2
    pattern B) and render the A-vs-B ballot panel via `ui.render.ballot_columns` in the
    pre-stage main column where the "Recommended ballot" subheader already sits; show the
    differing-pick highlight and, when `optimizer.warning` is set, the correlated-pick
    banner. Keep the empty-pre-run string ("Run to see the recommended ballot.") and the
    loading state (the existing Run progress bar) intact (UI-05). LEAVE the Live-mode hero
    as the Phase-4 "P(>=5) from here" placeholder — this plan fills the PRE-STAGE hero only.
    Flip the test_app stubs to GREEN; keep N small (N≈2000) in AppTest per the Phase-2
    latency budget.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_app.py 2>&1 | tail -5</automated>
  </verify>
  <done>tests/test_app.py passes: ballot A & B render side by side after Run, hero shows the recommended P(>=5) (placeholder gone), empty state preserved; full suite `uv run pytest -q` green; `uv run streamlit run app.py` shows the dual ballots + P(>=5) hero + (when applicable) the correlated-pick warning.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user (ratings/S/N) → optimize_cached | Same untrusted inputs as run_mc_cached; already validated by validate_ratings before Run |
| optimize_cached output → st.markdown(unsafe_allow_html=True) | App-generated ballot HTML built from team names + numeric P(>=5) only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-XSS | Tampering | ballot_columns via unsafe_allow_html | mitigate | Built from load_teams() names + numeric fmt_pct only; no user free-text interpolated (mirrors ci_bar_html) |
| T-03-RERUN | DoS | optimizer recompute each rerun | mitigate | @st.cache_data on the MC cache tuple — single compute per unique run (RESEARCH D6) |
| T-03-MC | Tampering | optimizer re-running MC in the UI | mitigate | optimize_cached calls engine.optimizer.optimize on the Result only; covered by the Plan-01 sample-only test |
</threat_model>

<verification>
- `uv run pytest -q tests/test_ui_helpers.py` — ballot_columns diff + colorblind-safe warning helper green.
- `uv run pytest -q tests/test_app.py` — ballot A/B render, P(≥5) hero, empty state green.
- `uv run pytest -q` — full suite green (no engine/Phase-2 regression).
- Manual (VALIDATION Manual-Only): `uv run streamlit run app.py` → Run → pre-stage shows dual ballots side by side, differing picks highlighted, P(≥5) hero, correlated-pick banner when the trap is present.
</verification>

<success_criteria>
- Ballot A (Max E[correct]) and Ballot B (Max P(≥5)) shown side by side with differing picks highlighted (OPT-03).
- Pre-stage hero = recommended ballot (B) P(≥5), replacing the Phase-2 placeholder (OPT-04).
- Correlated-pick warning fires (amber + glyph + text) when Ballot A's two 0-3 picks meet in R1; notes Ballot B avoids it (OPT-05 / UI-06).
- Optimizer memoized on the MC cache key; never recomputes per rerun, never re-runs the MC (RESEARCH D6).
- Ballot-panel empty/loading/success states all render (UI-05).
</success_criteria>

<output>
Create `.planning/phases/03-pickem-optimizer/03-02-SUMMARY.md` when done.
</output>
