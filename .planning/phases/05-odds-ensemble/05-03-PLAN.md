---
phase: 05-odds-ensemble
plan: 03
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - pyproject.toml
  - .gitignore
  - scripts/__init__.py
  - scripts/fetch_odds.py
  - ui/odds_loader.py
  - app.py
  - tests/test_app.py
autonomous: false            # carries a DEFERRED checkpoint:human-verify (live slug confirmation, gated on markets posting — not a build blocker)
requirements: [ODDS-05, ODDS-06, ODDS-07, ODDS-08]
tags: [fetch-script, cache-seam, streamlit, fail-soft, fetch-button, apptest]
user_setup:
  - service: oddspapi
    why: "OddsPapi (Pinnacle-anchored) is the only keyed provider; Polymarket + Kalshi are keyless public reads, so the app fetches a 2-source pool with NO key. The key only unlocks the Pinnacle anchor."
    env_vars:
      - name: ODDSPAPI_KEY
        source: "OddsPapi dashboard -> API key (free tier ~250 req/month). Leave blank to run keyless (Polymarket+Kalshi only) or rating-only."
    dashboard_config:
      - task: "Confirm IEM Cologne 2026 CS2 fixture coverage / event slugs / series tickers once markets post (DEFERRED — markets not posted 2026-05-29). Verify via gstack /browse, then adjust the adapter slug/ticker discovery."
        location: "oddspapi.io / gamma-api.polymarket.com / kalshi.com"

must_haves:
  truths:
    - "scripts/fetch_odds.py is the ONE entry both the UI 'fetch now' button AND a future v2 cron call (python -m scripts.fetch_odds); it loads .env (python-dotenv), discovers Cologne fixtures, calls the 05-01 providers, pools per match, and WRITES read-only data/odds_cache.json — the app NEVER imports it (ODDS-06/07, Pitfall 11)"
    - "data/odds_cache.json matches the FROZEN schema {_meta:{fetched_at,version,providers_present,round_hint}, blended:{\"lo-hi\":{p,var,n_sources,bo3}}} with sorted lo-hi engine-id string keys and p = P(lower-id wins the series); var is stored RAW (clamped later by beta_moment_fit) (ODDS-07/D7)"
    - "ui/odds_loader.load_odds_cache(path) is JSON-ONLY (no httpx/dotenv/provider import) and returns None on ANY error (missing file / bad JSON / wrong version) -> the app runs rating-only + the EXISTING 'live odds off' banner; it NEVER raises into the UI (ODDS-08, mirrors read_seeds_confirmed)"
    - "a present valid data/odds_cache.json feeds the sim: the loader's blended p/var per match flow into the back-solved ratings (engine.backsolve.fit_ratings) + the epistemic blend/var (engine.probs.epistemic_draws) via the EXISTING run_mc cache path, so the odds change the reported probabilities (ODDS-04/05 wiring)"
    - "the run cache key includes the cache's `_meta.fetched_at` (or a blend digest) so a FRESH fetch that moves only `var` (not the back-solved ratings) still invalidates the memoized Result — no stale epistemic band is served (T-05-STALEBAND/ODDS-07); if a clean 1-line key extension is not possible, the 05-03 SUMMARY MUST explicitly document the stale-band limitation so Phase 6 does not inherit it silently"
    - "the app makes NO network call on a rerun — monkeypatching httpx to raise still renders the app; only the explicit 'fetch now' button (a user action, OUT of the render path) triggers scripts.fetch_odds (Pitfall 11/ODDS-07)"
    - "the zero-config first run is PRESERVED: no ODDSPAPI_KEY + no data/odds_cache.json -> rating-only, the existing banner, no crash; odds_key_present() still checks os.environ ONLY (no httpx/dotenv pulled into the app) (DX-01/ODDS-08)"
    - "data/odds_cache.json is gitignored (a fetched artifact, not source); its ABSENCE is the normal first-run state and is handled as fail-soft None (ODDS-07/08)"
    - "the live Cologne slug/ticker confirmation is a DEFERRED user gstack /browse checkpoint (markets-not-posted is expected) — the plan does NOT hard-depend on a live fixture; a provider returning [] yields a valid empty-blended cache, not an error (ODDS-05, A7)"
  artifacts:
    - path: "scripts/fetch_odds.py"
      provides: "main(out_path) — load_dotenv, discover_fixtures (alias match reusing engine.teams.load_teams/build_round1_pairs), per-provider try/except fail-soft, pool() per match, write the frozen cache schema; runnable as python -m scripts.fetch_odds and importable by the button handler"
      exports: ["main", "discover_fixtures"]
      min_lines: 60
    - path: "ui/odds_loader.py"
      provides: "load_odds_cache(path) -> dict | None (json-only, fail-soft on any error, version-gated); NO httpx/dotenv/provider import"
      exports: ["load_odds_cache"]
    - path: "app.py"
      provides: "a 'fetch now' button (+ per-round trigger) calling scripts.fetch_odds OUT of the rerun path; load_odds_cache wired into the run path -> back-solved ratings + epistemic blend/var when a valid cache loads; the run cache key extended with _meta.fetched_at so a fresh fetch invalidates the cached Result; the EXISTING no-key 'live odds off' banner + zero-config first run PRESERVED"
    - path: "tests/test_app.py"
      provides: "AppTest additions: test_no_odds_failsoft (no key + no cache -> rating-only + banner, no crash), test_zero_config_first_run_still_works, test_cache_present_feeds_sim, test_app_makes_no_network_on_rerun (monkeypatch httpx to raise), test_cache_loader_failsoft, test_fresh_fetch_invalidates_cache (a fetched_at change re-runs the sim, not a stale serve)"
    - path: "pyproject.toml"
      provides: "httpx + python-dotenv added to deps (versions confirmed via pip index versions); odds/ + scripts/ in the packages list so python -m scripts.fetch_odds resolves"
    - path: ".gitignore"
      provides: "data/odds_cache.json ignored (fetched artifact)"
  key_links:
    - from: "app.py 'fetch now' button handler"
      to: "scripts.fetch_odds.main"
      via: "explicit user action ONLY (never on rerun) -> writes data/odds_cache.json; the app reads it back via load_odds_cache (Pitfall 11)"
      pattern: "fetch_odds|load_odds_cache"
    - from: "ui.odds_loader.load_odds_cache"
      to: "the run path (back-solve + epistemic)"
      via: "valid cache -> engine.backsolve.fit_ratings (ratings) + blend/var into run_mc; the run cache key folds in _meta.fetched_at so a var-only refetch is not stale-served; None -> rating-only + existing banner"
      pattern: "load_odds_cache|fit_ratings|fetched_at"
    - from: "app.py odds-off branch"
      to: "ui.state.odds_key_present + the existing 'live odds off' banner"
      via: "preserve verbatim — os.environ-only key check, no httpx/dotenv in the app import path (DX-01)"
      pattern: "odds_key_present|live odds off"
    - from: "scripts.fetch_odds.discover_fixtures"
      to: "engine.teams.load_teams / engine.swiss.build_round1_pairs"
      via: "reuse the proven team/seed set for alias matching; never re-derive seeds (the GATE-01 blocker)"
      pattern: "load_teams|build_round1_pairs"
---

<objective>
Wire the 05-01 odds core and the 05-02 back-solve/epistemic into the app via the read-only cache
seam — the v2-cron drop-in contract (D7/ODDS-07). Five jobs, all preserving the Phase-2 fail-soft
+ zero-config promises:

1. **`scripts/fetch_odds.py`** — the ONE entry both the UI "fetch now" button AND a future v2 cron
   call (`python -m scripts.fetch_odds`). It `load_dotenv()`s (keys live HERE, never in the app),
   `discover_fixtures(teams)` by alias-matching team names (reusing `engine.teams.load_teams` /
   `build_round1_pairs` — never re-deriving seeds), calls the 05-01 providers with per-provider
   `try/except` fail-soft, `pool()`s per match, and writes the FROZEN `data/odds_cache.json` schema.
2. **`ui/odds_loader.py`** — a JSON-only read path (NO httpx/dotenv/provider import) returning
   `None` on ANY error (missing file / bad JSON / wrong version), mirroring `read_seeds_confirmed`.
   `None` -> rating-only + the EXISTING `app.py:152` banner.
3. **App wiring** — a "fetch now" button (+ per-round trigger) calling the script OUT of the rerun
   path (Pitfall 11); when a valid cache loads, its blended p/var feed the back-solved ratings
   (`engine.backsolve.fit_ratings`) + the epistemic blend/var into the EXISTING `run_mc` cache path
   so the odds change the reported probabilities. The run cache key folds in the cache's
   `_meta.fetched_at` so a fresh fetch that moves only `var` (not the ratings) still invalidates the
   memoized Result — no stale epistemic band (T-05-STALEBAND).
4. **Preserve zero-config** — no key + no cache -> rating-only, the existing banner, no crash;
   `odds_key_present()` still checks `os.environ` ONLY (no httpx/dotenv pulled into the app, DX-01).
5. **Packaging + gitignore** — add `httpx`/`python-dotenv` (versions confirmed) to `pyproject.toml`,
   add `odds/`+`scripts/` to the packages list, gitignore `data/odds_cache.json`.

The new fail-soft fact (05-RESEARCH): Polymarket + Kalshi are keyless public reads, so the app
fetches a 2-source pool with NO key — the key only unlocks the Pinnacle anchor. **The live Cologne
slug/ticker confirmation is a DEFERRED user gstack `/browse` checkpoint** (markets-not-posted on
2026-05-29 is the expected state); the script + adapters are built against recorded fixtures and a
provider returning `[]` yields a valid empty-blended cache, not an error. This plan does NOT
hard-depend on a live fixture existing today.

Output: `pyproject.toml` + `.gitignore` updates, `scripts/fetch_odds.py`, `ui/odds_loader.py`,
`app.py` wiring, and AppTest coverage in `tests/test_app.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/05-odds-ensemble/05-RESEARCH.md
@.planning/phases/05-odds-ensemble/05-01-PLAN.md
@.planning/phases/05-odds-ensemble/05-02-PLAN.md
@TODOS.md
@app.py
@ui/state.py
@ui/cache.py
@engine/teams.py
@engine/backsolve.py
@.env.example
@tests/test_app.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 deps gate + scripts/fetch_odds.py + ui/odds_loader.py + RED AppTests</name>
  <read_first>
    @.planning/phases/05-odds-ensemble/05-RESEARCH.md
    @.planning/phases/05-odds-ensemble/05-01-PLAN.md
    @ui/state.py
    @engine/teams.py
    @tests/test_app.py
  </read_first>
  <files>pyproject.toml, .gitignore, scripts/__init__.py, scripts/fetch_odds.py, ui/odds_loader.py, tests/test_app.py</files>
  <behavior>
    - DEPS GATE (Wave 0): confirm the two new installs on PyPI before adding them — `pip index versions httpx` and `pip index versions python-dotenv` — and pin the confirmed current minor (httpx ~0.28.x, python-dotenv ~1.0.x per A1, but USE the versions the index reports). Add both to pyproject.toml deps, add `odds`/`scripts` to the packages list so `python -m scripts.fetch_odds` resolves, and add `data/odds_cache.json` to .gitignore (a fetched artifact, not source; .env is already ignored).
    - scripts/fetch_odds.py: `main(out_path="data/odds_cache.json") -> dict` loads .env via python-dotenv, builds teams via engine.teams.load_teams, calls `discover_fixtures(teams)` (alias match — reuse build_round1_pairs for the known R1 matchups), iterates (OddsPapiProvider, PolymarketProvider, KalshiProvider) each in its own try/except (a provider failure logs + continues — fail-soft), pools quotes per match via odds.base.pool, builds the FROZEN cache dict {_meta:{fetched_at,version:1,providers_present,round_hint}, blended:{match_key:{p,var,n_sources,bo3}}} (match_key = sorted "loid-hiid"), writes it read-only, returns the dict. `_meta.fetched_at` is an ISO-8601 UTC timestamp set at write time (the run cache key consumes it to detect a fresh fetch — T-05-STALEBAND). Runnable as `python -m scripts.fetch_odds`. NO streamlit import.
    - discover_fixtures(teams): resolves each provider's Cologne market by alias-matching team names; live URL discovery is a thin httpx-guarded branch built but DEFERRED-verified (the recorded-fixture path is what the tests exercise). A provider with no market yields [] -> a valid empty blended map (not an error).
    - ui/odds_loader.load_odds_cache(path="data/odds_cache.json") -> dict | None: json.loads(Path(path).read_text); if `_meta.version != 1` return None; return the dict; `except (OSError, ValueError): return None`. NO httpx/dotenv/provider import (json + pathlib only) so the app import path stays zero-config.
    - RED AppTests in tests/test_app.py (Task 2 flips them GREEN), marked per the file's Wave-0 convention: test_no_odds_failsoft, test_zero_config_first_run_still_works, test_cache_present_feeds_sim, test_app_makes_no_network_on_rerun, test_cache_loader_failsoft, test_fresh_fetch_invalidates_cache. The loader fail-soft test (missing/bad/wrong-version -> None) can be a pure unit assertion (cheaper than AppTest).
  </behavior>
  <action>
    First the DEPS GATE: run `pip index versions httpx` and `pip index versions python-dotenv`
    (Package Legitimacy Audit — both are CLAUDE.md-named, mainstream; a version-confirm task, not a
    human checkpoint). Add the confirmed pins to `pyproject.toml` `dependencies`, add `"odds"` and
    `"scripts"` to the packages/tool config so `python -m scripts.fetch_odds` imports, and append
    `data/odds_cache.json` to `.gitignore`. Create `scripts/__init__.py` (package marker) and
    `scripts/fetch_odds.py` per the behavior — the 05-RESEARCH "Fetch + cache seam" section
    (lines 406-459) is the canonical source for `main`, `discover_fixtures`, and the exact cache
    schema; implement them verbatim (sorted "lo-hi" keys, p = P(lower-id wins), raw var). Set
    `_meta.fetched_at` to an ISO-8601 UTC timestamp at write time so the app's run cache key can fold
    it in (T-05-STALEBAND). Mark the live-URL discovery branch
    `[DEFERRED — verify once Cologne markets post]` in a docstring (A2-A5), and ensure a provider
    returning [] produces a valid empty `blended: {}` cache, not an error. Create `ui/odds_loader.py`
    exactly mirroring `read_seeds_confirmed`'s fail-safe pattern (json + pathlib ONLY, version gate,
    return None on any error). Add the six RED AppTest/unit stubs to `tests/test_app.py` (no key +
    no cache -> rating-only + banner; zero-config first run; cache-present feeds the sim; no network
    on rerun via monkeypatching httpx to raise; loader fail-soft; a fresh fetched_at re-runs the sim
    not a stale serve). Do NOT edit app.py yet. Keep N small (N≈2000) in any AppTest per the Phase-2
    latency budget.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_app.py 2>&1 | tail -10</automated>
  </verify>
  <done>httpx/python-dotenv pinned in pyproject.toml (versions index-confirmed) with odds/+scripts/ packaged; data/odds_cache.json gitignored; scripts/fetch_odds.py exists (runnable as `python -m scripts.fetch_odds`, writes the frozen schema with an ISO-8601 `_meta.fetched_at`, per-provider fail-soft, no streamlit import) and ui/odds_loader.py is json-only fail-soft (None on any error); the new tests/test_app.py cases are present and RED (app.py not yet wired); `uv run pytest -q` shows only the new app cases failing. No engine mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: app.py — fetch-now button + cache-fed run path + preserved fail-soft (GREEN)</name>
  <read_first>
    @.planning/phases/05-odds-ensemble/05-RESEARCH.md
    @app.py
    @ui/state.py
    @ui/cache.py
    @ui/odds_loader.py
    @scripts/fetch_odds.py
    @engine/backsolve.py
    @engine/teams.py
    @tests/test_app.py
  </read_first>
  <files>app.py, tests/test_app.py</files>
  <behavior>
    - test_no_odds_failsoft: with NO ODDSPAPI_KEY and NO data/odds_cache.json, the app renders, runs rating-only, shows the existing 'live odds off (no ODDSPAPI_KEY) — using manual ratings' banner, and never raises (ODDS-08). No odds path crashes the absent-cache first run.
    - test_zero_config_first_run_still_works: a fresh run with no key + no cache hits Run and produces per-team P(advance) (the Phase-2 DX-01 promise unbroken).
    - test_cache_present_feeds_sim: with a valid data/odds_cache.json present (write one via scripts.fetch_odds.main against recorded fixtures, or a fixture cache file), load_odds_cache returns it and the run path uses the back-solved ratings + epistemic blend/var so at least one reported P differs from the pure rating-only run (the odds change the numbers, ODDS-04/05).
    - test_app_makes_no_network_on_rerun [Pitfall 11]: monkeypatch httpx (and the providers) to RAISE on any call; assert the app still renders and runs on a rerun — proving the render path makes NO provider call; only the explicit 'fetch now' button (not exercised on a plain rerun) would call scripts.fetch_odds.
    - test_cache_loader_failsoft: load_odds_cache on a missing path / malformed JSON / version!=1 returns None (pure unit).
    - test_fresh_fetch_invalidates_cache [T-05-STALEBAND]: write a cache, run the sim (memoizes a Result); then write a SECOND cache with the SAME back-solved ratings but a DIFFERENT `_meta.fetched_at` (and a moved `var`); assert the next run recomputes (the reported band reflects the new var) rather than serving the first run's stale memoized Result — proving `fetched_at` is in the run cache key. If folding `fetched_at` into the key is NOT a clean 1-line change, this test may instead assert the documented limitation is recorded (see action) — but PREFER the key extension.
    - The 'fetch now' button (and per-round trigger) calls scripts.fetch_odds.main on click ONLY (a user action, never in the render path); after it writes the cache, the next run reads it via load_odds_cache. The app imports ui.odds_loader (json-only) — it does NOT import scripts.fetch_odds at module top (import it lazily inside the button handler so httpx/dotenv never enter the app import path).
  </behavior>
  <action>
    In `app.py`: import `load_odds_cache` from `ui.odds_loader` (json-only) at the top; do NOT
    import `scripts.fetch_odds` or any `odds.*` provider at module top (import scripts.fetch_odds
    LAZILY inside the button handler so httpx/dotenv stay out of the app import path — DX-01).
    (1) Add a 'fetch now' button (and a per-round trigger in LIVE mode) whose handler lazily
    imports and calls `scripts.fetch_odds.main()` then reruns — this is an explicit user action,
    NEVER reached on a plain rerun (Pitfall 11). Surface a fail-soft st.error/st.info if the fetch
    raises (it shouldn't — the script is per-provider fail-soft — but the button never crashes the
    app).
    (2) In the run path (the `_run_or_serve`/`_compute_or_serve` region, app.py:355-398), BEFORE
    computing, call `cache = load_odds_cache()`; if it is a valid dict, derive back-solved ratings
    via `engine.backsolve.fit_ratings(targets, teams, S, anchor_id)` (build `targets` by
    `invert_series`-ing each blended match p to map level) and pass the per-match blend/var into the
    MC so the epistemic outer loop draws (the run_mc/run_mc_progressive call gains the cache-derived
    ratings override + blend/var); if `cache` is None, run rating-only EXACTLY as today.
    (3) STALE-BAND GUARD (T-05-STALEBAND): the run path keys on `_cache_key_for(ratings, locked)`
    (app.py:364) and `blend`/`var` are NOT in that key — so a fresh fetch that moves ONLY `var` (not
    the back-solved ratings) would serve the FIRST run's stale epistemic band. Fold the cache's
    `_meta.fetched_at` into the run cache key (e.g. `_cache_key_for(ratings, locked, cache.get('_meta', {}).get('fetched_at'))`)
    so any fresh fetch invalidates the memoized Result. PREFER this 1-line key extension; if for some
    reason it is not a clean change, instead document the limitation EXPLICITLY in the 05-03 SUMMARY
    (a fetch that moves only var without moving ratings serves a stale band) so Phase 6 does not
    inherit a silent stale-band bug — but the clean key extension is the intended path. Otherwise keep
    the cache_key derivation honest — the odds-derived ratings already change `freeze_ratings(ratings)`.
    (4) PRESERVE the existing fail-soft: the `if not odds_key_present(): st.info('live odds off
    …')` banner (app.py:152) stays verbatim; odds_key_present() keeps its os.environ-only check.
    With no cache the app branch is the unchanged rating-only path.
    Flip test_no_odds_failsoft, test_zero_config_first_run_still_works, test_cache_present_feeds_sim,
    test_app_makes_no_network_on_rerun, test_cache_loader_failsoft, and
    test_fresh_fetch_invalidates_cache to GREEN (keep N≈2000 in AppTests per the latency budget). Do
    NOT mutate any engine/odds/scripts module — import them only (the engine seams were filled in
    05-02; this plan only consumes them).
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_app.py 2>&1 | tail -10</automated>
  </verify>
  <done>tests/test_app.py passes: no-key+no-cache -> rating-only + the existing banner with no crash; the zero-config first run still produces P(advance); a present valid cache feeds the back-solve + epistemic so ≥1 reported P differs; a fresh fetch (new `_meta.fetched_at`, moved var) invalidates the memoized Result so the new band is recomputed — NOT stale-served (T-05-STALEBAND, via the fetched_at-in-key extension; if not folded into the key, the limitation is documented in the SUMMARY); the app makes NO provider/network call on a rerun (httpx monkeypatched to raise, app still renders); the loader is fail-soft None on missing/bad/wrong-version. `uv run pytest -q` full suite green; `uv run streamlit run app.py` -> no key -> rating-only + banner; with a cache present -> odds-fed sim; GATE-01 backtest still green (no engine mutation).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: DEFERRED — confirm live Cologne slugs/tickers via gstack /browse once markets post</name>
  <what-built>
    The OddsPapi/Polymarket/Kalshi adapters (05-01) + scripts/fetch_odds.py discovery are built and
    unit-tested against RECORDED JSON fixtures (markets are not posted on 2026-05-29 — the expected
    state). The live slug/ticker/field-name shapes (Assumptions A2-A5) are marked
    [DEFERRED — verify once Cologne markets post] and cannot be confirmed until IEM Cologne 2026
    markets exist. This is NOT a build blocker — the app already runs rating-only + 2-source-keyless
    fully fail-soft.
  </what-built>
  <how-to-verify>
    ONCE IEM Cologne 2026 CS2 markets post (closer to the event), using gstack `/browse` (never the
    mcp chrome tools — CLAUDE.md):
    1. OddsPapi: confirm CS2 IEM Cologne fixture coverage and the real `/fixtures` + `/odds` field
       names (tournament string, `bookmakerOdds`/market-id, participant names); adjust
       `odds/oddspapi.py` parsing + `discover_fixtures` if the recorded-fixture shapes differ.
       Budget the ~250 req/month free quota — manual button + per-round trigger only, no polling.
    2. Polymarket: confirm the Cologne event slug and whether per-match (vs only tournament-winner)
       markets exist; adjust `odds/polymarket.py` (keyless Gamma `/events`).
    3. Kalshi: confirm the CS2/Cologne series ticker (esports coverage is sparse — `[]` is a valid
       fail-soft result); adjust `odds/kalshi.py` (keyless trade-api v2).
    4. Run `python -m scripts.fetch_odds` against the live endpoints, confirm `data/odds_cache.json`
       writes a non-empty `blended` map, then hit Run in the app and confirm the odds-fed probs
       move vs rating-only.
    Any adapter adjustment is a small parser tweak validated by re-recording the fixture and
    re-running `uv run pytest -q tests/test_odds.py` — the math (de-vig/pool/back-solve) does not
    change.
  </how-to-verify>
  <resume-signal>Type "verified" once live slugs/tickers are confirmed and a live fetch populates the cache, or "deferred" to keep the recorded-fixture build as-is until markets post (the app remains fully functional rating-only + keyless either way).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env ODDSPAPI_KEY -> scripts/fetch_odds.py | The key is read ONLY in the fetch script via dotenv; never in ui/app; presence-only `odds_key_present` (os.environ) preserved — value never logged/returned (T-02-SECRET) |
| provider network/JSON -> scripts.fetch_odds | Untrusted external responses parsed per-provider in try/except (fail-soft); a failure logs + continues, never crashes the fetch or the app |
| data/odds_cache.json -> ui.odds_loader -> app | Read-only json; version-gated; loader returns None on any error -> rating-only + banner; raw `var` clamped downstream by beta_moment_fit (not in the file); `_meta.fetched_at` folds into the run cache key so a var-only refetch is not stale-served |
| 'fetch now' button -> scripts.fetch_odds | The ONLY path that calls providers; an explicit user action OUT of the render path; the app never calls get_quotes on a rerun (Pitfall 11) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-RERUN | DoS / Info disclosure | the app calling providers on every rerun (slow, quota burn, key-in-session) (Pitfall 11) | mitigate | the app reads data/odds_cache.json only; fetch is scripts/fetch_odds.py behind an explicit button; test_app_makes_no_network_on_rerun monkeypatches httpx to raise and asserts the app still renders |
| T-05-APPIMPORT | Info disclosure / DX | httpx/dotenv entering the app import path (zero-config regression, DX-01) | mitigate | ui/odds_loader.py is json+pathlib only; scripts.fetch_odds imported LAZILY inside the button handler; odds_key_present stays os.environ-only |
| T-05-NOCACHE | DoS | a missing/empty/bad cache crashing the app (the normal first-run state) (ODDS-08) | mitigate | load_odds_cache returns None on any error -> rating-only + the existing banner; test_no_odds_failsoft + test_cache_loader_failsoft assert it |
| T-05-STALEBAND | Tampering / Info disclosure | a fresh fetch that moves only `var` (not the back-solved ratings) serving the prior run's stale epistemic band, since `_cache_key_for(ratings, locked)` (app.py:364) excludes blend/var | mitigate | fold `_meta.fetched_at` into the run cache key so any fresh fetch invalidates the memoized Result; test_fresh_fetch_invalidates_cache asserts a new fetched_at recomputes the band. Fallback (if the 1-line key extension is not clean): the limitation is explicitly documented in the 05-03 SUMMARY so Phase 6 does not inherit it silently. Accepted residual: a hand-edited cache reusing an old fetched_at with a new var (out of scope — local single-user, version-gated) |
| T-05-SECRET | Info disclosure | ODDSPAPI_KEY leaking into the UI/session/logs | mitigate | key read ONLY in scripts/fetch_odds.py via dotenv; never in ui/app; presence-only check (state.py:144 rule) preserved; .env gitignored |
| T-05-TAMPER | Tampering | a hand-edited cache file skewing probs | mitigate | local single-user tool; `version` gate + the downstream beta_moment_fit clamp bound the blast radius; the cache is gitignored (not a trusted source artifact) |
| T-05-SC | Tampering | httpx/python-dotenv install provenance | mitigate | both CLAUDE.md-named, verified via `pip index versions` in the Wave-0 deps gate; no exotic packages; T-05-SC tracked here |
</threat_model>

<verification>
- `uv run pytest -q tests/test_app.py` — no-key+no-cache rating-only + banner, no crash (ODDS-08); zero-config first run intact (DX-01); a present cache feeds the back-solve+epistemic and moves ≥1 P (ODDS-04/05); a fresh fetch (new `_meta.fetched_at`, moved var) recomputes the band rather than stale-serving (T-05-STALEBAND); NO network on rerun (Pitfall 11/ODDS-07); loader fail-soft None on missing/bad/wrong-version.
- `uv run pytest -q` — full suite green; engine + odds (05-01) + back-solve/epistemic (05-02) + Phase-2/3/4 UI all green; GATE-01 Budapest backtest green (no engine mutation — this plan consumes the 05-02 seams, it does not touch engine files).
- DEFERRED manual (gstack /browse, once markets post): confirm live OddsPapi/Polymarket/Kalshi Cologne slugs/tickers + a live `python -m scripts.fetch_odds` populates `data/odds_cache.json`.
</verification>

<success_criteria>
- `scripts/fetch_odds.py` is the ONE entry (button AND v2 cron) writing the frozen `data/odds_cache.json` schema (with an ISO-8601 `_meta.fetched_at`); the app NEVER imports it and NEVER calls providers on a rerun (ODDS-06/07, Pitfall 11).
- `ui/odds_loader.load_odds_cache` is json-only, version-gated, fail-soft None on any error -> rating-only + the existing banner (ODDS-08); no httpx/dotenv in the app import path (DX-01).
- A present valid cache feeds the back-solved ratings + epistemic blend/var into the existing run path so the odds change the reported probabilities (ODDS-04/05 wiring); a fresh fetch invalidates the memoized Result via `_meta.fetched_at` in the run cache key so no stale epistemic band is served (T-05-STALEBAND).
- Zero-config first run preserved: no key + no cache -> rating-only, the existing banner, no crash; keyless Polymarket+Kalshi still yields a 2-source pool when fetched (ODDS-08).
- `data/odds_cache.json` gitignored; the v2 cron is a single crontab line with zero app change (ODDS-07/OPS-01 seam); the live slug/ticker confirmation is a deferred gstack /browse checkpoint, not a build blocker.
</success_criteria>

<output>
Create `.planning/phases/05-odds-ensemble/05-03-SUMMARY.md` when done.
</output>
</content>
