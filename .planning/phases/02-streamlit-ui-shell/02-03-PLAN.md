---
phase: 02-streamlit-ui-shell
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - app.py
  - ui/state.py
  - ui/render.py
  - README.md
  - .env.example
  - tests/test_app.py
autonomous: true
requirements: [UI-07, DX-02, DX-04, DX-05]
tags: [streamlit, ui, trust-badge, seed-banner, dx, readme]
user_setup: []

must_haves:
  truths:
    - "A persistent header trust badge reads EXACTLY `engine validated vs Valve rulebook unit tests — full backtest pending seed data` (UI-07)"
    - "The badge's validated state is gated on BOTH a BACKTEST_PASSED constant (currently False) AND seeds_confirmed — in Phase 2 both are unmet so the badge ships caveated and cannot silently drift to a green 'validated' claim (UI-07)"
    - "A persistent dismissable `⚠ Seeds are INFERRED` banner with a field-by-field reconcile area shows until a `seeds_confirmed` toggle dismisses it (DX-02)"
    - "A `live odds off (no ODDSPAPI_KEY) — using manual ratings` info banner shows when no key is set, never crashing (fail-soft stub; ODDS-08 seam)"
    - "README opens with the quickstart in the DX-04 order and ends with a 30-second event-day cold-start drill (DX-04)"
    - "`.env.example` lists optional provider keys and `.env` is gitignored (DX-05)"
  artifacts:
    - path: "app.py"
      provides: "header strip: trust badge (caveated, BOTH-gated) + INFERRED-seed banner + seeds_confirmed toggle + fail-soft odds-off info banner"
      min_lines: 120
    - path: "ui/state.py"
      provides: "BACKTEST_PASSED=False constant; trust_badge_text()/badge state gate; seeds_confirmed read helper; odds-key presence check"
      exports: ["BACKTEST_PASSED", "trust_badge_state"]
    - path: "README.md"
      provides: "DX-04 quickstart at top + event-day cold-start drill"
      contains: "uv run streamlit run app.py"
    - path: ".env.example"
      provides: "optional provider keys (DX-05)"
      contains: "ODDSPAPI_KEY"
  key_links:
    - from: "app.py"
      to: "ui.state.trust_badge_state"
      via: "badge reads BACKTEST_PASSED AND seeds_confirmed"
      pattern: "trust_badge"
    - from: "app.py"
      to: "data/stage1.json seeds_confirmed"
      via: "banner persists until the seeds_confirmed toggle flips session_state"
      pattern: "seeds_confirmed"
    - from: ".gitignore"
      to: ".env"
      via: "secrets never committed (already present)"
      pattern: "\\.env"
---

<objective>
Ship the trust + seed-safety header and the DX docs that make Phase 2 honest and event-day-ready. The trust badge reads the EXACT caveated wording and is gated on BOTH a `BACKTEST_PASSED=False` constant AND the seeds-confirmed toggle so it cannot silently claim more than is true (the deferred Budapest backtest, GATE-01/04/05). The persistent dismissable `⚠ Seeds are INFERRED` banner with a reconcile area protects against the silent seed-corruption failure mode until the user confirms. A fail-soft `live odds off` info banner stubs the Phase 5 seam without any network import. The README quickstart + cold-start drill and `.env.example` close DX-04/DX-05.

Purpose: After this plan a user (or a friend handed the repo) trusts exactly what is true, is loudly reminded the seeds are inferred until they confirm, and can go from clone to first sim in under two minutes — and on event day, run a 30-second cold-start drill instead of debugging.

Output: an extended `app.py` (header strip), `ui/state.py` (badge gate + BACKTEST_PASSED + seeds_confirmed read), `README.md`, `.env.example`, and AppTest coverage for the badge wording/gate and the dismissable banner.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/02-streamlit-ui-shell/02-UI-SPEC.md
@.planning/phases/02-streamlit-ui-shell/02-RESEARCH.md
@.planning/phases/02-streamlit-ui-shell/02-VALIDATION.md
@files/HANDOFF_claude_code.md
@.planning/phases/02-streamlit-ui-shell/02-02-SUMMARY.md
@app.py
@ui/state.py
@data/stage1.json
@.gitignore
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BACKTEST_PASSED gate + seeds_confirmed read + trust-badge state in ui/state.py (GREEN)</name>
  <read_first>
    @ui/state.py
    @data/stage1.json
    @engine/teams.py
    @.planning/phases/02-streamlit-ui-shell/02-UI-SPEC.md
    @.planning/phases/02-streamlit-ui-shell/02-RESEARCH.md
    @tests/test_app.py
  </read_first>
  <files>ui/state.py, tests/test_app.py</files>
  <behavior>
    - BACKTEST_PASSED is a module-level constant currently `False` (RESEARCH Open Question 3 — an explicit flag so the badge can't drift true; flipped only when GATE-01 lands).
    - trust_badge_state(seeds_confirmed) returns "validated" ONLY when BACKTEST_PASSED AND seeds_confirmed are BOTH true; otherwise returns the caveated state (Pitfall 6 — "both, not one").
    - The caveated badge text is EXACTLY `engine validated vs Valve rulebook unit tests — full backtest pending seed data` (test_trust_badge_wording asserts the exact string; assert it does NOT contain "Budapest" or a green-check claim).
    - A helper reads `seeds_confirmed` from data/stage1.json (read-only) as the initial banner state (the JSON ships `false`); the in-session toggle overrides it in session_state.
  </behavior>
  <action>
    Extend `ui/state.py`: add `BACKTEST_PASSED = False` (module-level, with a comment that it flips only when the deferred GATE-01 backtest actually runs — STATE.md 2026-05-29 decision). Add `TRUST_BADGE_CAVEATED = "engine validated vs Valve rulebook unit tests — full backtest pending seed data"` as the exact string (UI-SPEC Copywriting; MUST NOT mention Budapest/Austin or a green ✓). Add `trust_badge_state(seeds_confirmed: bool) -> str` returning `"validated"` iff `BACKTEST_PASSED and seeds_confirmed` else `"caveated"` (Pitfall 6 — gated on BOTH). Add `read_seeds_confirmed(path=None) -> bool` that parses the local stage1.json `seeds_confirmed` field read-only (do NOT mutate the frozen engine or the JSON; the engine's load_teams does not surface it, so read the raw JSON here) and falls back to False. Add an `odds_key_present() -> bool` helper that checks `os.environ.get("ODDSPAPI_KEY")` WITHOUT importing httpx/python-dotenv (just `os` — the fail-soft stub must not pull the Phase 5 deps).
    Flip test_trust_badge_wording to GREEN (assert the exact caveated string, the both-gated state logic, and absence of "Budapest"/green-check).
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_app.py::test_trust_badge_wording</automated>
  </verify>
  <done>BACKTEST_PASSED=False present; trust_badge_state returns "validated" only when both flags true; the caveated string is exact and Budapest-free; read_seeds_confirmed reads the JSON read-only; odds_key_present uses only os (no httpx/dotenv); test green.</done>
</task>

<task type="auto">
  <name>Task 2: Header strip in app.py — caveated badge, INFERRED-seed banner + confirm toggle, fail-soft odds banner</name>
  <read_first>
    @app.py
    @ui/state.py
    @ui/render.py
    @.planning/phases/02-streamlit-ui-shell/02-UI-SPEC.md
    @files/HANDOFF_claude_code.md
    @tests/test_app.py
  </read_first>
  <files>app.py, tests/test_app.py</files>
  <behavior>
    - test_trust_badge_caveated_until_both / test_trust_badge_wording (AppTest): the header shows the exact caveated badge text; it does NOT show a "validated"/green state (because BACKTEST_PASSED is False).
    - test_seed_banner / test_seed_banner_dismissable: the `⚠ Seeds are INFERRED` warning is present on first load; after toggling "seeds confirmed" it is dismissed (no longer rendered); the toggle drives session_state.
    - With no ODDSPAPI_KEY set, the `live odds off … using manual ratings` info banner renders and the app does not crash (fail-soft).
    - The plan-01/02 zero-config Run, mode toggle, and CI bars still work (no regression).
  </behavior>
  <action>
    Extend `app.py` with a persistent header strip rendered in BOTH modes (above the mode toggle / body), per UI-SPEC Layout "Header strip": (1) the trust badge using `ui.state.trust_badge_state(seeds_confirmed)` — render `TRUST_BADGE_CAVEATED` text in the caveated state; never a green ✓ or "Budapest" (UI-07, Pitfall 6); (2) the `⚠ Seeds are INFERRED — verify vs the official seed list before trusting outputs.` banner via `st.warning`, shown until dismissed, WITH a field-by-field reconcile area (a small expander/table listing each seed→team so the user can eyeball vs the official list — DX-02 "reconcile diff"), and a `seeds_confirmed` toggle (`st.toggle`/`st.checkbox`, key "seeds_confirmed") that on True dismisses the banner and is the gate the badge reads; persist via session_state seeded from `read_seeds_confirmed()`; (3) when `not odds_key_present()`, an `st.info` banner `live odds off (no ODDSPAPI_KEY) — using manual ratings` (one line, never a crash — ODDS-08/DX fail-soft seam; NO httpx/dotenv import). Keep the "seeds confirmed" toggle a positive confirmation (not destructive — no red, protecting the never-red/green rule).
    Flip test_seed_banner(_dismissable) and test_trust_badge_caveated_until_both to GREEN; keep plan-01/02 tests green (no regression).
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_app.py</automated>
  </verify>
  <done>Header shows the exact caveated trust badge (no validated/green, no Budapest); the INFERRED-seed banner + reconcile area shows until the seeds_confirmed toggle dismisses it; the fail-soft odds-off info banner shows with no key and no crash and no Phase-5 import; full suite green.</done>
</task>

<task type="auto">
  <name>Task 3: README quickstart + event-day cold-start drill + .env.example</name>
  <read_first>
    @files/HANDOFF_claude_code.md
    @.planning/phases/02-streamlit-ui-shell/02-UI-SPEC.md
    @.gitignore
    @pyproject.toml
    @tests/test_app.py
  </read_first>
  <files>README.md, .env.example, tests/test_app.py</files>
  <behavior>
    - test_env_example_present_and_gitignored: `.env.example` exists and lists ODDSPAPI_KEY (+ Polymarket/Kalshi optional keys); `.env` appears in .gitignore (already present — assert it stays).
    - README starts with the DX-04-ordered quickstart and ends with the 30-second cold-start drill.
  </behavior>
  <action>
    Create `README.md` with the quickstart AT THE TOP in the exact DX-04 / HANDOFF §10.6 order: (1) one-line what-it-is; (2) `uv run streamlit run app.py` as the primary command + the pip fallback line (`pip install streamlit numpy` then `streamlit run app.py`); (3) "first sim needs no API key" + the ~15s/100k-sims expectation; (4) what the numbers mean (2 lines: P(3-0)/P(advance)/P(0-3); CI bands = honest uncertainty, not false precision); (5) the [INFERRED]-seed reconcile step (verify vs the official seed list at lock, flip "seeds confirmed"); (6) optional `.env.example` for live odds (v1+/Phase 5). End the README with a 30-second event-day cold-start drill: `git pull` → confirm official seeds → flip "seeds confirmed" → Run. Mention the caveated trust-badge meaning (validated vs rulebook unit tests; full backtest pending seeds) so README and badge agree.
    Create `.env.example` listing the optional provider keys as commented placeholders: `ODDSPAPI_KEY=`, `POLYMARKET_...`, `KALSHI_...` with a note they are Phase-5/optional and the app runs rating-only without them. Confirm `.env` is already in `.gitignore` (it is — do not remove it).
    Flip test_env_example_present_and_gitignored to GREEN (file presence + key listed + `.env` in .gitignore).
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_app.py::test_env_example_present_and_gitignored</automated>
  </verify>
  <done>README opens with the DX-04-ordered quickstart and ends with the cold-start drill; .env.example lists ODDSPAPI_KEY (+ optional Polymarket/Kalshi) and notes rating-only fallback; .env stays gitignored; test green; full suite `uv run pytest -q` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| os.environ (ODDSPAPI_KEY) → app.py | Presence check only — no key value rendered or logged |
| data/stage1.json seeds_confirmed → app.py | Local read-only flag seeding the banner state |
| .env / .env.example | Secrets boundary — .env must never be committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-BADGE | Repudiation/Tampering | trust badge claim | mitigate | Badge gated on BOTH BACKTEST_PASSED (constant False) AND seeds_confirmed; caveated string is exact; cannot drift to a green "validated" claim while the backtest is deferred (UI-07, Pitfall 6) |
| T-02-SECRET | Information Disclosure | .env / odds key | mitigate | Only `.env.example` (no real values) is committed; `.env` stays gitignored; odds_key_present checks presence only and never renders/logs the key value |
| T-02-NOIMPORT | Tampering | fail-soft odds banner | mitigate | The odds-off banner uses only `os`; httpx/python-dotenv are NOT imported in Phase 2 (preserves the zero-config first-run promise; Phase 5 owns the network path) |
</threat_model>

<verification>
- `uv run pytest -q tests/test_app.py` — trust-badge wording/gate, seed-banner dismissable, env-example present + gitignored green; plan-01/02 regression green.
- `uv run pytest -q` — full suite green (engine unregressed).
- Manual (VALIDATION Manual-Only): README quickstart ordering + cold-start drill read-through; badge/banner render correctly in the running app.
</verification>

<success_criteria>
- Trust badge reads EXACTLY the caveated string, gated on BOTH BACKTEST_PASSED (False) AND seeds_confirmed; never a green/Budapest claim (UI-07).
- Persistent dismissable INFERRED-seed banner with a reconcile area; "seeds confirmed" toggle dismisses it (DX-02).
- Fail-soft `live odds off` info banner with no key, no crash, no Phase-5 import (ODDS-08 seam).
- README quickstart in DX-04 order + 30-second cold-start drill (DX-04).
- `.env.example` lists optional keys; `.env` gitignored (DX-05).
</success_criteria>

<output>
Create `.planning/phases/02-streamlit-ui-shell/02-03-SUMMARY.md` when done.
</output>
