---
phase: 02-streamlit-ui-shell
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app.py
  - ui/render.py
  - ui/state.py
  - tests/test_app.py
  - tests/test_ui_helpers.py
autonomous: true
requirements: [UI-01, UI-04, UI-06]
tags: [streamlit, ui, layout, theming, tokens, colorblind]
user_setup: []

must_haves:
  truths:
    - "A Pre-stage / Live mode toggle is present and selecting it reorders the main column (UI-01)"
    - "Pre-stage main column order: ballot-area placeholder > per-team probs > bracket (collapsed); Live order: locked-pick status placeholder > delta-probs area > bracket (UI-01)"
    - "Every probability cell renders the number PLUS an always-visible inline Wilson CI mini-bar beneath it (never hover/expand-hidden) (UI-04)"
    - "All numbers (probs, records, sim count) render monospace (UI-06)"
    - "Status uses colorblind-safe blue(advanced)/amber(eliminated) — NEVER red/green — always paired with an ASCII glyph + text label (`o live` / `x dead` / `/ secured`) (UI-06)"
    - "The accent color is reserved for the Run CTA, the hero number, and the active mode segment only — not status, CI bars, or ordinary elements (UI-06)"
  artifacts:
    - path: "app.py"
      provides: "st.segmented_control mode toggle + mode-conditional main-column ordering; ballot/live-status structured placeholders; collapsed bracket expander"
      min_lines: 90
    - path: "ui/render.py"
      provides: "status_glyph/status_label/status_hue mapping (ASCII glyphs, blue/amber), hero-number renderer, CI-bar already present from plan 01"
      exports: ["status_badge_html", "STATUS"]
    - path: "ui/state.py"
      provides: "Mode enum used to drive the main-column reorder"
      exports: ["Mode"]
  key_links:
    - from: "app.py"
      to: "ui.state.Mode"
      via: "st.segmented_control selection → mode-conditional section ordering"
      pattern: "segmented_control"
    - from: "app.py"
      to: "ui.render.ci_bar_html"
      via: "per-team prob cells render number + inline CI bar"
      pattern: "ci_bar_html"
    - from: "app.py"
      to: "ui.render.status_badge_html"
      via: "status rows render glyph + label + colorblind-safe hue"
      pattern: "status"
---

<objective>
Layer the two-mode shell and the locked visual-token contract onto the plan-01 runnable slice. Add the `st.segmented_control` Pre-stage/Live toggle whose selection reorders the main column (UI-01), make every probability cell a number + always-visible inline Wilson CI mini-bar (UI-04), and enforce the UI-06 token system: monospace numbers, one reserved accent, and colorblind-safe blue/amber status always paired with an ASCII glyph + text label.

Purpose: After this plan the user can switch between Pre-stage and Live framing and read honest, never-hidden uncertainty in the locked colorblind-safe visual language — the design contract made real. The ballot and live-status panels ship as structured placeholders wired for plans 3/4 (the optimizer and live mode are later phases); this plan owns the SHELL + probs presentation, not their content.

Output: an extended `app.py` (mode toggle + reordering + placeholders + collapsed bracket), an extended `ui/render.py` (status glyph/label/hue mapping + hero renderer), and AppTest/unit coverage for the toggle-reorders behavior and the never-red/green palette.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/02-streamlit-ui-shell/02-UI-SPEC.md
@.planning/phases/02-streamlit-ui-shell/02-RESEARCH.md
@.planning/phases/02-streamlit-ui-shell/02-VALIDATION.md
@.planning/phases/02-streamlit-ui-shell/02-01-SUMMARY.md
@app.py
@ui/render.py
@ui/state.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Status tokens + hero renderer in ui/render.py (colorblind-safe, ASCII) (GREEN)</name>
  <read_first>
    @ui/render.py
    @.planning/phases/02-streamlit-ui-shell/02-UI-SPEC.md
    @.planning/phases/02-streamlit-ui-shell/02-RESEARCH.md
    @tests/test_ui_helpers.py
  </read_first>
  <files>ui/render.py, tests/test_ui_helpers.py</files>
  <behavior>
    - A STATUS mapping defines exactly three states: advanced/secured (glyph "/", blue #3B82F6), live (glyph "o", blue #3B82F6), eliminated/dead (glyph "x", amber #F59E0B).
    - No status hue is red or green (test_status_palette_never_red_green): assert every hue in STATUS is in {#3B82F6, #F59E0B}, never a red/green family value.
    - status_badge_html(state) returns a span containing BOTH the ASCII glyph AND the text label (color is reinforcement, not the only signal) — glyphs are ASCII only (`o`/`x`/`/`), never Unicode (cp1252 console lesson from Phase 1).
    - The hero-number renderer produces a display-size monospace number using the accent color (the one big number; not red/green/amber).
  </behavior>
  <action>
    Extend `ui/render.py` with the `STATUS` dict from RESEARCH "Status glyph + label" example: keys advanced/live/eliminated → (glyph, label, hue) with ASCII glyphs `/ secured`, `o live`, `x dead` and hues blue `#3B82F6` (advanced+live) / amber `#F59E0B` (eliminated). Add `status_badge_html(state) -> str` rendering `<span style="color:{hue}">{glyph} {label}</span>` — color ALWAYS paired with glyph+label (UI-06; ~8% of men fail color alone, so glyph/label is the real signal). Glyphs MUST be ASCII (`o`/`x`/`/`), NEVER `●`/`✓` (Phase 1 Windows cp1252 lesson, CLAUDE.md). Add a `hero_number_html(pct) -> str` that renders a display-size (28px) monospace number in the accent color `#7C5CFC` (the one hero number per UI-SPEC Typography; accent reserved per UI-SPEC Color). All inputs numeric/enum — no user free-text into HTML (T-02-XSS).
    Flip test_status_palette_never_red_green and a status_badge_html/hero unit test to GREEN in tests/test_ui_helpers.py.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_ui_helpers.py</automated>
  </verify>
  <done>STATUS has the three colorblind-safe states with ASCII glyphs; status_badge_html pairs glyph+label+hue; no hue is red/green; hero_number_html renders accent monospace; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Two-mode toggle + mode-conditional main-column reorder + CI bars on every cell</name>
  <read_first>
    @app.py
    @ui/render.py
    @ui/state.py
    @.planning/phases/02-streamlit-ui-shell/02-UI-SPEC.md
    @.planning/phases/02-streamlit-ui-shell/02-RESEARCH.md
    @tests/test_app.py
  </read_first>
  <files>app.py, tests/test_app.py</files>
  <behavior>
    - test_two_mode_toggle / test_mode_toggle_reorders: a Pre-stage/Live toggle is present; switching mode changes the order of the main-column sections (assertable via the element tree order or section keys in AppTest).
    - In Pre-stage the per-team probs section precedes the (collapsed) bracket; the ballot-area placeholder is the top section. In Live the locked-pick status placeholder is the top section, then delta-probs area, then bracket.
    - Every probability cell still renders number + inline CI bar from plan 01 (UI-04 regression stays green).
    - The shipped-default zero-config Run (plan 01) still works after the reorder (no regression).
  </behavior>
  <action>
    Extend `app.py`: add `st.segmented_control` at the top of the main area for Pre-stage / Live (UI-SPEC Open-Items #2 default; `st.radio(horizontal=True)` is the sanctioned fallback if segmented_control misbehaves under AppTest), bound to `ui.state.Mode`. Mark the active segment with the accent (Streamlit applies primaryColor automatically). Per UI-SPEC Layout/IA, conditionally order the main column by mode:
    - PRE-STAGE: (1) Recommended-ballot placeholder area with the UI-SPEC empty copy `Run to see the recommended ballot.` and a hero-number slot (Phase 3 fills the P(≥5) content — render the shell + the hero_number_html slot now), (2) per-team probs table (the plan-01 render, each cell = number + ci_bar_html), (3) bracket in a collapsed `st.expander` showing the seeded R1 `(seed, seed+8)` table only (UI-SPEC bracket empty state; record-bucket substance is Phase 4).
    - LIVE: (1) locked-pick status placeholder with the UI-SPEC empty copy `Lock a result to go live.` and the P(≥5)-from-here hero slot (Phase 4 fills), using status_badge_html for the o-live/x-dead/-secured legend, (2) delta-probs area (reuse the probs render; delta content is Phase 4), (3) bracket expander.
    Keep ballot/live-status as STRUCTURED PLACEHOLDERS wired to real data later — do NOT build the optimizer (Phase 3) or live re-sim/locking (Phase 4); this plan is UI-01..07/DX-01..05 scope only. Ensure every probability cell uses ci_bar_html so the CI bar is ALWAYS visible (UI-04 — never behind hover/expand). Numbers render monospace (codeFont from config + the monospace CI-bar/number HTML).
    Flip test_two_mode_toggle / test_mode_toggle_reorders to GREEN; keep the plan-01 zero-config and CI-bar-presence tests green (no regression).
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_app.py</automated>
  </verify>
  <done>The Pre-stage/Live toggle is present and reorders the main column per UI-SPEC; per-team probs render number + always-visible inline CI bar; ballot/live-status/bracket are structured placeholders with the correct empty-state copy; plan-01 zero-config Run and CI-bar tests still green; full suite `uv run pytest -q` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app.py → st.markdown(unsafe_allow_html=True) | Status badge + CI-bar + hero HTML rendered in the browser |
| user (segmented_control) → app.py | Mode selection drives layout (no engine path) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-XSS | Tampering | status_badge_html / ci_bar_html / hero_number_html via unsafe_allow_html | mitigate | All rendered HTML is built from numeric values + a fixed enum of ASCII glyph/label/hue constants; NO team name or user free-text is interpolated into markup (RESEARCH Security Domain) |
| T-02-MODE | Tampering | segmented_control mode value | accept | Mode only reorders layout; it touches no engine call and no file/network; invalid values cannot occur (single-select widget) |
</threat_model>

<verification>
- `uv run pytest -q tests/test_app.py` — two-mode toggle present + reorders; plan-01 zero-config + CI-bar regression green.
- `uv run pytest -q tests/test_ui_helpers.py` — status palette never red/green; glyph+label pairing; hero renderer green.
- `uv run pytest -q` — full suite green (engine unregressed).
- Manual (VALIDATION Manual-Only): CI bars render under every number; blue/amber status + glyph/label actually render and read colorblind-safe; contrast ≥4.5:1; monospace alignment; visual hero-vs-table hierarchy.
</verification>

<success_criteria>
- Pre-stage/Live toggle present; main column reorders by mode per UI-SPEC (UI-01).
- Every probability cell = number + always-visible inline Wilson CI mini-bar (UI-04).
- Monospace numbers; one reserved accent (CTA/hero/active segment); colorblind-safe blue/amber status always paired with ASCII glyph + text label, never red/green (UI-06).
- Ballot / live-status / bracket are structured placeholders with correct empty-state copy (scoped to plans 3/4 to fill).
- No regression of the plan-01 zero-config Run or CI-bar presence.
</success_criteria>

<output>
Create `.planning/phases/02-streamlit-ui-shell/02-02-SUMMARY.md` when done.
</output>
