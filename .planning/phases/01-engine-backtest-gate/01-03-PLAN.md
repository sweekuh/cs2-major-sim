---
phase: 01-engine-backtest-gate
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - engine/montecarlo.py
  - engine/__main__.py
  - tests/test_mc.py
autonomous: true
requirements: [ENG-09, MC-01, MC-02, MC-03, MC-04, MC-05, PROB-03, PROB-05]
must_haves:
  truths:
    - "run_mc_progressive is generator-first: it yields per-chunk partial aggregates (e.g. 20×5k) including a running P(advance), and a separate cached-wrapper-shaped function drains it to the final result"
    - "RNG is SeedSequence(seed).spawn(n_chunks) over a PINNED chunk count, so the same (seed, N) yields identical per-team counts across runs and machines and survives chunking"
    - "MC tallies per-team 3-0 / advance (any 3-W) / 0-3 over N sims (default 100k) and reports per-team P with Wilson confidence bands computed by hand (no scipy)"
    - "MC retains the FULL per-sim record sample (not just marginals) so Phase 3 P(≥5) and Phase 4 re-sim can score against it"
    - "The epistemic outer loop is present as a single-draw no-op (one source); the reported band collapses to the inner Wilson band"
    - "uv run python -m engine prints per-team P(3-0)/P(advance)/P(0-3) with Wilson bands and the invariants hold (ΣP(3-0)≈2, ΣP(0-3)≈2, ΣP(advance)≈8 within ±0.05 at N≥50k)"
  artifacts:
    - path: "engine/montecarlo.py"
      provides: "SeedSequence.spawn RNG, outer-epistemic/inner-aleatoric loop, per-chunk yield, sample retention, Wilson band, cached-wrapper seam"
      exports: ["run_mc_progressive", "wilson"]
      min_lines: 60
    - path: "engine/__main__.py"
      provides: "headless smoke run printing per-team probs + Wilson bands"
  key_links:
    - from: "engine/montecarlo.py"
      to: "engine.swiss.simulate_stage"
      via: "inner loop calls simulate_stage per sim"
      pattern: "simulate_stage"
    - from: "engine/montecarlo.py"
      to: "SeedSequence.spawn"
      via: "pinned n_chunks child generators"
      pattern: "SeedSequence|spawn"
---

<objective>
Complete the headless end-to-end Walking Skeleton: engine/montecarlo.py runs N stage simulations over
the proven swiss engine and produces per-team P(3-0)/P(advance)/P(0-3) with Wilson bands, and
engine/__main__.py prints them — the CLI "one real interaction" that proves the full pipeline (fixtures
→ swiss → MC → probs → output) runs locally via `uv run python -m engine`.

The MC is built generator-first (yields per-chunk partials for the Phase 2 progress bar), uses
SeedSequence.spawn over a pinned chunk count for chunk-safe reproducibility (the Phase 1×2 seam), retains
the full per-sim record sample (the Phase 3 seam), and structures the epistemic loop as an outer
single-draw no-op (the Phase 5 seam). After this plan, the MC tests flip from xfail to green.

Purpose: Deliver the trustworthy probability output the whole product is about, on a reproducible chunked
path the shipped app will reuse, with all three cross-phase seams structured correctly now (retrofitting
any of them is a rewrite of the hottest path).
Output: engine/montecarlo.py, engine/__main__.py, implemented tests/test_mc.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-engine-backtest-gate/01-RESEARCH.md
@.planning/phases/01-engine-backtest-gate/01-VALIDATION.md
@files/HANDOFF_claude_code.md
@CLAUDE.md
@.planning/phases/01-engine-backtest-gate/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: run_mc_progressive — SeedSequence.spawn, per-chunk yield, sample retention, Wilson, epistemic stub</name>
  <files>engine/montecarlo.py, tests/test_mc.py</files>
  <behavior>
    - test_reproducibility (ENG-09/MC-05): same (seed, N) → identical per-team 3-0/advance/0-3 counts across two runs; result is invariant to re-running with the SAME pinned n_chunks (SeedSequence.spawn, Pitfall 10)
    - test_record_sample_retained (MC-04): after a run the full per-sim record matrix (one record vector per sim) is retained, length == N, not just marginals
    - test_invariants (MC-01/03): at N≥50k, ΣP(3-0)≈2, ΣP(0-3)≈2, ΣP(advance)≈8 within ±0.05; exactly 2 finish 3-0 and 2 finish 0-3 each sim; 8 advance
    - test_epistemic_band_collapses_to_wilson (PROB-03/05): single source → epistemic spread is zero → reported band equals the inner Wilson band
    - Wilson band (MC-02): hand-coded Wilson score interval (no scipy); wilson(k, n) matches the closed form
  </behavior>
  <read_first>
    - .planning/phases/01-engine-backtest-gate/01-RESEARCH.md "Code Examples" (SeedSequence.spawn over pinned chunks, Wilson score interval) and Patterns 1/2/3 (functional core, epistemic outer/aleatoric inner stub, generator-first two-function split)
    - files/HANDOFF_claude_code.md §6 (invariants, determinism/backtest) and §10.5 "Loading = progress bar + live tally" (per-chunk partial aggregates)
    - CLAUDE.md "Non-negotiable build rules" (SeedSequence.spawn pinned chunks; epistemic outer loop; avoid pandas/scipy)
  </read_first>
  <action>
    Implement engine/montecarlo.py functional-core / generator-first (Pattern 1/3): no streamlit/httpx
    import; RNG injected via a seed param. Build run_mc_progressive(teams, ratings, S, N, locked, *, seed,
    n_chunks=20) as a generator:
    - ss = np.random.SeedSequence(seed); child_seeds = ss.spawn(n_chunks); one default_rng per chunk. PIN
      n_chunks (default 20) so reproducibility depends only on (seed, n_chunks), both controlled — the
      backtest and the shipped app run the SAME chunked path (ENG-09, Pitfall 10). Split N across the
      pinned chunks deterministically.
    - OUTER epistemic loop (PROB-03): wrap the chunk loop in `for p_vec in epistemic_draws(...)`; in
      Phase 1 epistemic_draws yields exactly ONE point draw (single-source no-op). Structure it outer now
      so Phase 5 fills K Beta draws without a rewrite.
    - INNER aleatoric loop: for each chunk, for each sim, call engine.swiss.simulate_stage(...) with that
      chunk's rng; accumulate per-team 3-0/advance(any 3-W)/0-3 tallies (MC-01) AND append the per-sim
      record vector to a retained sample matrix (MC-04). After each chunk, yield a Partial carrying
      (done, total, running P(advance)) for the Phase 2 progress bar (MC-05).
    - Provide a final aggregation that computes per-team P(3-0)/P(advance)/P(0-3) with hand-coded Wilson
      bands (MC-02; wilson(k, n, z=1.96), no scipy). Aggregate across epistemic draws → reported band =
      epistemic spread ⊇ Wilson; with one source it collapses to Wilson only (PROB-05).
    - Shape a cached-wrapper seam: a separate function (e.g. run_mc that drains the generator) that Phase
      2 will decorate with @st.cache_data — do NOT import streamlit here; just keep the two-function split.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_mc.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Same (seed, N) → identical per-team counts across runs (SeedSequence.spawn, pinned n_chunks)
    - Full per-sim record sample retained (length N); invariants hold ±0.05 at N≥50k
    - Wilson band hand-coded (no scipy); epistemic outer loop is a single-draw no-op collapsing band to Wilson
    - run_mc_progressive yields per-chunk Partial(done,total,running_p_adv); two-function cache seam present; no streamlit import
  </acceptance_criteria>
  <done>Generator-first reproducible MC with sample retention, Wilson bands, and all three cross-phase seams structured (ENG-09, MC-01..05, PROB-03/05).</done>
</task>

<task type="auto">
  <name>Task 2: engine/__main__.py — headless smoke run</name>
  <files>engine/__main__.py</files>
  <read_first>
    - files/HANDOFF_claude_code.md §2 build-order item 1 ("a __main__ smoke test prints per-team probs"; thread a seed) and §6 "Sanity (eyeball)" (GamerLegion highest P(3-0)/advance; THUNDER dOWNUNDER/FlyQuest highest P(0-3))
    - .planning/phases/01-engine-backtest-gate/01-RESEARCH.md System Architecture Diagram (per-team P + Wilson printed by __main__)
  </read_first>
  <action>
    Implement engine/__main__.py so `uv run python -m engine` loads the default teams/ratings from
    engine.teams, drains run_mc_progressive at a default N (use a fast default like 50k for the smoke run
    with a fixed seed for reproducibility; allow N override via argv), and prints a per-team table of
    P(3-0)/P(advance)/P(0-3) each with its Wilson band, plus the invariant sums (ΣP(3-0), ΣP(0-3),
    ΣP(advance)). This is the Walking Skeleton's "one real interaction" and the documented full-stack
    local run. Keep it dependency-light (no streamlit/httpx). Eyeball sanity: GamerLegion should top
    P(3-0)/P(advance); THUNDER dOWNUNDER / FlyQuest should top P(0-3).
  </action>
  <verify>
    <automated>uv run python -m engine 5000</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -m engine` prints a 16-row per-team table with P(3-0)/P(advance)/P(0-3) + Wilson bands
    - Printed invariant sums satisfy ΣP(3-0)≈2, ΣP(0-3)≈2, ΣP(advance)≈8 (within tolerance at the smoke N)
    - Run is reproducible for a fixed seed; no streamlit/httpx import
  </acceptance_criteria>
  <done>Headless end-to-end smoke run prints reproducible per-team probabilities with Wilson bands (the Walking Skeleton interaction).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed/N/argv → MC | seed, N, n_chunks are caller/CLI-supplied; no network/auth/DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering (integrity) | chunk-count drift breaking "reproducible" backtest | mitigate | PIN n_chunks; reproducibility depends only on (seed, n_chunks); the backtest runs the SAME chunked path the app ships (Pitfall 10) — this is the real Phase 1 integrity threat |
| T-03-02 | Denial of service | argv N parsing (negative/huge/NaN) | mitigate | Validate N is a positive int; cap or reject absurd values in __main__; statistical RNG only, never for secrets |
</threat_model>

<verification>
- `uv run pytest -q tests/test_mc.py` — reproducibility, invariants, sample retention, epistemic-collapse all green
- `uv run python -m engine 5000` — prints per-team table + Wilson bands + invariant sums; reproducible
- `uv run pytest -q` — math + swiss + MC all green; only the backtest gate test remains xfail (wave 4)
</verification>

<success_criteria>
- SeedSequence.spawn over pinned chunks → reproducible across runs/machines (ENG-09); backtest will reuse this path
- Per-team P(3-0)/P(advance)/P(0-3) + Wilson bands; invariants ±0.05 at N≥50k (MC-01/02/03)
- Full per-sim sample retained (MC-04); generator-first two-function seam (MC-05); epistemic outer no-op (PROB-03/05)
- `uv run python -m engine` is the headless Walking Skeleton run
</success_criteria>

<output>
Create `.planning/phases/01-engine-backtest-gate/01-03-SUMMARY.md` when done.
</output>
