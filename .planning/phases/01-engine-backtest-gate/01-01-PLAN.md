---
phase: 01-engine-backtest-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .gitignore
  - engine/__init__.py
  - engine/teams.py
  - engine/probs.py
  - data/stage1.json
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_probs.py
  - tests/test_swiss.py
  - tests/test_mc.py
  - tests/test_backtest_budapest_2025.py
autonomous: true
requirements: [ENG-01, ENG-03, PROB-01, PROB-02, PROB-03, PROB-04, PROB-05, MC-06, GATE-02]
must_haves:
  truths:
    - "uv run pytest collects the suite and runs the math guards green; correctness/engine/gate tests are present and RED (xfail-marked) pending later waves"
    - "engine/probs.py p_map is logistic with S default 40; series() applies p²(3−2p) for Bo3 and identity for Bo1; an odds-override series prob passes through untouched (Bo3 NOT re-applied)"
    - "difficulty() on Valve's worked example (opponents 2-0 and 1-1) returns 2"
    - "engine/teams.py exposes the 16-team Cologne seed→team map and derives R1 pairings as [(i,i+8) for i in range(1,9)] with no second hardcoded copy"
    - "Beta moment-fit helper clamps variance < p(1−p); epistemic outer-loop helper yields exactly one point draw in Phase 1 (single-source no-op); reported band collapses to the inner Wilson band for a single source"
    - "engine/ imports neither streamlit, httpx, nor requests"
  artifacts:
    - path: "pyproject.toml"
      provides: "uv-managed project, requires-python >=3.12, numpy + pytest pins, [tool.pytest.ini_options] testpaths"
      contains: "requires-python"
    - path: "engine/teams.py"
      provides: "16-team seed→team map, R1 derivation, default ratings, Stage config"
      contains: "range(1, 9)"
    - path: "engine/probs.py"
      provides: "p_map logistic, Bo3 closed form, odds-override passthrough, Beta-clamp + epistemic-stub helpers"
      exports: ["p_map", "series"]
    - path: "data/stage1.json"
      provides: "editable [INFERRED] Cologne seeds + default ratings fixture"
      contains: "GamerLegion"
    - path: "tests/conftest.py"
      provides: "shared fixtures (teams, seeds) for the whole suite"
    - path: "tests/test_probs.py"
      provides: "test_difficulty_formula, test_p_map, test_bo3 math guards"
      contains: "test_difficulty_formula"
  key_links:
    - from: "engine/teams.py"
      to: "R1 pairing"
      via: "derived list comprehension"
      pattern: "i \\+ 8"
    - from: "tests/test_probs.py"
      to: "engine.probs"
      via: "import"
      pattern: "from engine.probs import"
---

<objective>
Lay the bottom of the headless pipeline: a `uv`-bootstrapped Python >=3.12 project, the pure
`engine/probs.py` math core, the `engine/teams.py` fixture (single seed→team source of truth with
R1 derived), the shipped `data/stage1.json`, and the FULL Wave 0 test scaffold for the entire phase.

This plan is the Walking Skeleton's foundation and the Nyquist Wave 0: it creates every test file the
later plans verify against, so the math guards go green immediately while the engine/MC/gate tests sit
RED (xfail) until their owning waves implement them. After this plan, `uv run pytest` runs and the
math-correctness guards (difficulty formula, p_map, Bo3) pass.

Purpose: Establish the dependency-free compute core's math + fixtures + test harness so every
downstream wave has concrete contracts and a failing-test target. Implements the load-bearing
`Σ(opp.wins − opp.losses)` difficulty formula and the `p²(3−2p)` Bo3 closed form — two facts that, if
wrong, silently corrupt every probability.
Output: pyproject.toml, engine/__init__.py, engine/teams.py, engine/probs.py, data/stage1.json,
tests/ scaffold (conftest + 4 test modules), .gitignore.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-engine-backtest-gate/01-RESEARCH.md
@.planning/phases/01-engine-backtest-gate/01-VALIDATION.md
@files/HANDOFF_claude_code.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the uv project, deps, and full Wave 0 test harness</name>
  <files>pyproject.toml, .gitignore, engine/__init__.py, tests/__init__.py, tests/conftest.py, tests/test_probs.py, tests/test_swiss.py, tests/test_mc.py, tests/test_backtest_budapest_2025.py</files>
  <read_first>
    - files/HANDOFF_claude_code.md §1 (repo layout), §6 (acceptance tests), §10.6 (uv bootstrap)
    - .planning/phases/01-engine-backtest-gate/01-RESEARCH.md "Validation Architecture" (Wave 0 gaps, test→req map) and "Standard Stack" (install + version-verify commands)
    - .planning/phases/01-engine-backtest-gate/01-VALIDATION.md (Per-Task Verification Map, Wave 0 Requirements)
  </read_first>
  <action>
    Initialize a uv-managed project. Create pyproject.toml with [project] requires-python ">=3.12",
    a [tool.pytest.ini_options] block setting testpaths to tests, and dependencies numpy and (dev)
    pytest. Before pinning, run `pip index versions numpy` and `pip index versions pytest` to confirm
    the current 2.4.x / 8.x lines (research tagged these [ASSUMED]); pin the confirmed versions. Add
    deps via `uv add numpy` and `uv add --dev pytest`. Create .gitignore covering __pycache__, .venv,
    *.pyc, and .env. Create empty engine/__init__.py and tests/__init__.py.

    Create the FULL Wave 0 test scaffold so every later wave has a RED target (Nyquist rule). Use
    pytest markers to keep the suite collectable and the math guards green while engine/MC/gate tests
    stay RED: mark not-yet-implementable tests with pytest.mark.xfail(reason="implemented in wave N",
    strict=False) so collection succeeds and the suite is not falsely green.
    - tests/conftest.py: shared fixtures returning the 16-team seed→team map and a minimal record-group
      builder usable by swiss/MC tests (import from engine.teams once it exists).
    - tests/test_probs.py: test_difficulty_formula (Valve worked example == 2, GATE-02/ENG-03),
      test_p_map (logistic; equal ratings → 0.5), test_bo3 (p=0.6 → 0.648, p=0.5 → 0.5; MC-06),
      and test_odds_override_passthrough (an explicit series prob is returned unchanged, Bo3 NOT
      re-applied; PROB-02). These target this plan's code → NOT xfail; must pass.
    - tests/test_swiss.py: stub test_fold_uses_difficulty_rank, test_forced_rematch_pairing,
      test_no_valid_matching_fallback, test_locked_results_honored as xfail (wave 2).
    - tests/test_mc.py: stub test_invariants, test_reproducibility, test_record_sample_retained,
      test_epistemic_band_collapses_to_wilson as xfail (wave 3).
    - tests/test_backtest_budapest_2025.py: stub test_backtest_budapest_2025 as xfail (wave 4 — the gate).
    Reference 01-VALIDATION.md Per-Task Verification Map for the exact requirement→test rows; do not
    invent new automated commands. Write the actual test code in the files (no fenced code in the plan).
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_probs.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -q` collects all four test modules without import/collection errors
    - `uv run pytest -q tests/test_probs.py` runs (will pass after Task 2; collection is green now)
    - pyproject.toml pins requires-python >=3.12 and confirmed numpy/pytest versions; testpaths set
    - engine/, MC, and gate tests are present and marked xfail (suite is not falsely green)
  </acceptance_criteria>
  <done>uv project bootstraps; full test scaffold collects; math guards target real code; non-math tests RED via xfail.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: engine/probs.py — p_map, Bo3 closed form, odds-override passthrough, epistemic + Beta-clamp stubs</name>
  <files>engine/probs.py, tests/test_probs.py</files>
  <behavior>
    - test_p_map: p_map(r, r, S=40) == 0.5; higher-rated team > 0.5; matches 1/(1+10^(-(ra-rb)/S))
    - test_bo3: series(0.6, bo3=True) == 0.648; series(0.5, bo3=True) == 0.5; series(p, bo3=False) == p
    - test_difficulty_formula: difficulty() on opponents 2-0 and 1-1 returns 2 (single canonical definition swiss.py imports in wave 2)
    - test_odds_override_passthrough: when an explicit market series prob is supplied for a matchup, it is used directly and series()/Bo3 is NOT re-applied (PROB-02)
    - test_epistemic_band_collapses_to_wilson (xfail until MC wired, but helpers exist): epistemic_draws yields exactly ONE point draw in Phase 1; beta_moment_fit clamps variance < p*(1-p)
  </behavior>
  <read_first>
    - files/HANDOFF_claude_code.md §5 (Math: p_map, series, odds override, epistemic OUTER loop, Beta clamp)
    - .planning/phases/01-engine-backtest-gate/01-RESEARCH.md "Code Examples" (p_map, series, Wilson) and Pattern 2 (epistemic outer / aleatoric inner stub)
    - CLAUDE.md "Non-negotiable build rules" (Bo3 closed form; epistemic outer loop; clamp var < p(1-p))
  </read_first>
  <action>
    Implement engine/probs.py as pure functions, no state, no streamlit/httpx/requests import.
    - p_map(ra, rb, S=40.0): logistic 1/(1 + 10**(-(ra-rb)/S)) per PROB-01 (S default 40, becomes a
      Phase 2 slider).
    - series(p, bo3): p*p*(3-2*p) when bo3 else p — the MC-06 closed form, used as a SINGLE Bernoulli
      draw by the MC (never sample 3 maps).
    - An odds-override passthrough (PROB-02): a series-prob resolver that, given an optional market
      series prob for THIS exact matchup, returns it directly and does NOT re-apply series()/Bo3;
      otherwise falls back to series(p_map(...), bo3). Stub the market-prob source as None in Phase 1
      (odds arrive Phase 5) but wire the branch now.
    - Epistemic OUTER-loop scaffold (PROB-03): epistemic_draws(blend, var) generator that in Phase 1
      yields exactly ONE point p-vector (single-source no-op); structured so Phase 5 fills K Beta draws.
    - beta_moment_fit(mean, var) helper (PROB-04): clamp var < mean*(1-mean) before computing Beta
      (a, b); present even though unused until Phase 5.
    - Reported-band contract (PROB-05): reported band = epistemic spread ⊇ inner Wilson; with one source
      it collapses to Wilson only. Wilson itself lives in montecarlo.py (wave 3); document the collapse
      contract in the epistemic helper here.
    - difficulty(): implement the pure formula Σ(opp.wins − opp.losses) here (it needs only opp
      wins/losses) so test_difficulty_formula passes in this wave; swiss.py imports this canonical
      definition in wave 2. Use NOT Σ opp.wins (ENG-03, Pitfall 1).
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_probs.py -x</automated>
  </verify>
  <acceptance_criteria>
    - test_p_map, test_bo3, test_difficulty_formula, test_odds_override_passthrough PASS
    - series(0.6, True) == 0.648 and series(0.5, True) == 0.5 exactly
    - difficulty() on opponents 2-0 and 1-1 returns 2 (per GATE-02/ENG-03)
    - epistemic_draws yields exactly one draw in Phase 1; beta_moment_fit clamps var < p(1-p)
    - engine/probs.py imports no streamlit/httpx/requests
  </acceptance_criteria>
  <done>Math core passes all guards; Bo3 closed form and difficulty formula verified; odds-override and epistemic/Beta stubs structured for Phase 5.</done>
</task>

<task type="auto">
  <name>Task 3: engine/teams.py + data/stage1.json — single seed→team map with derived R1</name>
  <files>engine/teams.py, data/stage1.json</files>
  <read_first>
    - files/HANDOFF_claude_code.md §3 (16-team fixture table, seeds, R1 = [(i,i+8) for i in range(1,9)], default ratings, "derive — do not hardcode a second copy")
    - .planning/phases/01-engine-backtest-gate/01-RESEARCH.md Architectural Responsibility Map (teams.py owns fixtures + R1 derivation) and Anti-Pattern 4
  </read_first>
  <action>
    Implement engine/teams.py as the single source of truth for the Cologne Stage-1 fixture. Define a
    per-team record (id, name, seed, rating, wins, losses, opps as a set of ids) and a seed→team map for
    all 16 teams using the HANDOFF §3 [INFERRED] seeds and default ratings (GamerLegion=1/92 …
    FlyQuest=16/40). Derive R1 pairings as the list comprehension over range(1, 9) pairing seed i with
    seed i+8, mapped through the seed→team map — do NOT write a second hardcoded R1 table (ENG-01,
    Anti-Pattern 4). Expose Stage config (16 teams, advance at 3 wins, eliminate at 3 losses). Ship
    data/stage1.json containing the same [INFERRED] seeds + ratings in editable JSON form (the Phase 2
    editor and DX-01 zero-config run consume this; Phase 1 just ships it). On any JSON load path,
    validate that there are 16 unique seeds and 16 teams; raise loudly on mismatch or duplicate seed
    (V5 input-validation, threat T-01-01). teams.py imports no streamlit/httpx/requests.
  </action>
  <verify>
    <automated>uv run python -c "from engine.teams import build_round1_pairs, load_teams; t=load_teams(); p=build_round1_pairs(t); assert len(t)==16 and len(p)==8 and {a.seed for a,_ in p}=={1,2,3,4,5,6,7,8} and all(b.seed==a.seed+8 for a,b in p)"</automated>
  </verify>
  <acceptance_criteria>
    - 16 teams load; R1 yields 8 pairs; every pair is (seed i, seed i+8) derived, not hardcoded twice
    - data/stage1.json exists and round-trips the same seeds/ratings; load validation rejects dup/short seed lists
    - engine/teams.py imports no streamlit/httpx/requests
  </acceptance_criteria>
  <done>Single seed→team source of truth; R1 derived per ENG-01; shipped JSON fixture validated on load.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer JSON → engine | data/stage1.json and ratings are developer-supplied; only untrusted-ish input in Phase 1 (no network, no auth, no multi-user) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | engine/teams.py JSON load (data/stage1.json) | mitigate | Validate 16 unique seeds / 16 teams on load; raise loudly on dup seed, short list, or NaN/out-of-range rating |
| T-01-02 | Information disclosure | numpy statistical RNG | accept | RNG is statistical (numpy), never repurposed for secrets; documented, no secret material in Phase 1 |
| T-01-SC | Tampering | uv add numpy / pytest installs | mitigate | numpy + pytest are canonical PyPI packages (per RESEARCH Package Legitimacy Audit, Approved); verify versions via `pip index versions` before pinning; no [ASSUMED]/[SUS] checkpoint required |
</threat_model>

<verification>
- `uv run pytest -q tests/test_probs.py` — math guards green (difficulty==2, p_map, Bo3 0.648/0.5, odds passthrough)
- `uv run pytest -q` — full suite collects; engine/MC/gate tests RED via xfail, suite not falsely green
- `uv run python -c "import engine.probs, engine.teams"` — engine imports without streamlit/httpx/requests
</verification>

<success_criteria>
- uv project bootstraps on Python >=3.12; numpy/pytest pinned to verified current versions
- Difficulty formula returns 2 on Valve's worked example (ENG-03/GATE-02); Bo3 closed form exact (MC-06)
- R1 derived from the single seed→team map (ENG-01); odds-override passthrough + epistemic/Beta stubs wired (PROB-02..05)
- Full Wave 0 test scaffold present so wave 2/3/4 have RED targets
</success_criteria>

<output>
Create `.planning/phases/01-engine-backtest-gate/01-01-SUMMARY.md` when done.
</output>
