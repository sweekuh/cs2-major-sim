---
phase: 07-stage-2-inter-stage-seeding-chain
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_stage2.py
  - data/stage2.json
autonomous: true
requirements: [STG-02]
tags: [swiss, stage2, invariants, freeze]

must_haves:
  truths:
    - "Selecting Stage 2 runs the FROZEN simulate_stage/run_mc on the Stage-2 fixture and yields per-team P(3-0)/P(advance)/P(0-3) with Sigma P(3-0)~2, Sigma P(0-3)~2, Sigma P(advance)~8 (+/-0.05) — same invariants as Stage 1 (STG-02)."
    - "A Stage-2 bracket is submittable by hand TODAY: data/stage2.json carries 16 editable seeds (invited 1-8 by VRS placeholders + qualifier 9-16 placeholders) with seeds_confirmed=false so the per-stage [INFERRED] banner stays loud."
    - "GATE-01 stays green BY CONSTRUCTION — no engine file is touched; tests/test_backtest_budapest_2025.py and tests/test_teams_load_stage.py::test_load_teams_unchanged both pass."
  artifacts:
    - path: "tests/test_stage2.py"
      provides: "STG-02 Sigma=2/8/2 invariant on the Stage-2 fixture, loaded via load_stage"
      contains: "test_stage2_invariants"
    - path: "data/stage2.json"
      provides: "16-team Stage-2 fixture: invited 1-8 (VRS-ordered [INFERRED]) + qualifier 9-16 placeholders, seeds_confirmed=false"
      contains: "\"seeds_confirmed\": false"
  key_links:
    - from: "tests/test_stage2.py"
      to: "engine.teams.load_stage + engine.montecarlo.run_mc"
      via: "load_stage('data/stage2.json') -> run_mc(teams=...)"
      pattern: "load_stage\\(.*stage2"
    - from: "data/stage2.json"
      to: "engine.teams._validate_fixture_for_size"
      via: "load_stage validates 16 unique seeds 1..16 on read"
      pattern: "\"size\": 16"
---

<objective>
Deliver Slice 1 of Phase 7 (STG-02): prove Stage 2 simulates end-to-end through the FROZEN
engine and ship a real, editable, hand-submittable Stage-2 fixture — so a Stage-2 Pick'Em
ballot is producible TODAY (seeds editable by hand) before the auto-derivation chain (Plans
02/03) lands.

This is wiring + data + a test, NOT engine work. Phase 6 already shipped `load_stage`, the
`stage_id`-leading cache key in `run_mc_cached`/`optimize_cached`, and the `"stage2"` selector.
The only new code is (a) a Sigma=2/8/2 invariant test that loads the Stage-2 fixture and runs
`run_mc` (mirroring `test_mc.py::test_invariants`), and (b) replacing the Phase-6 placeholder
`data/stage2.json` with a real-shaped 16-team fixture (8 invited by VRS [INFERRED] + 8 qualifier
placeholders), `seeds_confirmed=false`.

Purpose: STG-02 — "Stage 2 Swiss simulates via the FROZEN engine with Stage-2 teams; per-team
probs pass the same structural invariants (Sigma = 2/8/2) as Stage 1, no engine edit required."
Output: tests/test_stage2.py (new), data/stage2.json (real fixture replacing the placeholder).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/07-stage-2-inter-stage-seeding-chain/07-RESEARCH.md
@.planning/phases/07-stage-2-inter-stage-seeding-chain/07-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the placeholder data/stage2.json with a real-shaped 16-team Stage-2 fixture</name>
  <files>data/stage2.json</files>
  <read_first>
    - data/stage2.json (the Phase-6 placeholder this task replaces — keep stage.size/advance/eliminate, keep seeds_confirmed=false)
    - data/stage1.json (the canonical CONFIRMED-fixture shape + _comment provenance style to mirror)
    - engine/teams.py lines 150-216 (`_validate_fixture_for_size` + `load_stage`: the loud-fail contract this fixture round-trips — exactly 16 unique seeds 1..16, unique names, 0 < rating < 1000)
    - .planning/phases/07-stage-2-inter-stage-seeding-chain/07-PATTERNS.md lines 106-137 (the data/stage2.json pattern: keep size:16, seeds_confirmed:false, replace placeholders, update _comment with derivation/[INFERRED] caveat)
    - .planning/phases/07-stage-2-inter-stage-seeding-chain/07-RESEARCH.md lines 415-421 (Open Question 2 resolution: invited 8 live as the first 8 entries; derivation fills 9-16)
  </read_first>
  <action>
    Rewrite `data/stage2.json` keeping the `stage` block exactly (`size: 16`, `advance_at_wins: 3`,
    `eliminate_at_losses: 3`) and `seeds_confirmed: false`. Replace the 16 `"Stage2 Seed N"`
    placeholder rows with a real-shaped 16-row `teams` list following the VERIFIED Valve merge rule
    (decisions_already_made #1): seeds 1-8 are the 8 INVITED teams in global-VRS rank order; seeds
    9-16 are placeholder qualifier slots (the 8 Stage-1 advancers, filled by hand or by the Plan-02/03
    derivation). Use real invited-team names where known from RESEARCH (held `[INFERRED]` — the
    SPECIFIC Cologne Stage-2 invited 8 + VRS ranks are event-time data per 07-RESEARCH.md Assumption
    A1); if not yet posted, use clearly-marked `[INFERRED]` placeholder invited names. Give each row a
    plausible rating in the engine bound `0 < r < 1000` (the rating-only fallback prior, editable;
    A2). Seeds 9-16 stay placeholder names (e.g. `"Stage-1 Qualifier (seed 9)"`) — these are filled
    by the seeding chain in Plan 03. Update the top `_comment` to: state the derivation source (8
    Stage-1 qualifiers by final Buchholz via the Phase-7 seeding chain + 8 VRS-invited), mirror the
    stage1.json provenance style, and carry the `[INFERRED]` merge-rule caveat + "reconcile vs the
    official Cologne Stage-2 seed list when it posts" note. Do NOT set `seeds_confirmed: true`. Do NOT
    add an `all_bo3` flag (Stage 2 is Bo1-default; all-Bo3 is Stage 3 / Phase 8 per A4). Do NOT touch
    engine/teams.py, engine/swiss.py, or load_teams.
  </action>
  <verify>
    <automated>cd "C:\Users\sweek\Documents\Claude\Projects\cs2 major sim" && python -c "from engine.teams import load_stage; t,c=load_stage('data/stage2.json'); assert len(t)==16; assert [x.seed for x in t]==list(range(1,17)); assert c['size']==16; assert c['seeds_confirmed'] is False; import json; raw=json.load(open('data/stage2.json',encoding='utf-8')); assert 'Stage2 Seed' not in json.dumps(raw); print('stage2 fixture OK: 16 seeds, seeds_confirmed=false, placeholders removed')"</automated>
  </verify>
  <acceptance_criteria>
    - `load_stage('data/stage2.json')` returns 16 Teams with seeds exactly 1..16 and `cfg['size']==16`, `cfg['seeds_confirmed'] is False` (the command above exits 0).
    - The JSON no longer contains the literal string `"Stage2 Seed "` (placeholder rows removed).
    - The `_comment` names the Phase-7 seeding-chain derivation source AND carries the `[INFERRED]` + reconcile-vs-official caveat.
    - The `stage` block is unchanged (`size:16`, `advance_at_wins:3`, `eliminate_at_losses:3`); no `all_bo3` key added.
  </acceptance_criteria>
  <done>data/stage2.json is a real-shaped 16-team fixture (invited 1-8 VRS-ordered [INFERRED] + qualifier 9-16 placeholders), seeds_confirmed=false, validating clean through load_stage.</done>
</task>

<task type="auto">
  <name>Task 2: Add the STG-02 Sigma=2/8/2 invariant test on the Stage-2 fixture (no engine edit)</name>
  <files>tests/test_stage2.py</files>
  <read_first>
    - tests/test_mc.py lines 71-82 (`test_invariants`: the exact Sigma=2/8/2 marginal check to mirror — only change is loading the Stage-2 fixture instead of load_teams())
    - tests/test_mc.py lines 54-68 (`test_record_sample_retained`: the per-sim "exactly 2x(3,0), 2x(0,3), 8 with wins==3" loop — copy for the stronger per-sim invariant)
    - tests/test_teams_load_stage.py lines 45-59 (the precedent for calling `load_stage` in a test and unpacking `(teams, cfg)`)
    - engine/montecarlo.py lines 338-375 (`run_mc` signature: `run_mc(teams, ratings, S, N=..., locked=None, *, seed, ...)` returns `Result` with `.counts_30/.counts_advance/.counts_03`)
    - .planning/phases/07-stage-2-inter-stage-seeding-chain/07-PATTERNS.md lines 179-205 (the test_stage2 pattern + pinned references)
  </read_first>
  <action>
    Create `tests/test_stage2.py` with module header conventions mirroring `tests/test_mc.py` lines
    1-15 (`from __future__ import annotations`, docstring naming STG-02, `import` the unit under test +
    `engine.teams.load_stage`). Add `test_stage2_invariants`: load the Stage-2 teams via
    `teams, _cfg = load_stage("data/stage2.json")`, run `r = run_mc(teams=teams, ratings=None, S=40.0,
    N=50000, locked={}, seed=2024)`, then assert `abs(sum(c/N for c in r.counts_30.values()) - 2.0)
    <= 0.05`, the same for `r.counts_03` vs 2.0, and `r.counts_advance` vs 8.0 — byte-for-byte the
    `test_mc.py::test_invariants` assertions, only the fixture source changed. Add a second test
    `test_stage2_per_sim_degenerate` that runs a small `N=500` `run_mc` and copies the
    `test_record_sample_retained` per-sim loop verbatim (each sim in `r.sample`: exactly 2 with
    `w==3 and l==0`, 2 with `l==3 and w==0`, 8 with `w==3`) against the Stage-2 Result — proving the
    FROZEN engine runs Stage-2 teams with the same structural guarantees. Reference STG-02 in the
    docstrings. Do NOT edit any engine file; this test is pure black-box assertion against the frozen
    `simulate_stage`/`run_mc` path (the whole point of STG-02: "different teams only").
  </action>
  <verify>
    <automated>cd "C:\Users\sweek\Documents\Claude\Projects\cs2 major sim" && uv run pytest tests/test_stage2.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_stage2.py` exists and `uv run pytest tests/test_stage2.py -x -q` exits 0.
    - `test_stage2_invariants` loads `data/stage2.json` via `load_stage` (not `load_teams`) and asserts Sigma P(3-0)~2, Sigma P(0-3)~2, Sigma P(advance)~8 within +/-0.05 at N=50000.
    - `test_stage2_per_sim_degenerate` asserts every sim has exactly 2x(3,0), 2x(0,3), 8 with wins==3.
    - The test imports/edits NO engine source (it only calls `load_stage` + `run_mc`).
  </acceptance_criteria>
  <done>tests/test_stage2.py proves Stage 2 passes the same Sigma=2/8/2 + per-sim invariants as Stage 1 through the unedited engine.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the freeze — full suite + GATE-01 + load_teams-unchanged all green</name>
  <files>(verification-only — runs git diff + pytest; touches no source)</files>
  <read_first>
    - tests/test_backtest_budapest_2025.py lines 62-113 (GATE-01: the round-by-round backtest — the cross-cutting exit criterion that MUST stay green)
    - tests/test_teams_load_stage.py lines 131-144 (`test_load_teams_unchanged`: the freeze-regression proof that the frozen Stage-1 path was not perturbed)
    - .planning/phases/07-stage-2-inter-stage-seeding-chain/07-RESEARCH.md lines 483-488 (the GATE-01 stay-green check: Phase 7 touches no engine file, so the freeze holds by construction)
    - .planning/phases/07-stage-2-inter-stage-seeding-chain/07-VALIDATION.md lines 19-31 (full suite + GATE-01 commands; ~154 baseline tests)
  </read_first>
  <action>
    Run the freeze-regression proof for Slice 1. First confirm NO engine file changed in this plan by
    running `git diff --name-only HEAD -- engine/swiss.py engine/montecarlo.py engine/teams.py
    engine/probs.py engine/live.py` and verifying the output is EMPTY (this plan only adds
    tests/test_stage2.py and edits data/stage2.json — a data file + a test, never engine code). Then
    run the GATE-01 backtest (`tests/test_backtest_budapest_2025.py`), the freeze-regression test
    (`tests/test_teams_load_stage.py::test_load_teams_unchanged`), and the full suite — all must pass.
    If the git diff over engine files is non-empty, STOP: an engine edit means the Stage-2
    generalization is wrong (STG-02 / GATE-01 violation) and the change must be reverted out of the
    engine. Do not loosen any assertion to make a test pass.
  </action>
  <verify>
    <automated>cd "C:\Users\sweek\Documents\Claude\Projects\cs2 major sim" && git diff --name-only HEAD -- engine/swiss.py engine/montecarlo.py engine/teams.py engine/probs.py engine/live.py > diff_engine.txt && powershell -Command "if ((Get-Content diff_engine.txt) -and (Get-Content diff_engine.txt).Length -gt 0) { Write-Error 'ENGINE FILE CHANGED - STG-02/GATE-01 violation'; exit 1 } else { Write-Host 'no engine edits' }" && del diff_engine.txt && uv run pytest tests/test_backtest_budapest_2025.py tests/test_teams_load_stage.py::test_load_teams_unchanged tests/test_stage2.py -x -q && uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only HEAD` over engine/swiss.py, engine/montecarlo.py, engine/teams.py, engine/probs.py, engine/live.py is EMPTY (no engine edits in this plan).
    - `uv run pytest tests/test_backtest_budapest_2025.py -q` exits 0 (GATE-01 green).
    - `uv run pytest tests/test_teams_load_stage.py::test_load_teams_unchanged -q` exits 0 (freeze regression green).
    - `uv run pytest -q` exits 0 (full suite, including the new test_stage2.py).
  </acceptance_criteria>
  <done>Slice 1 is freeze-clean: no engine file touched, GATE-01 green, load_teams unchanged, full suite green with the new Stage-2 invariant tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editable Stage-2 fixture (data/stage2.json) -> engine | hand-entered / [INFERRED] seeds + ratings cross into the pairing/sim math; a malformed fixture silently corrupts every Stage-2 pairing |
| frozen engine path (GATE-01) <-> Stage-2 generalization | Stage 2 must reuse simulate_stage/run_mc byte-unchanged; any engine edit to "support Stage 2" breaks the backtest |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | data/stage2.json (derived/edited seeds) | mitigate | `load_stage` -> `_validate_fixture_for_size` loud-fails on !=16 seeds / dup seed / out-of-range rating (Task 1 verify asserts the clean round-trip); seeds_confirmed=false keeps the [INFERRED] banner loud (V5 Input Validation) |
| T-07-02 | Tampering | engine freeze (GATE-01) | mitigate | Task 3 asserts an EMPTY `git diff` over all engine files + GATE-01 + `test_load_teams_unchanged` green; an engine edit fails the plan rather than silently corrupting the proven pairing logic |
| T-07-03 | Information disclosure (false precision) | unconfirmed Stage-2 seeds presented as authoritative | accept | seeds_confirmed=false -> the shipped per-stage [INFERRED] banner (STG-05, Phase 6) already surfaces "seeds inferred, verify vs official"; reconcile at event time (A1) — no new surface in this plan |

No network, no auth, no new endpoints, no package installs in this plan — Stage 2 runs entirely
through the existing frozen engine + Phase-6 scaffold. No high-severity threats.
</threat_model>

<verification>
- `uv run pytest tests/test_stage2.py -x -q` — STG-02 Sigma=2/8/2 + per-sim invariants on the Stage-2 fixture.
- `uv run pytest tests/test_backtest_budapest_2025.py -q` — GATE-01 green (cross-cutting exit criterion).
- `uv run pytest tests/test_teams_load_stage.py::test_load_teams_unchanged -q` — freeze regression green.
- `git diff --name-only HEAD` over engine/* empty — no engine edit (STG-02 "different teams only").
- `uv run pytest -q` — full suite green.
</verification>

<success_criteria>
- data/stage2.json is a real-shaped 16-team Stage-2 fixture (invited 1-8 VRS-ordered [INFERRED] + qualifier 9-16 placeholders), seeds_confirmed=false, validating clean through load_stage.
- tests/test_stage2.py proves Stage 2 passes Sigma=2/8/2 and the per-sim degenerate invariant through the UNEDITED engine.
- A Stage-2 Pick'Em bracket is hand-submittable today (editable seeds via the shipped ratings editor + per-stage banner).
- GATE-01 green, load_teams unchanged, no engine file touched, full suite green.
</success_criteria>

<output>
Create `.planning/phases/07-stage-2-inter-stage-seeding-chain/07-01-SUMMARY.md` when done.
</output>
