---
phase: 07-stage-2-inter-stage-seeding-chain
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - engine/seeding.py
  - tests/test_seeding.py
autonomous: true
requirements: [SEED-01, SEED-02]
tags: [seeding, buchholz, determinism, pure-function]

must_haves:
  truths:
    - "engine.seeding.seed_next_stage derives the ordered seed-1..16 Stage-2 Team list from a finished Stage-N {id: Team} replay + the invited list: invited take seeds 1-8 by VRS rank, qualifiers take seeds 9-16 by the literal Valve final-standings chain (losses ascending, -difficulty, prior_seed) — NOT interleaved (SEED-01)."
    - "Buchholz comes from the canonical engine.probs.difficulty (Sigma(opp.wins-opp.losses)) — there is NO second Buchholz copy in seeding.py (ENG-03 trap avoided)."
    - "Identical prior-stage finals + invited list yield BYTE-IDENTICAL seeds across two independent seed_next_stage calls — the two-run determinism gate (SEED-02) passes; the function is pure (no file I/O, no streamlit/httpx import, no RNG affecting output)."
    - "GATE-01 stays green — seeding.py is a NEW sibling that imports engine.probs.difficulty + engine.teams.Team and edits no engine file."
  artifacts:
    - path: "engine/seeding.py"
      provides: "Pure seed_next_stage(prior_finals, invited) -> list[Team] with a documented tiebreak chain"
      exports: ["seed_next_stage"]
      contains: "from engine.probs import difficulty"
      min_lines: 40
    - path: "tests/test_seeding.py"
      provides: "SEED-02 two-run determinism gate + hand-worked tie + purity/canonical-Buchholz source guards + SEED-01 merge-rule test"
      contains: "test_seed_derivation_is_deterministic"
  key_links:
    - from: "engine/seeding.py"
      to: "engine.probs.difficulty"
      via: "from engine.probs import difficulty (canonical Buchholz, no second copy)"
      pattern: "from engine\\.probs import difficulty"
    - from: "engine/seeding.py"
      to: "engine.teams.Team + ADVANCE_AT_WINS"
      via: "qualifier filter t.wins >= ADVANCE_AT_WINS; output is list[Team]"
      pattern: "ADVANCE_AT_WINS"
    - from: "tests/test_seeding.py"
      to: "engine.seeding.seed_next_stage"
      via: "two-run determinism + tie + merge assertions"
      pattern: "seed_next_stage"
---

<objective>
Build the correctness heart of Phase 7 (SEED-01/SEED-02): the pure, file-I/O-free, RNG-free
`engine/seeding.py` that derives Stage N+1 seeds from Stage N's FINAL Buchholz, and lock it with
`tests/test_seeding.py` — the SEED-02 two-run determinism gate written TEST-FIRST (RED before the
implementation exists). This module is the single derivation path the Plan-03 wiring consumes.

The VERIFIED Valve merge rule (decisions #1): invited teams take seeds 1-8 by global VRS rank;
the 8 advancing qualifiers take seeds 9-16 by their FINAL prior-stage seed. NOT interleaved. The
qualifier final-seed tiebreak chain (decisions #2, the literal rulebook reading resolving Open
Question 1): `(losses_ascending, -difficulty, prior_seed)` where `difficulty` is the canonical
`engine.probs.difficulty` (Buchholz Sigma(opp.w - opp.l)) — NEVER a second copy (ENG-03 trap).
Determinism (SEED-02) is THE gate: identical inputs -> byte-identical seeds, which requires an
explicit total tiebreak key and zero reliance on dict/set iteration order.

This is a TDD plan: write the determinism gate + tie + merge tests RED first, then implement
`seed_next_stage` minimally to GREEN, then refactor. GATE-01 stays green by construction — this
is a NEW sibling module that edits nothing.

Purpose: SEED-01 (the Valve merge + canonical Buchholz) + SEED-02 (the two-run determinism gate).
Output: engine/seeding.py (new pure module), tests/test_seeding.py (new — the correctness gate).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/07-stage-2-inter-stage-seeding-chain/07-RESEARCH.md
@.planning/phases/07-stage-2-inter-stage-seeding-chain/07-PATTERNS.md
</context>

<feature>
  <name>Pure inter-stage seed derivation (engine/seeding.py::seed_next_stage)</name>
  <files>engine/seeding.py, tests/test_seeding.py</files>
  <read_first>
    - engine/probs.py lines 1-21 (the functional-core purity header to mirror) + lines 64-71 (`difficulty(t)` canonical Buchholz `sum(o.wins - o.losses for o in t.opps)` — the ONE definition to import; it needs each team's `.opps` populated with opponent OBJECTS)
    - engine/swiss.py lines 37-40 (the `from engine.probs import difficulty` import precedent — seeding.py is the third consumer, NEVER a second copy) + lines 75-85 (`rank_group`: the precedent for a DOCUMENTED single-path tiebreak key `(-difficulty(t), t.seed)` written as one sorted with a naming comment)
    - engine/teams.py lines 50-72 (the `Team` dataclass: id/name/seed/rating + mutable wins/losses/opps, `eq=False` identity hashing) + lines 20-23 (ADVANCE_AT_WINS/ELIMINATE_AT_LOSSES constants)
    - tests/test_mc.py lines 1-15 (test header conventions), lines 31-42 (`test_reproducibility`: the two-run determinism SHAPE to mirror for the SEED-02 gate), lines 114-121 (`test_no_forbidden_imports`: the source-guard purity test to mirror)
    - tests/test_backtest_budapest_2025.py lines 47-94 (`_load_fixture`/`_seeding`/`_build_teams` + the `name_to_seed` + `locked[frozenset((w, ell))] = w` idiom to build a finished full-lock Stage-1 from tests/fixtures/budapest_2025_stage1.json; the `simulate_stage(teams, None, 40.0, default_rng(0), locked, pairings_out=[])` full-lock replay returns `{id: Team}` with `.opps` populated)
    - engine/live.py lines 247-266 (`derive_bracket`: the throwaway-rng full-lock replay idiom + comment to mirror for the `_build_finished_stage1_replay` test helper) + lines 236-244 (`_fresh_teams`)
    - .planning/phases/07-stage-2-inter-stage-seeding-chain/07-RESEARCH.md lines 154-218 (Pattern 1: the seed_next_stage shape + the id-vs-seed subtlety) + lines 322-361 (the SEED-01/02 test code examples) + lines 411-416 (Open Question 1 resolution: the literal `(losses_ascending, -difficulty, prior_seed)` chain — decisions #2)
    - .planning/phases/07-stage-2-inter-stage-seeding-chain/07-PATTERNS.md lines 24-102 (the seeding.py purity header + canonical-Buchholz import + tiebreak-chain precedents) + lines 141-175 (the test_seeding.py composed patterns)
  </read_first>
  <behavior>
    Cases (input -> expected output), written as tests BEFORE the implementation:
    - Two independent `seed_next_stage(prior_finals, invited)` calls on the SAME fixed input -> byte-identical `[(seed, name, rating), ...]` lists, and the seed sequence is exactly `1..16` (SEED-02 gate).
    - Invited 8 occupy seeds 1-8 (by VRS rank ascending); the 8 qualifiers occupy seeds 9-16; no invited name appears in 9-16 and no qualifier in 1-8 (SEED-01 merge, NOT interleaved).
    - Two qualifiers with the SAME losses AND equal `difficulty()` but prior Stage-1 seeds 3 and 7 -> the prior-seed-3 team gets the earlier (lower) Stage-2 seed (the `(losses, -difficulty, prior_seed)` final discriminator resolves the tie deterministically).
    - A qualifier set where a 3-0 advancer has LOWER Buchholz than a 3-2 advancer -> the 3-0 team still seeds ABOVE the 3-2 team (losses-ascending dominates difficulty — the literal rulebook chain, Open Question 1 resolution).
    - `seeding.py` source contains `from engine.probs import difficulty` and contains NO inline Buchholz (`o.wins - o.losses` / `opp.wins - opp.losses` absent) — canonical, no second copy (ENG-03).
    - `seeding.py` source imports no streamlit/httpx/requests and has no `np.random`/`default_rng` call affecting output (purity guard for SEED-02).
    - A complete 16-team finished replay yields exactly 8 qualifiers (`t.wins >= ADVANCE_AT_WINS`); a derived list of length != 16 is impossible (asserted).
  </behavior>
  <implementation>
    RED -> GREEN -> REFACTOR, three atomic commits.

    RED — write tests/test_seeding.py FIRST (it must fail because engine/seeding.py does not yet
    exist). Module header mirrors tests/test_mc.py lines 1-15 (`from __future__ import annotations`,
    docstring naming SEED-01/SEED-02, `import numpy as np`). Provide a shared deterministic helper
    `_build_finished_stage1_replay()` that returns a `{id: Team}` map with `.opps` populated by
    replaying a fully-locked stage: build the locked dict from tests/fixtures/budapest_2025_stage1.json
    using the `name_to_seed` + `locked[frozenset((w, ell))] = w` idiom (tests/test_backtest_budapest_2025.py
    lines 80-89), build fresh Teams (`Team(id=s, name=name, seed=s, rating=50.0)`), then
    `simulate_stage(teams, None, 40.0, np.random.default_rng(0), locked, pairings_out=[])` — the
    SAME full-lock replay trick engine/live.py::derive_bracket uses (lines 247-266); the returned
    `{id: Team}` has final `.wins/.losses` and populated `.opps` for `difficulty()`. Provide a fixed
    `_fixed_invited_8()` returning the 8 invited descriptors (name + vrs_rank + rating). Write:
    `test_seed_derivation_is_deterministic` (the gate — two calls, assert the `[(seed,name,rating)]`
    lists equal AND seeds == `list(range(1,17))`); `test_invited_take_seeds_1_8_qualifiers_9_16`
    (SEED-01 merge); `test_qualifier_tie_breaks_on_prior_seed` (the hand-worked equal-difficulty tie ->
    prior_seed discriminator); `test_loss_bucket_outranks_buchholz` (a 3-0 with lower Buchholz seeds
    above a 3-2 — the literal chain); `test_buchholz_uses_canonical_difficulty` (source guard:
    `"from engine.probs import difficulty" in src` AND `"o.wins - o.losses" not in src` AND
    `"opp.wins - opp.losses" not in src`); `test_seeding_is_pure` (source guard: no `import streamlit`,
    `import httpx`, `import requests`, and no `default_rng`/`np.random` in the seeding source — mirror
    tests/test_mc.py::test_no_forbidden_imports lines 114-121). Run the file and CONFIRM it fails
    (ImportError / no module). Commit: `test(07-02): add SEED-02 determinism gate + SEED-01 merge/tie tests (RED)`.

    GREEN — create engine/seeding.py as a pure module. Header mirrors engine/probs.py lines 1-21
    (the "No state, no I/O, no streamlit/httpx/requests; referentially transparent so the gate can
    verify in isolation" discipline) and names the load-bearing facts (canonical Buchholz via
    engine.probs.difficulty; pure/RNG-free -> byte-identical across two runs, SEED-02). Imports:
    `from __future__ import annotations`, `from engine.probs import difficulty` (the canonical import
    line, mirroring engine/swiss.py lines 37-40 — NO second copy), `from engine.teams import
    ADVANCE_AT_WINS, Team`. Define `seed_next_stage(prior_finals: dict[int, Team], invited: list[...])
    -> list[Team]`: (1) `qualifiers = [t for t in prior_finals.values() if t.wins >= ADVANCE_AT_WINS]`;
    assert `len(qualifiers) == 8` (fail loud on a non-complete replay — Pitfall 5). (2) Rank qualifiers
    by the DOCUMENTED literal chain `sorted(qualifiers, key=lambda t: (t.losses, -difficulty(t),
    t.seed))` (losses ascending = a 3-0 outseeds a 3-2; then Difficulty Score descending; then the
    prior Stage-1 initial seed ascending) — write the key as ONE sorted with a comment naming every
    level, mirroring engine/swiss.py::rank_group lines 75-85. (3) Rank invited by
    `sorted(invited, key=lambda iv: (iv.vrs_rank, iv.name))` (VRS ascending, name fallback for a total
    order). (4) Build the output list: seeds 1-8 = invited in VRS order as fresh `Team(id=i, name=...,
    seed=i, rating=...)`; seeds 9-16 = qualifiers in ranked order as `Team(id=9+offset, name=t.name,
    seed=9+offset, rating=t.rating)`. Note the id-vs-seed distinction (07-RESEARCH.md lines 216-218):
    the OUTPUT Team's `.seed` is its Stage-2 list position; the qualifier's `t.seed` used in the
    tiebreak key is its Stage-1 initial seed — keep these distinct. Provide an `InvitedTeam`
    descriptor (a `@dataclass(frozen=True)` with `name: str`, `vrs_rank: int`, `rating: float`) OR
    accept any object exposing `.name/.vrs_rank/.rating` — document the chosen shape. Do NOT import
    streamlit/httpx/json/pathlib; do NOT call `np.random` or `default_rng` anywhere in this module
    (accept the already-replayed `{id: Team}` as an argument so numpy stays OUT of seeding.py — the
    cleaner purity claim per 07-PATTERNS.md line 79). Run the tests to GREEN. Commit:
    `feat(07-02): pure seed_next_stage — Valve merge + canonical Buchholz (GREEN)`.

    REFACTOR (if needed) — tighten the docstring tiebreak comment, ensure the merge-rule docstring
    quotes the Valve rule verbatim (invited 1-8 pre-event seed; advancing 8 -> 9-16 final prior-stage
    seed; NOT interleaved) and marks it `[INFERRED]` + editable until reconciled. Re-run tests GREEN.
    Commit (only if changes made): `refactor(07-02): document the literal Valve tiebreak chain`.
  </implementation>
  <acceptance_criteria>
    - `engine/seeding.py` exists, exports `seed_next_stage`, and `uv run pytest tests/test_seeding.py -x -q` exits 0.
    - The RED commit landed test_seeding.py FAILING (module absent) BEFORE engine/seeding.py existed; the plan produced RED -> GREEN (-> REFACTOR) atomic commits with the prescribed messages.
    - `test_seed_derivation_is_deterministic` asserts byte-identical `[(seed,name,rating)]` across two calls AND seeds == `list(range(1,17))` (the SEED-02 gate).
    - `test_invited_take_seeds_1_8_qualifiers_9_16` (merge, not interleaved), `test_qualifier_tie_breaks_on_prior_seed`, and `test_loss_bucket_outranks_buchholz` (the literal `(losses, -difficulty, prior_seed)` chain) all pass.
    - `test_buchholz_uses_canonical_difficulty` + `test_seeding_is_pure` pass: source contains `from engine.probs import difficulty`, contains no inline `o.wins - o.losses`/`opp.wins - opp.losses`, and imports no streamlit/httpx/requests with no `default_rng`/`np.random`.
    - `git diff --name-only HEAD` over engine/swiss.py, engine/montecarlo.py, engine/teams.py, engine/probs.py, engine/live.py is EMPTY; GATE-01 (`tests/test_backtest_budapest_2025.py`) green.
  </acceptance_criteria>
</feature>

<verify_after_each_commit>
After EVERY commit in this plan run: `cd "C:\Users\sweek\Documents\Claude\Projects\cs2 major sim" && uv run pytest tests/test_seeding.py tests/test_backtest_budapest_2025.py -x -q`
(the slice under test + GATE-01). RED commit: test_seeding.py fails as expected (module absent) but
GATE-01 stays green. GREEN/REFACTOR commits: both green. Never loosen the determinism gate.
</verify_after_each_commit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| derived seed list (seed_next_stage output) -> Stage-2 fixture/sim | a wrong or non-deterministic derivation silently corrupts every Stage-2 pairing and presents false precision |
| canonical Buchholz (engine.probs.difficulty) <-> seeding.py | seeding MUST import the one definition; a second inline Buchholz copy drifts to the Sigma(opp.wins) bug (ENG-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | seed_next_stage ordering (Buchholz tie / dict-set iteration order) | mitigate | explicit TOTAL tiebreak key `(losses, -difficulty(t), t.seed)` + the two-run determinism gate `test_seed_derivation_is_deterministic` asserts byte-identical output (SEED-02); no reliance on iteration order (V5 Input Validation / integrity) |
| T-07-05 | Tampering | Buchholz re-implementation (ENG-03 Sigma-w bug) | mitigate | `test_buchholz_uses_canonical_difficulty` source guard asserts `from engine.probs import difficulty` present AND no inline `o.wins - o.losses`; the tie test would order ties differently under a Sigma-w bug |
| T-07-06 | Tampering | off-by-one qualifier count (!= 8) | mitigate | `seed_next_stage` asserts exactly 8 advancers (`t.wins >= ADVANCE_AT_WINS`); a non-16 derived list is impossible — the SEED-02 gate also asserts seeds == 1..16 |
| T-07-07 | Information disclosure (false precision) | non-deterministic seeds presented as authoritative | mitigate | the two-run determinism gate (SEED-02) is the named correctness gate; downstream the [INFERRED] banner stays loud until reconciled (handled in Plan 03) |
| T-07-08 | Tampering | engine freeze (GATE-01) | mitigate | seeding.py is a NEW sibling importing difficulty + Team, editing nothing; per-commit `tests/test_backtest_budapest_2025.py` stays green |

Pure function — no network, no auth, no new endpoints, no package installs. No high-severity threats.
</threat_model>

<verification>
- `uv run pytest tests/test_seeding.py -x -q` — SEED-02 two-run determinism gate + SEED-01 merge/tie + purity/canonical-Buchholz source guards all green.
- `uv run pytest tests/test_backtest_budapest_2025.py -q` — GATE-01 green (seeding.py edits no engine file).
- `python -c "import engine.seeding as s; src=open(s.__file__,encoding='utf-8').read(); assert 'from engine.probs import difficulty' in src; assert 'o.wins - o.losses' not in src and 'opp.wins - opp.losses' not in src; assert 'default_rng' not in src and 'import streamlit' not in src and 'import httpx' not in src; print('seeding.py pure + canonical Buchholz OK')"` — the purity + canonical-import guards.
- `uv run pytest -q` — full suite green.
</verification>

<success_criteria>
- engine/seeding.py is a pure module exporting `seed_next_stage` that imports `engine.probs.difficulty` (no second Buchholz copy) and `engine.teams.Team`/`ADVANCE_AT_WINS`, with a documented `(losses, -difficulty, prior_seed)` tiebreak chain.
- The SEED-02 two-run determinism gate passes byte-identical; the hand-worked tie + loss-bucket-outranks-Buchholz tests pass; the merge test proves invited 1-8 / qualifiers 9-16 (not interleaved).
- The purity + canonical-Buchholz source guards pass (no streamlit/httpx/requests, no RNG affecting output, no inline Buchholz).
- GATE-01 green, no engine file touched, full suite green; the plan produced RED->GREEN(->REFACTOR) atomic commits.
</success_criteria>

<output>
Create `.planning/phases/07-stage-2-inter-stage-seeding-chain/07-02-SUMMARY.md` when done.
</output>
