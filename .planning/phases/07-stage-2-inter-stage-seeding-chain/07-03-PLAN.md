---
phase: 07-stage-2-inter-stage-seeding-chain
plan: 03
type: execute
wave: 2
depends_on: ["07-02-stage-2-inter-stage-seeding-chain"]
files_modified:
  - engine/seeding.py
  - tests/test_seeding.py
  - app.py
  - ui/state.py
autonomous: true
requirements: [SEED-03]
tags: [seeding, completeness-gate, live-prefix, app-wiring]

must_haves:
  truths:
    - "When Stage 1 is COMPLETE and validated (every round fully locked AND every team terminated at 3W/3L, via the engine.live.LivePrefixIncomplete / full-lock-prefix precondition), the next stage's seeds auto-derive from it through seed_next_stage; a PARTIAL/incomplete Stage 1 produces NO seed list (SEED-03)."
    - "The standings the chain consumes come from the deterministic FULL-LOCK replay of the prior stage (simulate_stage with locked=all_results), so it is RNG-invariant — two different throwaway RNG seeds give identical derived seeds."
    - "Derived Stage-2 seeds feed the UI at runtime as an editable [INFERRED] session overlay (NOT a fixture rewrite); data/stage2.json stays the editable fallback and the per-stage seeds_confirmed=false banner persists until reconciled."
    - "GATE-01 stays green — the completeness gate REUSES engine.live (no engine mutation); app wiring imports seed_next_stage and edits no frozen engine file."
  artifacts:
    - path: "engine/seeding.py"
      provides: "stage_is_complete(teams, locked_results, S) completeness gate reusing the LivePrefixIncomplete / full-lock precondition + the full-lock replay standings helper"
      contains: "LivePrefixIncomplete"
    - path: "tests/test_seeding.py"
      provides: "SEED-03 complete->seeds / partial->none + RNG-invariant full-lock replay tests"
      contains: "test_partial_stage_yields_no_seeds"
    - path: "app.py"
      provides: "Auto-derivation glue: complete prior stage -> derive + offer editable [INFERRED] Stage-2 seeds overlay; partial -> no seed list"
      contains: "seed_next_stage"
  key_links:
    - from: "engine/seeding.py (stage_is_complete)"
      to: "engine.live.legal_pairings_for_round / LivePrefixIncomplete"
      via: "reuse the full-lock-prefix precondition; partial prefix -> not complete -> no derivation"
      pattern: "legal_pairings_for_round|LivePrefixIncomplete"
    - from: "app.py"
      to: "engine.seeding.seed_next_stage + stage_is_complete"
      via: "guarded auto-derive behind the completeness gate (try/except-LivePrefixIncomplete idiom)"
      pattern: "seed_next_stage"
    - from: "app.py (derived seeds)"
      to: "the editable session overlay + per-stage [INFERRED] banner"
      via: "session overlay key, seeds_confirmed=false; no fixture rewrite"
      pattern: "seeds_confirmed_"
---

<objective>
Land Slice 2's wiring (SEED-03): auto-derive Stage 2's seeds from a COMPLETE Stage 1 by reusing
the existing `engine.live.LivePrefixIncomplete` / full-lock-prefix completeness precondition, and
feed the derived seeds into the app as an editable `[INFERRED]` session overlay — replacing the
hand-seeded fallback the moment Stage 1's finals are locked. A partial/incomplete Stage 1 produces
NO seed list (never seed off sampled winners).

This depends on Plan 02 (`engine.seeding.seed_next_stage`). It adds a small pure completeness gate
+ standings helper to `engine/seeding.py` (`stage_is_complete` reusing the live full-lock machinery
+ the `simulate_stage(..., locked=all_N, pairings_out=[])` replay trick — decisions #4/#5), extends
`tests/test_seeding.py` with the SEED-03 completeness + RNG-invariance tests, and wires the app glue
behind the gate using the EXACT try/except-`LivePrefixIncomplete` idiom `_prefill_results_into_locked`
already uses (app.py lines 922-938). The derived seeds are a SESSION OVERLAY, not a fixture rewrite
(decisions #6, mirroring the Phase-6 results pre-fill pattern); `data/stage2.json` stays the editable
fallback and the per-stage `seeds_confirmed=false` banner (STG-05, shipped Phase 6) persists.

Purpose: SEED-03 — "when a stage's results are complete and validated (reusing the
LivePrefixIncomplete guard), the next stage's seeds auto-derive; a partial stage produces no seeds."
Output: engine/seeding.py (+stage_is_complete + replay helper), tests/test_seeding.py (+SEED-03
tests), app.py (auto-derive glue + editable [INFERRED] overlay), ui/state.py (the overlay session key).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/07-stage-2-inter-stage-seeding-chain/07-RESEARCH.md
@.planning/phases/07-stage-2-inter-stage-seeding-chain/07-PATTERNS.md
@.planning/phases/07-stage-2-inter-stage-seeding-chain/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Completeness gate + full-lock replay helper in engine/seeding.py (SEED-03 core)</name>
  <files>engine/seeding.py, tests/test_seeding.py</files>
  <read_first>
    - engine/seeding.py (the Plan-02 module this task extends — keep seed_next_stage numpy-free; the new helpers may import engine.live + engine.swiss, which are themselves streamlit-free)
    - engine/live.py lines 209-217 (`LivePrefixIncomplete` — the EXACT exception SEED-03 names)
    - engine/live.py lines 269-316 (`legal_pairings_for_round`: the full-lock-prefix precondition — raises LivePrefixIncomplete unless every round r < round_idx is fully locked)
    - engine/live.py lines 247-266 (`derive_bracket`: the `simulate_stage(_fresh_teams(teams), None, S, default_rng(0), dict(locked), pairings_out=[])` full-lock replay trick to reuse for standings; `_fresh_teams` lines 236-244)
    - engine/live.py lines 47-58 (`locked_dict_from_results`: lock-list -> engine `locked` dict projection) + lines 64-79 (`_records_from_locked`: running (wins,losses) tally)
    - engine/swiss.py lines 262-298, 319-338 (`simulate_stage` returns `{id: Team}` with `.opps` populated; the active-team filter `t.wins < ADVANCE_AT_WINS and t.losses < ELIMINATE_AT_LOSSES`)
    - engine/teams.py lines 50-72 (Team dataclass), ADVANCE_AT_WINS/ELIMINATE_AT_LOSSES (lines 22-23)
    - .planning/phases/07-stage-2-inter-stage-seeding-chain/07-RESEARCH.md lines 233-250 (Pattern 3: completeness-gated auto-derive) + lines 320-386 (the SEED-03 / RNG-invariant test code examples)
    - tests/test_seeding.py (the Plan-02 test file this task extends; reuse its `_build_finished_stage1_replay` helper) + tests/test_backtest_budapest_2025.py lines 47-89 (the fixture->locked idiom for building partial vs full lock lists)
  </read_first>
  <action>
    TDD: extend tests/test_seeding.py FIRST with the SEED-03 tests (RED), then add the helpers to
    engine/seeding.py (GREEN).

    RED — add to tests/test_seeding.py: `test_partial_stage_yields_no_seeds` — build a Stage-1 lock
    list covering only rounds 0..2 (rounds 3-4 unlocked) from tests/fixtures/budapest_2025_stage1.json
    (the lock entries carry `round_idx`; filter to the first three rounds) and assert
    `stage_is_complete(stage1_teams, partial_locks, S=40.0)` is False (the app/helper must then NOT
    derive). `test_complete_stage_is_complete` — the FULL Budapest lock list (all rounds) ->
    `stage_is_complete(...)` is True. `test_full_lock_replay_is_rng_invariant` — derive the standings
    via the full-lock replay TWICE (the helper uses a fixed throwaway rng internally; assert two calls
    return identical `{id: (wins, losses)}`) AND assert `seed_next_stage(final, invited)` is identical
    across the two derivations (a fully-locked prefix never samples, so the replay is RNG-invariant —
    07-RESEARCH.md). Run the file; the three new tests fail (helpers absent). Commit:
    `test(07-03): add SEED-03 completeness + RNG-invariant replay tests (RED)`.

    GREEN — add to engine/seeding.py: (1) a standings helper `final_standings_from_locked(teams,
    locked_results, S) -> dict[int, Team]` that builds the engine locked dict via
    `engine.live.locked_dict_from_results(locked_results)` and replays
    `simulate_stage(_fresh_teams(teams), None, S, np.random.default_rng(0), dict(locked),
    pairings_out=[])` (import `_fresh_teams` from engine.live OR re-implement the one-line fresh-team
    rebuild locally to avoid a private import — document the throwaway-rng determinism in a comment
    mirroring engine/live.py line 257). This is the ONLY place numpy enters seeding.py and it does NOT
    affect output (full lock -> no sampling), so the SEED-02 purity claim on `seed_next_stage` itself
    is unaffected — keep `seed_next_stage` numpy-free and confine the import to this helper. (2)
    `stage_is_complete(teams, locked_results, S) -> bool`: replay to get the standings, then return
    True iff every team in the replayed standings has terminated (`w >= ADVANCE_AT_WINS or l >=
    ELIMINATE_AT_LOSSES`) AND the full prefix is locked — verify the latter by calling
    `engine.live.legal_pairings_for_round(teams, locked_results, S, num_rounds)` inside a try/except:
    if it raises `LivePrefixIncomplete` (a partial prefix) return False; reuse the precondition rather
    than reinventing it (decisions #5). Derive `num_rounds` from the replay's `pairings_out` length
    (ask for the round PAST the last so an incomplete prefix raises). Run the tests GREEN. Commit:
    `feat(07-03): completeness gate + full-lock replay standings (GREEN)`.

    Do NOT edit engine/live.py, engine/swiss.py, engine/teams.py, engine/montecarlo.py, or
    engine/probs.py — `stage_is_complete`/`final_standings_from_locked` are NEW seeding.py helpers that
    CALL the frozen engine + reuse the live precondition; they never mutate it.
  </action>
  <verify>
    <automated>cd "C:\Users\sweek\Documents\Claude\Projects\cs2 major sim" && uv run pytest tests/test_seeding.py tests/test_backtest_budapest_2025.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `engine.seeding.stage_is_complete` and `engine.seeding.final_standings_from_locked` exist and are importable.
    - `test_partial_stage_yields_no_seeds` (partial -> not complete), `test_complete_stage_is_complete` (full -> complete), and `test_full_lock_replay_is_rng_invariant` (two derivations -> identical standings + identical derived seeds) all pass.
    - `stage_is_complete` reuses `engine.live.legal_pairings_for_round` / catches `LivePrefixIncomplete` (the source contains `LivePrefixIncomplete` or imports it from engine.live).
    - `git diff --name-only HEAD` over engine/live.py, engine/swiss.py, engine/teams.py, engine/montecarlo.py, engine/probs.py is EMPTY.
    - GATE-01 (`tests/test_backtest_budapest_2025.py`) green.
  </acceptance_criteria>
  <done>engine/seeding.py has a completeness gate + RNG-invariant full-lock replay standings helper, both reusing the frozen engine + live precondition; SEED-03 completeness tests green; no frozen engine edit.</done>
</task>

<task type="auto">
  <name>Task 2: Wire auto-derivation into app.py — editable [INFERRED] Stage-2 seed overlay behind the completeness gate (SEED-03 UI)</name>
  <files>app.py, ui/state.py</files>
  <read_first>
    - app.py lines 860-946 (`_prefill_results_into_locked`: the canonical try/except-`LivePrefixIncomplete` "guard the apply behind the complete prefix, swallow on partial -> do nothing" idiom to mirror — lines 922-938 are the exact pattern)
    - app.py lines 303-319 (the stage selector: `_STAGE_LABELS`, `stage_id`, `teams, _stage_cfg = load_stage(_path_for_stage(stage_id))`, `by_seed`)
    - app.py lines 195-242 (the per-stage `seeds_confirmed_{stage_id}` banner + reconcile rows — the editable [INFERRED] surface the derived seeds feed; do NOT bypass it)
    - app.py lines 324-340 (the Ratings `data_editor`: the editable-overlay precedent — rows are `{"seed","team","rating"}` per team; the derived overlay rebinds the editor source the same way a stage switch rebinds `teams` at line 318)
    - app.py lines 1286-1296 (where `_prefill_results_into_locked()` is called in the run flow — the derive glue runs alongside, consuming KEY_LOCKED for the prior stage)
    - app.py lines 142-163 (`_STAGE_NUMBERS` / `_stage_int_for` — the stage_id<->int reconciliation; reuse to map "current stage -> prior stage" for the derivation)
    - ui/state.py lines 46-86 (the `KEY_*` session-key block to add `KEY_DERIVED_SEEDS` to), 79 (`KEY_LOCKED`), 211-247 (`add_lock`/`locked_dict`), 146-159 (`read_seeds_confirmed`)
    - ui/cache.py lines 39-48 (`_path_for_stage`)
    - engine/seeding.py (the Plan-02 `seed_next_stage` + this plan's Task-1 `stage_is_complete`/`final_standings_from_locked`, and the `InvitedTeam` descriptor shape — the functions/types app.py calls)
    - .planning/phases/07-stage-2-inter-stage-seeding-chain/07-RESEARCH.md lines 423-425 (decision: hold derived seeds as an editable SESSION OVERLAY, NOT a fixture rewrite) + 07-PATTERNS.md lines 209-236 (the app wiring pattern)
  </read_first>
  <action>
    First add `KEY_DERIVED_SEEDS = "derived_seeds_overlay"` to the `KEY_*` block in ui/state.py
    (lines 46-86), with a comment naming it the per-stage derived-seed session overlay (SEED-03). Then
    add the auto-derivation glue to app.py mirroring the `_prefill_results_into_locked` discipline. Add
    a helper (e.g. `_derive_next_stage_seeds()`) that: reads the PRIOR stage's `KEY_LOCKED` lock list,
    calls `engine.seeding.stage_is_complete(prior_teams, prior_locked_results, S)`, and ONLY when True
    derives `final = engine.seeding.final_standings_from_locked(prior_teams, prior_locked_results, S)`
    then `derived = engine.seeding.seed_next_stage(final, invited)` — where `invited` is read from the
    next stage's fixture (the first 8 seed rows of data/stage2.json, per Plan-01 Open-Question-2
    resolution) as `engine.seeding.InvitedTeam` descriptors. Wrap the completeness/derive in the SAME
    try/except idiom as app.py lines 935-938 (`except Exception: # incomplete prefix -> no derivation`
    -> return/skip, emitting NO seed list — SEED-03 partial-stage behavior). On a successful derive,
    store the derived seeds in `st.session_state[KEY_DERIVED_SEEDS]` as an EDITABLE SESSION OVERLAY the
    Stage-2 ratings/seed editor reads as its starting rows — NOT a write to data/stage2.json
    (decisions #6; mirror the results pre-fill which holds fetched data in session). The derived
    overlay carries `seeds_confirmed=false` semantics: do NOT flip the per-stage
    `seeds_confirmed_{stage_id}` toggle; the [INFERRED] banner (app.py lines 220-242) MUST still show
    for Stage 2 until the user reconciles. Surface a one-line caption when the overlay is active
    ("Stage-2 seeds derived from the locked Stage-1 finals — [INFERRED], verify vs the official seed
    list"); when Stage 1 is incomplete, leave the existing fixture-based Stage-2 fallback in place (the
    banner already says seeds are inferred). Determine "prior stage of the current" via the existing
    `_STAGE_NUMBERS`/`_stage_int_for` mapping (Stage 2 derives from Stage 1). Do NOT edit any frozen
    engine file; do NOT rewrite data/stage2.json from the app; all wiring is session/UI only.
  </action>
  <verify>
    <automated>cd "C:\Users\sweek\Documents\Claude\Projects\cs2 major sim" && python -c "import ast; src=open('app.py',encoding='utf-8').read(); assert 'seed_next_stage' in src and 'stage_is_complete' in src, 'derive glue missing'; ast.parse(src); st=open('ui/state.py',encoding='utf-8').read(); assert 'KEY_DERIVED_SEEDS' in st, 'overlay key missing'; print('derive glue + overlay key present, app.py parses')" && uv run pytest tests/test_app.py tests/test_seeding.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `ui/state.py` defines `KEY_DERIVED_SEEDS`; `app.py` imports and calls `engine.seeding.seed_next_stage` + `stage_is_complete` (both names appear in the source) and parses clean (`ast.parse` succeeds).
    - The derive glue is gated behind `stage_is_complete` using the try/except-`LivePrefixIncomplete` idiom (a partial prior stage emits NO derived seed list).
    - Derived seeds are stored in `st.session_state[KEY_DERIVED_SEEDS]` (a SESSION overlay) — there is no `open('data/stage2.json', 'w')` / fixture write in app.py.
    - The per-stage `seeds_confirmed_{stage_id}` banner logic is NOT bypassed (no `seeds_confirmed: true` is set for Stage 2 by the derivation).
    - `uv run pytest tests/test_app.py tests/test_seeding.py -x -q` exits 0.
  </acceptance_criteria>
  <done>app.py auto-derives Stage-2 seeds from a complete locked Stage-1 (via the completeness gate) and offers them as an editable [INFERRED] session overlay; a partial Stage-1 produces no seed list; the per-stage banner persists; no frozen engine edit, no fixture rewrite.</done>
</task>

<task type="auto">
  <name>Task 3: Freeze + full-suite proof — GATE-01 green, no frozen engine edit, SEED-03 path green end-to-end</name>
  <files>(verification-only — runs git diff + pytest; touches no source)</files>
  <read_first>
    - tests/test_backtest_budapest_2025.py lines 62-113 (GATE-01 — the cross-cutting exit criterion)
    - tests/test_teams_load_stage.py lines 131-144 (`test_load_teams_unchanged` — freeze regression)
    - .planning/phases/07-stage-2-inter-stage-seeding-chain/07-VALIDATION.md lines 33-41 (sampling rate: after every wave run the full suite + GATE-01; before verify-work the two-run determinism gate must be green)
    - .planning/phases/07-stage-2-inter-stage-seeding-chain/07-RESEARCH.md lines 483-488 (GATE-01 stays green by construction — no FROZEN engine file touched across the whole phase; the NEW engine/seeding.py sibling is permitted)
  </read_first>
  <action>
    Run the end-of-slice freeze proof. Confirm NO FROZEN engine file changed across the phase by
    running `git diff --name-only <phase-base> HEAD` scoped to engine/swiss.py, engine/montecarlo.py,
    engine/teams.py, engine/probs.py, engine/live.py and asserting the output is EMPTY (the only
    engine-tree file ADDED in the phase is the NEW engine/seeding.py sibling, which is permitted; the
    five FROZEN files must show no diff). Use the phase base commit
    bbda143483d6535a8e019b0930b2a219a9c262da. Then run: the GATE-01 backtest,
    `test_load_teams_unchanged`, the full `tests/test_seeding.py` (SEED-01 merge + SEED-02 determinism
    gate + SEED-03 completeness/RNG-invariance), `tests/test_stage2.py` (STG-02 from Plan 01), and the
    full suite — all green. If any FROZEN engine file shows a diff, STOP and revert it out of the
    engine (the seeding chain must ride on the frozen engine, never edit it — GATE-01). Do not loosen
    any assertion, especially the two-run determinism gate.
  </action>
  <verify>
    <automated>cd "C:\Users\sweek\Documents\Claude\Projects\cs2 major sim" && git diff --name-only bbda143483d6535a8e019b0930b2a219a9c262da HEAD -- engine/swiss.py engine/montecarlo.py engine/teams.py engine/probs.py engine/live.py > diff_frozen.txt && powershell -Command "if ((Get-Content diff_frozen.txt) -and (Get-Content diff_frozen.txt).Length -gt 0) { Write-Error 'FROZEN ENGINE FILE CHANGED - GATE-01 violation'; exit 1 } else { Write-Host 'frozen engine untouched (seeding.py sibling permitted)' }" && del diff_frozen.txt && uv run pytest tests/test_backtest_budapest_2025.py tests/test_teams_load_stage.py::test_load_teams_unchanged tests/test_seeding.py tests/test_stage2.py -x -q && uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only bbda143...HEAD` over engine/swiss.py, engine/montecarlo.py, engine/teams.py, engine/probs.py, engine/live.py is EMPTY (frozen engine untouched across the phase).
    - `uv run pytest tests/test_backtest_budapest_2025.py -q` exits 0 (GATE-01 green).
    - `uv run pytest tests/test_teams_load_stage.py::test_load_teams_unchanged -q` exits 0 (freeze regression green).
    - `uv run pytest tests/test_seeding.py tests/test_stage2.py -q` exits 0 (SEED-01/02/03 + STG-02 green).
    - `uv run pytest -q` exits 0 (full suite green).
  </acceptance_criteria>
  <done>The whole Phase-7 seeding chain is freeze-clean: no FROZEN engine file touched, GATE-01 green, load_teams unchanged, SEED-01/02/03 + STG-02 green, full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prior-stage locked results (KEY_LOCKED) -> seeding chain | seeding off a PARTIAL (unlocked / sampled-winner) stage produces a single-RNG-draw bracket; only a fully-locked complete stage is a known fact |
| derived seed overlay (session) -> Stage-2 editor/sim | the overlay is [INFERRED] + editable; presenting it as confirmed would launder a guess into false precision |
| frozen engine path (GATE-01) <-> seeding chain | the completeness gate + replay REUSE engine.live/engine.swiss; any mutation of a frozen engine file breaks the backtest |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | Tampering | seeding off a partial/sampled stage | mitigate | `stage_is_complete` reuses the `LivePrefixIncomplete` / full-lock-prefix precondition; `test_partial_stage_yields_no_seeds` asserts a partial stage derives NO seeds (SEED-03, V5 Input Validation) |
| T-07-10 | Tampering | non-deterministic standings from the replay | mitigate | the full-lock replay never samples (locked branch in `_play`); `test_full_lock_replay_is_rng_invariant` asserts identical standings + identical derived seeds across derivations |
| T-07-11 | Information disclosure (false precision) | derived seeds presented as authoritative | accept | the overlay carries seeds_confirmed=false; the shipped per-stage [INFERRED] banner (STG-05) stays loud + a "[INFERRED], verify vs official" caption; reconcile at event time (A1) |
| T-07-12 | Tampering | frozen engine path (GATE-01) | mitigate | Task 3 asserts an EMPTY `git diff` over the five FROZEN engine files + GATE-01 + `test_load_teams_unchanged` green; only the NEW engine/seeding.py sibling is added |
| T-07-13 | Tampering | silent fixture overwrite of data/stage2.json from the app | mitigate | the derivation writes a SESSION overlay only; Task-2 verify asserts no `open('data/stage2.json','w')` in app.py — the committed [INFERRED] fixture stays the editable baseline |

Reuses existing seams; no network, no auth, no new endpoints, no package installs. No high-severity threats.
</threat_model>

<verification>
- `uv run pytest tests/test_seeding.py -x -q` — SEED-03 completeness (complete->seeds / partial->none) + RNG-invariant replay, alongside the Plan-02 SEED-01/02 gates, all green.
- `uv run pytest tests/test_app.py -x -q` — the app still parses + runs (AppTest) with the derive glue wired.
- `uv run pytest tests/test_backtest_budapest_2025.py -q` — GATE-01 green.
- `git diff --name-only bbda143...HEAD` over the five frozen engine files empty — no frozen engine edit (seeding.py sibling permitted).
- `uv run pytest -q` — full suite green.
</verification>

<success_criteria>
- engine/seeding.py has a `stage_is_complete` completeness gate (reusing `engine.live.LivePrefixIncomplete` / the full-lock-prefix precondition) and an RNG-invariant `final_standings_from_locked` full-lock replay helper.
- When Stage 1 is complete + validated, the app auto-derives Stage-2 seeds and offers them as an editable [INFERRED] session overlay; a partial Stage 1 produces NO seed list; the per-stage seeds_confirmed=false banner persists.
- Derived seeds are a session overlay (KEY_DERIVED_SEEDS), never a fixture rewrite of data/stage2.json.
- GATE-01 green, no FROZEN engine file touched (only the NEW engine/seeding.py sibling added), load_teams unchanged, SEED-01/02/03 + STG-02 + full suite green.
</success_criteria>

<output>
Create `.planning/phases/07-stage-2-inter-stage-seeding-chain/07-03-SUMMARY.md` when done.
</output>
